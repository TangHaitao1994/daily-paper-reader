---
title: Repurposing a Speech Classifier for Guided Diffusion-Based Speech Generation
title_zh: 将语音分类器重新用于基于扩散的引导式语音生成
authors: "Rostislav Makarov, Timo Gerkmann"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20457v1"
tags: ["query:speech-model"]
score: 8.0
evidence: 重新利用语音分类器进行引导扩散式语音生成
tldr: 传统分类器指导扩散生成需分别训练分类器与扩散模型，资源开销大。本工作提出将预训练语音分类器直接作为生成主干，仅附加轻量子网络并重用它提取的中间表征，通过去噪得分匹配目标训练，实现条件语音合成。实验表明，该方法在单个模型内完成高质量语音生成，显著降低内存占用与计算成本，为判别模型向生成任务迁移提供了有效途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20457v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1545, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20457v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 825, \"height\": 1007, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20457v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1767, \"height\": 619, \"label\": \"Table\"}]"
motivation: 分类器指导扩散生成需维护两个独立模型，导致存储和计算冗余，寻求更紧凑的单一模型方案。
method: 冻结基于对数梅尔谱的噪声条件分类器，附加轻量子网络复用其内部表征，仅训练该子网络以执行去噪得分匹配。
result: 所提重用途方法同单个主干模型生成高品质语音，且相比于两模型方案减少了内存与计算开销。
conclusion: 预训练语音分类器可被成功重用于扩散生成，为判别与生成模型融合提供了轻量级高效方案。
---

## 摘要
分类器引导是一种通过使用以噪声为条件的分类器来引导采样过程朝向目标类别，从而控制扩散生成的方法。分类器引导的一个缺点是需要两个分别训练的模型：一个分类器和一个扩散模型。因此，我们研究了一种更紧凑的替代方案，将常规训练的语音分类器重新用作扩散生成的骨干网络。从冻结的、在log-Mel空间中以噪声为条件的分类器出发，我们附加一个轻量级子网络，复用分类器的中间表示，并仅在去噪得分匹配目标下训练该子网络。我们的工作表明，预训练的分类器可以被重新用于条件生成，在判别式建模和条件语音合成之间架起一座有吸引力的桥梁，从而在单骨干模型中实现高语音质量，同时减少内存占用和计算成本。

## Abstract
Classifier guidance is a way to control diffusion generation by using a noise-conditioned classifier to steer the sampling process toward a target class. One drawback of classifier guidance is that it requires two separately trained models: a classifier and a diffusion model. We therefore study a more compact alternative in which a conventionally trained speech classifier is repurposed as the backbone for diffusion generation. Starting from a frozen noise-conditioned classifier in log-Mel space, we attach a lightweight subnetwork that reuses intermediate classifier representations and train only this subnetwork under a Denoising Score Matching objective. Our work shows that a pretrained classifier can be repurposed for conditional generation, providing an appealing bridge between discriminative modeling and conditional speech synthesis resulting in high speech quality within a single-backbone model, with reduced memory footprint and computational cost.