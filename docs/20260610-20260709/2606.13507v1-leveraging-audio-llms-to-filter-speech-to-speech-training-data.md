---
title: Leveraging Audio-LLMs to Filter Speech-to-Speech Training Data
title_zh: 利用音频大语言模型过滤语音到语音训练数据
authors: "Qixu Chen, Satoshi Nakamura"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13507v1"
tags: ["query:speech-model"]
score: 9.0
evidence: 训练音频语言模型过滤噪声语音到语音训练数据
tldr: 大规模语音挖掘语料为端到端语音到语音翻译（S2ST）提供丰富训练数据，但常含噪声、错位和语义错误。提出利用音频大语言模型，从原始语音对直接判断是否保留训练样本。采用两阶段秩到蒸馏策略生成可靠伪标签并训练过滤模型，无需人工标注。在CVSS-C和SpeechMatrix数据集上显著提升S2ST性能，最高ASR-BLEU增益达1.4，验证了基于声学保真度与跨语言语义一致性的数据筛选有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13507v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1770, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13507v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1636, \"height\": 514, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13507v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 852, \"height\": 942, \"label\": \"Table\"}]"
motivation: 现有大规模语音翻译训练数据噪声多，直接训练损害性能，需要高效、可扩展的自动过滤方法。
method: 通过轻量级排序器生成保留/丢弃伪标签，再训练音频大语言模型直接从语音对预测筛选决策的Rank-to-Distill策略。
result: 在CVSS-C和SpeechMatrix上，过滤后训练端到端S2ST，ASR-BLEU最高提1.4，优于无过滤基线。
conclusion: 音频大语言模型可联合声学与语义一致性有效过滤语音训练数据，无需人工标注，提升S2ST鲁棒性。
---

## 摘要
大规模挖掘的语料库为端到端语音到语音翻译（S2ST）提供了丰富的训练数据，但可能包含噪声、不对齐和语义错误。过滤噪声数据对于保持稳健的语音翻译性能至关重要。我们研究如何训练一个音频语言模型，直接从音频对配对的语音做出保留或丢弃的决策。为了在没有人工标注的情况下获得可靠的监督，我们采用了一种可扩展的两阶段Rank-to-Distill策略。一个轻量级排序器从带噪语音对中生成保留或丢弃的伪标签，然后训练一个音频大语言模型直接从原始配对语音中预测保留或丢弃。所得模型能够联合捕获声学保真度和跨语言语义一致性，用于语音条件数据的选择。在CVSS-C和SpeechMatrix上的实验表明，与未过滤训练相比，性能持续提升，端到端S2ST的ASR-BLEU最高提升+1.4。

## Abstract
Large-scale mined corpora provide abundant training data for end-to-end speech-to-speech translation (S2ST) but may contain noise, misalignment, and semantic errors. Filtering noisy data is crucial to maintain robust speech translation performance. We study how to train an audio-language model to make keep/drop decisions on paired speech directly from audio. To obtain reliable supervision without manual labels, we adopt a scalable two-stage Rank-to-Distill strategy. A lightweight ranker generates keep/drop pseudo-labels from noisy speech pairs, then trains an audio large language model to predict keep/drop directly from raw paired speech. The resulting model jointly captures acoustic fidelity and cross-lingual semantic consistency for the selection of speech-conditioned data. Experiments on CVSS-C and SpeechMatrix show consistent improvements over unfiltered training, yielding up to +1.4 ASR-BLEU for end-to-end S2ST.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：端到端语音到语音翻译（S2ST）依赖于大规模挖掘或自动对齐的配对语音数据，但这些数据中含有大量噪声、分割错误、翻译不一致等质量问题，直接使用会损害模型训练。
- **整体含义**：提出一种直接基于原始音频的语音对筛选方法，通过训练音频大语言模型来判定某个语音对是否应该保留用于S2ST模型训练，从而自动过滤低质量数据，提升翻译性能，且无需人工标注。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
- **核心思想**：利用两阶段的“Rank → Distill”自举策略：先通过弱监督训练一个轻量级排序器，该排序器可对大量无标签语音对进行打分，然后取高、低极值作为伪标签，再用这些伪标签微调一个音频大语言模型，使其能直接从原始语音对预测“保留”或“丢弃”。
- **关键技术细节**：
  - **质量信号构建**：从声学保真度（SNR）、感知质量（MOS）、语义一致性（LLM评判、BLEURT、BLASER 2.0‑QE等）多个维度自动计算质量分数，用于选出高置信度的“干净”和“噪声”样例。
  - **训练对构建**：从SpeechMatrix中根据严格阈值选出干净语音对作为正例，对其施加控制的声学噪声、时序错位、压缩损伤等生成负例，构造有监督的排序训练集。
  - **阶段一：Ranker**：基于LambdaMART（LightGBM）训练一个轻量级模型，对语音对输出连续质量分数；然后在未标注池上取top‑K 和 bottom‑K作为伪标签（keep/drop）。
  - **阶段二：Distill**：将伪标签语音对格式化为指令跟随样本，微调音频LLM（如Qwen2‑Audio），使其直接从源、目标语音预测keep或drop。
- **公式/算法流程（文字说明）**：
  - Ranker训练使用成对偏序损失：$$L_{\text{rank}} = -\log \sigma\big(g(x_i) - g(x_j)\big)$$，其中 $g(\cdot)$ 为评分函数。
  - 最终过滤框架：给定全量候选语音对，Ranker评分 → 选取top‑K与bottom‑K → 微调音频LLM → 推理时用音频LLM直接过滤。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- **数据集**：
  - 主实验在CVSS‑C（法语→英语），并提供高质量合成目标语音与真实源语音。
  - 额外引入SpeechMatrix作为大规模挖掘数据（法语→英语），仅过滤SpeechMatrix部分，CVSS‑C全部保留。
  - 仅保留SpeechMatrix的20%参与过滤实验。
  - 通用性实验：德语→英语（DE→EN）在相同设置下验证。
- **Benchmark 与评价指标**：端到端S2ST模型训练后的ASR‑BLEU（sacreBLEU），使用Fairseq S2UT配方和固定wav2vec 2.0 ASR评估。
- **对比方法**：
  - 未过滤基线（保留全部SpeechMatrix）。
  - 基于声学指标的直接过滤：SNR阈值过滤、MOS阈值过滤。
  - 基于语义的过滤：LLM（8B/70B）评判、BLEURT、BLASER 2.0‑QE。
  - 与本方法的阶段一（Ranker）直接过滤进行对比。
  - 随机保留同等数量数据。
  - 消融实验：仅使用语义信号、替换音频LLM架构（Audio Flamingo 3）等。

### 4. 资源与算力
- **S2ST训练算力**：4×A100 GPU，300k max updates，FP16，早停patience=5。
- **音频LLM微调**：使用4‑bit量化，LoRA（r=16，α=32），训练2个epoch，学习率2×10⁻⁴，batch=8，梯度累积4步，但未明确说明所用GPU数量或训练时长。
- **Ranker训练**：LightGBM轻量级模型，消耗算力极小，未给出具体资源。

### 5. 实验数量与充分性
- **主对比实验**：表1列出20行结果，覆盖无过滤、声学过滤、语义过滤、随机保留、本文完整方法及消融配置。
- **消融实验**：
  - 阶段一缺失（直接音频LLM过滤）效果极差，证明合成扰动无法捕获真实噪声分布。
  - 去除阶段二（仅用Ranker）并调整数据量匹配。
  - 声学/感知信号缺失（仅语义）实验两例。
  - 模型替换：Qwen2‑Audio换成Audio Flamingo 3。
  - 语言方向泛化：DE→EN实验。
- **实验充分性**：对比基线多样，包含声学、语义、文本LLM、无参考QE等强基线；消融覆盖方法关键环节；在第二种语言对上验证了泛化性。实验在同等数据预算（477k对）下公平比较，结论较可靠。

### 6. 论文的主要结论与发现
- 音频LLM能直接从原始语音对中联合捕获声学质量与跨语言语义一致性，有效过滤S2ST训练数据。
- 两阶段Rank→Distill策略能在无人工标注下获得可靠伪标签，并成功蒸馏到音频LLM。
- 在CVSS‑C + 部分SpeechMatrix（法语→英语）上，音频LLM过滤后S2ST的ASR‑BLEU提升+1.4，优于同等数据预算下所有基线（包括70B文本LLM）。
- 仅靠声学或单一语义信号效果有限，多维度质量信号（声学+感知+语义）互补，可得到更优排序器。

### 7. 优点：方法或实验设计上有哪些亮点
- **无需人工标注**：全自动生成训练信号，具备良好扩展性。
- **直接音频理解**：避免ASR转录链式误差，音频LLM直接感知声学退化与跨语言对齐问题。
- **两阶段自举设计**：Ranker容易训练，且其极值伪标签质量高，有效指导音频LLM学习。
- **综合质量评估**：整合声学、感知、语义多个维度，比单一维度过滤更鲁棒。
- **实验严谨**：在相同数据预算下公平对比各种强基线，并有消融和跨语言验证。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **二元决策局限**：当前仅输出keep/drop，无法在固定数据总量下灵活控制保留比例，缺乏预算感知的选择能力。
- **排序器泛化风险**：排序器训练依赖于特定阈值筛选出的“干净”数据和合成扰动，可能未覆盖所有真实噪声模式。
- **语言方向和领域有限**：主要实验仅在法语→英语、德语→英语进行，尚未验证更多语言对或领域外数据。
- **音频LLM推理成本**：直接使用大型音频LLM过滤海量数据可能带来较高计算开销，文中未详细讨论其效率。
- **语义信号依赖外部模型**：构建监督信号使用了多个预训练模型（ASR、LLM、BLASER等），多信号组合的配置可能敏感，且引入额外资源需求。

（完）
