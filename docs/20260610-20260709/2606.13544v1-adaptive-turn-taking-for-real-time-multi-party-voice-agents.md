---
title: Adaptive Turn-Taking for Real-time Multi-Party Voice Agents
title_zh: 面向实时多方语音代理的自适应轮次控制
authors: "Soumyajit Mitra, Prabhat Pandey, Abhinav Jain, Shanmukha Sahith, K V Vijay Girish"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13544v1"
tags: ["query:speech-model"]
score: 9.0
evidence: 具有角色扮演话轮转换能力，用于多方对话的语音代理
tldr: "实时多方语音交互中，适时接话对语音代理极具挑战，因发言权动态竞争且用户期望多样，传统系统常忽略角色差异，引发误打断或响应缓慢。为应对此问题，我们设计了ModeratorLM，一个角色扮演式语音代理，以分配的角色为条件，利用语音大语言模型进行chunk-wise流式推理，并开发推理增强变体，通过思维链推理融合历史对话与角色信息，实现自适应接话决策。同时，我们创建了多角色口语对话数据集RolePlayConv以支持训练和评估。在真实会议与RolePlayConv上的实验显示，相对非角色条件基线，接话精确率提高逾40%，召回率提升逾70%，且误中断大幅降低。此工作验证了角色条件对提升多轮对话代理性能的关键作用，并为构建高自然度实时多方语音系统提供了有效方法和数据资源。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13544v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1548, \"height\": 538, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.13544v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 835, \"height\": 728, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13544v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1649, \"height\": 320, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13544v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 738, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13544v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 837, \"height\": 654, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.13544v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 828, \"height\": 414, \"label\": \"Table\"}]"
motivation: 多方语音对话中，动态发言权竞争和用户期望差异使得适时接话成为挑战，现有系统缺乏角色特定的接话策略，导致误打断或响应延时。
method: 提出ModeratorLM，一个基于语音大语言模型chunk-wise流式处理的角色扮演代理，通过思维链推理增强变体，融合对话历史与角色，自适应决策接话时机。
result: "相比非角色条件基线，接话精确率提高逾40%，召回率提升逾70%，误中断显著减少。"
conclusion: ModeratorLM通过角色条件接话有效提升多轮对话流畅性，为实时多方代理提供新范式，并贡献了RolePlayConv数据集。
---

## 摘要
多方对话中的轮次控制仍是语音代理的一个基本挑战，特别是在动态发言权竞争和用户期望不一的情况下。我们提出ModeratorLM，这是一个角色扮演语音代理，在多方环境中根据明确分配的角色来调节轮次行为。该系统基于以块流式方式运行的语音大语言模型。我们还引入了一种推理增强变体，它结合了对会话上下文和分配角色的思维链推理。我们构建了RolePlayConv，一个具有多样助手角色的大规模合成多方语音对话数据集。在真实会议数据和RolePlayConv上的实验表明，与无角色条件的基线相比，轮次精确率提高了40%以上，召回率提高了70%以上，同时显著减少了误报中断。

## Abstract
Turn-taking in multi-party spoken conversations remains a fundamental challenge for voice-based agents, particularly under dynamic floor competition and varying user expectations. We propose ModeratorLM, a role-playing voice agent that conditions turn-taking behavior on an explicitly assigned role in multi-party settings. The system is built on a speech large language model operating in chunk-wise streaming manner. We further introduce a reasoning-augmented variant that incorporates chain-of-thought reasoning over conversational context and the assigned role. We construct RolePlayConv, a large-scale synthetic dataset of spoken multi-party conversations with diverse assistant roles. Experiments on real-world meeting data and RolePlayConv show improved turn-taking precision by over 40% and recall by more than 70%, while substantially reducing false-positive interruptions compared to non-role-conditioned baselines.

---

## 论文详细总结（自动生成）

好的，我将基于提供的论文内容，以资深学术论文分析助手的身份，使用中文、Markdown格式，对《Adaptive Turn-Taking for Real-time Multi-Party Voice Agents》一文进行结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义
*   **研究动机与背景**：
    *   当前语音对话代理在**多方对话**中存在根本性挑战。传统的轮次控制依赖停顿、静音检测等声学语义线索，这在两人对话中有效，但难以推广到存在话语重叠、动态发言权竞争和协商分配的多方场景中。
    *   用户对会话代理的期望随语境而变化，例如有时希望其为被动倾听者，有时则是主动引导者，这催生了对**角色扮演语言代理**的需求。
    *   已有角色扮演研究主要停留在文本对话领域，忽略了**在实时语音、多方交互中，指定角色如何影响轮次控制行为**这一关键问题。

*   **整体含义**：
    *   本文旨在提出一个能够根据被明确赋予的角色来调节自身轮次行为的语音代理，使其在多方对话中知道“何时该说话”以及“是否该介入”，从而更自然地满足不同场景下的用户期望。

### 2. 论文提出的方法论
*   **核心思想**：
    *   构建一个名为 **ModeratorLM** 的角色扮演式语音代理，它将轮次决策与系统角色进行显式绑定，使模型能根据角色理解并执行不同的接话策略。

*   **关键技术细节与算法流程**：
    *   **系统架构 (ModeratorLM)**：
        *   **基础组件**：由一个语音编码器和一个骨干大语言模型构成。
        *   **流式处理**：系统以**动态大小的音频块**为单位进行流式处理。每个音频块经过语音编码器和可训练的线性投影层后，与对应带说话人标注的文本转录一起，顺序地附加到LLM的上下文中。
        *   **端到端轮次决策**：与依赖外置语音活动检测模块的方案不同，本系统将轮次决策完全交由语音LLM处理。针对每个输入音频块，LLM生成两种可能的输出：
            1.  `轮次 + 响应`：输出控制令牌，表示助手应接管发言权，并随后生成助手的文本回复。
            2.  `无轮次`：输出空序列，表示助手不采取任何行动。
    *   **推理增强变体 (ModeratorLM-Think)**：
        *   在潜在的轮次点，LLM会先进行**思维链推理**，分析会话上下文和角色义务，生成推理文本，然后再输出接话与否的决策。这为决策提供了认知基础。
    *   **RolePlayConv数据集构建管道**：
        *   **角色设定**：策划了125个详细的助手角色（如“42岁的印度CEO.。.”），包含沟通风格、语气等属性。
        *   **对话生成**：使用LLM基于随机采样的角色和主题，生成3-6位说话者的连贯多方对话，并对说话长度进行限制以贴近口语。
        *   **推理轨迹增强**：为助手及部分非助手回合生成LLM推理轨迹，作为训练ModeratorLM-Think的监督信号。
        *   **语音合成**：使用文本转语音模型，为每个说话者分配不同声音属性，并在句子间插入从真实对话数据估计的静音间隔，合成最终的语音对话。总计生成了约7.5万个训练对话。

### 3. 实验设计
*   **数据集与场景**：
    *   **NOTSOFAR-1 (NSF-1)**：真实录制的正式会议数据，约4人，平均时长6分钟。论文为其设计了通过LLM和人工评估混合的方式指定“助手”角色及其描述。
    *   **RolePlayConv评估集**：采用与训练集相同的生成管道，但使用**在训练阶段未见过的角色**和**不同的LLM** 进行生成，以严格评估零样本泛化能力。

*   **评估指标**：
    *   **客观指标**：将轮次决策视为二分类任务，评估精确率(@P)、召回率(@R)、F1分数(@F1)、宏平均准确率(@A)、误报率(@FP)。同时定义了**被动未响应率 (@RM)**，用于衡量当说话人直接对助手发言时，模型未能接话的比例。
    *   **主观指标**：采用**LLM-as-a-Judge**方法，由裁判模型根据角色描述和对话历史，对轮次决策的恰当性（0-1分）和回复内容的角色保真度（1-10分）进行评估。

*   **对比方法**：
    *   **Moshi**：为两人对话设计的语音基座模型，本文评估其在多方场景下的直接表现。
    *   **MP-Baseline**：同样在RolePlayConv数据集上训练的多方语音代理，但在训练期间**移除了角色条件信号**。此基线用于精确量化角色条件的作用。
    *   **ModeratorLM (本文方法)**。
    *   **ModeratorLM-Think (本文方法)**。

### 4. 资源与算力
*   **文中未明确提及**：
    *   论文没有提供关于训练所使用的GPU型号、数量、训练总时长或能耗等算力相关的具体信息。

### 5. 实验数量与充分性
*   **实验数量**：
    *   **主实验**：在2个数据集上对比了4个模型，围绕7个关键指标展开。
    *   **主观评估**：使用LLM-as-a-Judge对轮次和响应进行了2个维度的打分。
    *   **消融实验**：针对评估设置进行了系统分析，包括：
        1.  三种不同的音频分块策略（动态、固定、按轮次固定）。
        2.  三种文本转录输入条件（真实转录、无转录、语音识别假设）。
        3.  为推理变体提供了真实推理轨迹，探索性能上限。
*   **充分性与公平性**：
    *   **充分**：实验设计覆盖了真实和合成数据集、客观与主观指标、多个维度的消融变量，能够有效支持论文主张。特别是在合成数据上使用未见过的角色和LLM进行零样本测试，增强了结论的可信度。
    *   **公平**：通过设计`MP-Baseline`（使用相同训练数据但无角色条件），做到了控制单一变量，能公平地证明角色条件带来的增益。采用平均多次运行的动态分块结果来减少非确定性，增加了客观指标的可靠性。

### 6. 论文的主要结论与发现
*   **角色条件至关重要**：无角色条件的模型无法在多方对话中实现精确、符合角色的轮次。ModeratorLM系列模型在轮次精确率和召回率上相比非角色条件基线有显著提升（精确率超40%，召回率超70%），并大幅减少了误打断。
*   **思维链推理带来增益**：ModeratorLM-Think通过显式推理，在保持极低误报率的同时，显著提升了召回率和被动响应能力，实现了精确率与召回率之间更好的权衡。
*   **推理提供认知基础**：推理步骤起到了“基础对齐层”的作用，使模型能先分析语境和角色义务再决策，从而产生时机准确且风格一致的回合。
*   **动态分块训练至关重要**：如果没有动态分块，模型可能会过度依赖音频块长度而非对话内容来做决策。

### 7. 优点
*   **问题定义新颖**：首次明确地将**角色扮演**概念引入**实时语音、多方对话系统**的轮次控制中，填补了研究空白。
*   **技术方案创新**：
    *   实现了完全由**语音LLM端到端地负责轮次决策**，替代了传统的外置语音活动检测模块。
    *   引入**思维链推理**来解释和指导轮次决策，增强了模型决策的可解释性和性能。
*   **数据集贡献**：构建并公开了首个用于训练角色条件语音代理的大规模合成多方对话数据集**RolePlayConv**，包含详细的角色描述和推理轨迹。
*   **实验评估扎实**：
    *   设计了公平的**MP-Baseline**，有效控制了变量。
    *   结合了**真实数据、零样本合成数据**以及**客观指标和LLM-as-a-Judge主观评估**，评估体系全面。
    *   消融实验深入分析了分块、转录和推理等因素的影响。

### 8. 不足与局限
*   **算力信息缺失**：未提供训练过程中的资源消耗详情，使得其他人难以复现或评估其环境要求。
*   **合成数据偏置**：核心训练数据`RolePlayConv`是合成的，虽然便捷且规模大，但可能与真实人类互动的复杂性、不可预测性和口语现象（如犹豫、修正、重叠话语）存在分布差异，这可能在真实部署中导致性能下降。
*   **全双工能力未涉及**：当前模型是半双工模式，在“听”和“说”之间切换。论文未涉及现代语音对话中另一重要前沿领域——**同时聆听和说话的全双工交互**能力。
*   **回复仅为文本形式**：助手的回复以文本形式生成，要成为完整的语音代理，需额外集成流式TTS模块，这在实际系统中会引入延迟和集成复杂性，而论文未对此进行探讨。
*   **真实数据集评估受限**：NOTSOFAR-1作为会议数据集，其固定的交互模式可能无法完全代表需要角色扮演的全部多方语音交互场景，且通过混合方法指定“助手”角色也非原生属性。

（完）
