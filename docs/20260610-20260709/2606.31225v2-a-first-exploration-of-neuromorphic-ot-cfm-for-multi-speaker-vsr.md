---
title: A First Exploration of Neuromorphic OT-CFM for Multi-Speaker VSR
title_zh: 多说话人视觉语音识别的神经形态OT-CFM初步探索
authors: "Lin Chen, Jingping Fang, Hairui Liu, Chenyang Xu, Junhao Chen, Xiaorui Li, Weidong Cai, Xiaoming Chen"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31225v2"
tags: ["query:speech-model"]
score: 9.0
evidence: 使用神经形态视觉的视觉语音识别，视觉+语音多模态任务
tldr: "多说话人场景下的视觉语音识别（VSR）常因快速头部运动、遮挡以及传统RGB视频的低帧率与运动模糊而性能不佳。为解决这些难题，LipsFlow提出将RGB视频转换为高时间分辨率的事件流，利用ByteTrack跟踪和TalkNet活跃说话人检测技术，将多说话人场景自动分割为单说话人片段，并借助可学习的事件表征捕获微秒级唇部动态，实现对视觉退化的固有鲁棒性。为了高效建模密集事件特征并适应特定说话人模式，引入最优传输条件流匹配（OT-CFM），在语义潜在空间中强制直线轨迹生成，使推理仅需两个常微分方程（ODE）步骤，极大降低了计算延迟。同时，设计了一种双层语义监督机制，融合token级别的BERT权重绑定与句子级别的语言先验，有效缓解了同音词歧义问题。在标准视觉语音识别基准上，LipsFlow取得了22.3%的词错误率（WER），且延迟仅为240毫秒，确立了事件驱动多说话人VSR的鲁棒高效新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31225v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31225v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1254, \"height\": 716, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31225v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1238, \"height\": 677, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31225v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1255, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31225v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1260, \"height\": 554, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31225v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1276, \"height\": 566, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31225v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1011, \"height\": 389, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31225v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1215, \"height\": 305, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31225v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1023, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31225v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 797, \"height\": 339, \"label\": \"Table\"}]"
motivation: 多说话人VSR面临快速头部运动、遮挡和传统RGB视频低帧率与运动模糊的挑战，亟需高时间分辨率和鲁棒表征。
method: LipsFlow将RGB视频转为事件流，利用ByteTrack和TalkNet分割单说话人片段，通过事件表征捕获微秒唇动，并采用OT-CFM的两步ODE推理和双层语义监督实现高效识别。
result: "在基准数据集上，LipsFlow以22.3%的词错误率和仅240毫秒的推理延迟取得最先进性能。"
conclusion: LipsFlow为事件驱动多说话人视觉语音识别建立了鲁棒且高效的新范式，显著推进了复杂场景下的VSR性能。
---

## 摘要
在复杂的多说话人场景中，视觉语音识别（VSR）任务受到头部快速运动、遮挡和细微唇部发音的严重阻碍。传统的基于RGB的方法由于帧率低和运动模糊而在此处举步维艰。为了克服这些问题，我们提出了LipsFlow，一种受神经形态启发的VSR框架，将RGB视频转换为高时间分辨率的事件流。对于多说话人情况，我们采用ByteTrack跟踪和TalkNet活动说话人检测，将场景在时间上分段为单个说话人片段，从而能够进行针对每个说话人的分析。通过可学习的基于事件的表征明确捕捉微秒级的发音动态，LipsFlow实现了对视觉退化固有的鲁棒性。为了高效建模这些密集的基于事件的特征并适应说话人特定的发音模式，我们引入了最优传输条件流匹配（OT-CFM）。它在语义潜在空间中强制执行确定的直线轨迹生成，将推理延迟降低到仅两个常微分方程（ODE）步骤。此外，我们设计了一种双层语义监督机制，结合了词元级BERT权重绑定和句子级先验，以解决同形异音词歧义。在竞争性基准上验证后，LipsFlow在240毫秒延迟下实现了22.3%的最先进词错误率（WER），为基于事件的VSR建立了一个高度鲁棒且高效的范式。

## Abstract
Visual Speech Recognition (VSR) tasks in complex multi-speaker scenarios are severely hindered by rapid head motions, occlusions, and subtle lip articulations. Traditional RGB-based methods struggle here due to low rates and motion blur of frames. To overcome these, we propose LipsFlow, a neuromorphic-inspired VSR framework that converts RGB videos into high-temporal-resolution event streams. For multi-speaker, we employ ByteTrack tracking and TalkNet active speaker detection to temporally segment scenes into single-speaker clips, enabling focused per-speaker analysis. By explicitly capturing microsecond-level articulatory dynamics via learnable event-based representations, LipsFlow achieves inherent robustness against visual degradation. To efficiently model these dense event-based features and adapt to speaker-specific articulatory patterns, we introduce Optimal Transport Conditional Flow Matching (OT-CFM). It enforces deterministic, straight-line trajectory generation in a semantic latent space, slashing inference latency to just two Ordinary Differential Equation (ODE) steps. Furthermore, we design a Dual-Level Semantic Supervision mechanism combining token-level BERT weight tying and sentence-level priors to resolve homophene ambiguities. Validated on competitive benchmarks, LipsFlow achieves a state-of-the-art WER of 22.3\% at 240 ms latency, establishing a highly robust and efficient paradigm for event-based VSR.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **核心问题**：在复杂的多说话人场景下，传统 RGB 相机受限于低帧率（25-30 FPS）与运动模糊，导致唇部视觉语音识别（VSR）严重丢失毫秒级的细微发音动态（如爆破音 `/p/`, `/b/`），且易受到遮挡、头部晃动等视觉退化干扰。
*   **整体含义**：本文首次将神经形态事件流与最优传输条件流匹配（Optimal Transport Conditional Flow Matching, OT-CFM）相结合，提出 **LipsFlow** 框架，旨在从根本上突破 RGB 传感器的物理瓶颈，并以极低的推理延迟实现多说话人 VSR。

### 2. 论文提出的方法论

LipsFlow 的核心流程与关键技术包括：

*   **多说话人预处理与数据管线**
    *   使用 ByteTrack 进行鲁棒的身份跟踪，结合 Kalman 滤波和 IoU 关联来维持遮挡下的身份一致性。
    *   通过 RetinaFace + Dlib 人脸关键点检测、仿射变换对齐与裁剪，消除头部刚性抖动，提取稳定的唇部 ROI。
    *   利用 TalkNet 主动说话人检测，将视觉身份与音频流绑定，精确分割出有效讲话片段，并建立三层标注协议（静默、仅口腔动作、主动讲话），过滤非语音视觉噪声。

*   **可学习的事件表征（Learnable Event-based Representation）**
    *   **层次帧插值**：使用轻量 RIFE 模型将 RGB 帧从 25 FPS 逐层上采样（2× → 2× → 5×）至 500 FPS，以恢复高频唇动。
    *   **自适应事件流生成**：通过一个可微量化器和轻量 CNN 回归的局部阈值，将上采样后的视频帧转换为事件流，使网络选择性放大发音微运动。
    *   **事件编码与上下文建模**：采用 3D-2D 混合编码器（3D Stem + 2D ResNet）在 125 FPS 提取局部特征，再送入 12 层 Conformer 编码器建模全局时序依赖，得到视觉特征 **V**。

*   **说话人条件 OT-CFM**
    *   **长度预测与单调对齐**：用混合长度预测器估计语义 Token 数量 N，并通过单调对齐模块将 125 Hz 的视觉特征映射到 N 个 Token 槽，获得条件特征 **C**。
    *   **说话人身份编码**：使用 TDNN + 时间统计池化从事件特征提取全局说话人嵌入 **s**，并施加 InfoNCE 对比损失以强制解耦身份无关的视觉噪声。
    *   **最优传输流匹配**：在预训练的 BERT 语义隐空间内，学习一条从高斯先验 **x₀** 到目标语义 **x₁** 的**确定性直线概率流**。通过回归向量场匹配位移，训练损失简化为 `L_FM`。
    *   **双条件 CFM-Former**：16 层模型通过**交叉注意力**注入视觉条件 **C**，并通过**自适应层归一化 (AdaLN)** 由时间嵌入和说话人嵌入 **s** 调制，实现内容生成与身份特征的解耦。
    *   **高效推理**：利用 OT 路径的直线特性，仅使用**两步欧拉求解器 (NFE=2)** 即可从噪声恢复目标语义 `x₁`，极大降低延迟。

*   **先验引导的语义解码器**
    *   **BERT 权重绑定**：将恢复的语义特征投影到预训练 BERT 的嵌入矩阵上，通过带温度系数的余弦相似度直接生成词汇概率分布。
    *   **双层语义监督**：
        *   Token 级：交叉熵损失 `L_CE` 保证局部精度。
        *   句子级：绕过不可微的离散解码，通过**软加权 Token 嵌入求和**得到连续表示，送入冻结的 Sentence-BERT 计算语义一致性损失 `L_sem`，以端到端的方式解决同形异音词歧义。
    *   **自适应终止**：训练时在目标序列后随机追加 `[PAD]` Token，使模型学会在语义流形内部识别终止信号，灵活控制生成长度。

### 3. 实验设计

*   **数据集与评估场景**
    *   **DVS-Lip**：包含真实事件流的大规模唇读数据集，作为主基准。
    *   **AVA**：自然多说话人场景，使用本文的数据管线将 RGB 帧转为事件流，并细分为三个退化子集：**快速运动 (Rapid Motion)**、**低光照 (Low Light)** 和**严重遮挡 (Severe Occlusion)**，测试环境鲁棒性。
*   **对比方法**
    *   **视频类 (RGB)**：Auto-AVSR, VATLM, RAVEn, SynthVSR, CMAVSR, LipGen, Llama-AVSR。
    *   **事件类**：MSTP, SNN-Lip。
*   **评估指标**
    *   词错误率 (WER)、视位错误率 (VER)、实时因子 (RTF)、推理延迟 (ms)、参数量 (M)。环境鲁棒性实验还计算相对退化值。

### 4. 资源与算力

*   **硬件**：4 块 NVIDIA A100 80GB GPU。
*   **软件加速**：使用 PyTorch 分布式数据并行，BF16 混合精度训练，TF32 加速。
*   **训练时长**：
    *   第一阶段（冻结视觉前端）：10 epochs，约 15k 迭代。
    *   第二阶段（全模型微调）：20 epochs，约 30k 迭代。
    *   总训练约 45k 迭代，耗时约 30 小时。
*   **优化细节**：全局 Batch Size 64，AdamW 优化器，余弦退火学习率调度，梯度裁剪。

### 5. 实验数量与充分性

*   **实验组数**：共进行了 5 张表格的定量实验，涵盖主对比、鲁棒性、多维度消融。
    *   **主对比 (Table 1)**：在 DVS-Lip 上与 9 种基线方法全面比较。
    *   **鲁棒性对比 (Table 2)**：在 AVA 的 3 种退化场景下与 6 种方法对比。
    *   **消融实验**：
        *   **架构消融 (Table 3)**：验证多模态融合策略与生成范式（AR vs. Diffusion vs. OT-CFM）。
        *   **效率消融 (Table 4)**：分析 OT-CFM 采样步数 NFE 对速度-精度的权衡。
        *   **训练策略消融 (Table 5)**：逐步验证损失函数、课程学习和数据增强的贡献。
*   **充分性与公平性**：实验设计较为全面，覆盖了精度、效率、鲁棒性和各组件贡献。严格遵循数据集官方训练/测试协议，基线方法采用官方复现原则，具备客观性和公平性。

### 6. 论文的主要结论与发现

1.  LipsFlow 在 DVS-Lip 上取得 **22.3% WER，19.8% VER** 的最先进性能，显著超越此前最优的事件方法 MSTP (8.2%)，甚至优于基于大规模语言模型的方法 Llama-AVSR (1.6%)。
2.  在效率上，通过 OT-CFM 的直线概率流，仅需 **2 步 ODE 求解器**即可实现高质量生成，推理 RTF 低至 0.18，比扩散模型 (SynthVSR) **快 26 倍**，同时保持高精度。
3.  对环境退化具有卓越鲁棒性：在低光照和快速运动场景下 WER 退化极小 (<2.0%)，远远优于所有 RGB 方法。
4.  神经形态事件流有效弥补了 RGB 传感器的物理缺陷，结合 OT-CFM 强大的生成先验与双层语义监督，成功解决了视觉语音中的同形异音词歧义问题。

### 7. 优点

*   **范式新颖性**：首次将神经形态事件视觉与 OT-CFM 生成式建模结合，为 VSR 提供了同时解决物理感知瓶颈和推理效率瓶颈的统一框架。
*   **系统完整性**：设计了从多说话人数据预处理、可学习事件表征到高效生成解码的端到端流水线，技术细节扎实。
*   **效率与精度的平衡**：OT-CFM 的直线轨迹设计使其能以极低的计算成本达到超越复杂扩散模型甚至大语言模型的精度，实时性极强。
*   **鲁棒性验证**：通过细分退化场景的实验，有力地证明了事件流对运动模糊和光子匮乏的内在免疫力，比仅比较清洁数据更有说服力。
*   **消融实验细致**：对架构组件、生成范式、训练损失和推理步数都进行了详尽的消融分析，增强了结果的可信度。

### 8. 不足与局限

*   **数据依赖性**：最高性能依赖于从高清 RGB 视频仿真生成的事件流（AVA 实验），虽然 DVS-Lip 使用了真实事件流，但仿真到真实域之间的差距未被充分探讨。
*   **实验覆盖范围有限**：仅在两个数据集上测试，缺少更多大型多说话人基准（如 LRS3 的多说话人改编版本）的验证。对极端口语速度、方言或非正面姿势的未见情况的鲁棒性不明确。
*   **预处理管线复杂度**：多说话人分割、人脸对齐、ROI 稳定化等步骤构成了较重的预处理依赖，可能成为实际部署的瓶颈，且其误差对最终性能的影响未做敏感性分析。
*   **算力需求较高**：虽然推理效率极高，但训练使用了 4 块 A100 GPU 和 30 小时，对资源有限的场景可能存在门槛。
*   **未深入探讨失败案例**：论文主要集中在平均性能的对比与提升，缺少对模型在特定困难音素或复杂交互中识别错误的定性分析，难以理解其残差极限。

（完）
