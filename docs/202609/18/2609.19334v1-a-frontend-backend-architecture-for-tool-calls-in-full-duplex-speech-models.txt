Title: A frontend-backend architecture for tool calls in full-duplex speech models

URL Source: https://arxiv.org/pdf/2609.19334v1

Published Time: Fri, 18 Sep 2026 00:10:55 GMT

Number of Pages: 5

Markdown Content:
# A FRONTEND-BACKEND ARCHITECTURE FOR TOOL CALLS IN FULL-DUPLEX SPEECH MODELS 

# Ke Hu, Slyne Deng, Chen Chen, Elena Rastorgueva, Edresson Casanova, Punit Kumar, Dharmendra Choudhary, Nikhil Srihari, Ameya Sunil Mahabaleshwarkar, Viet Anh Trinh, Slim Essid, Oluwatobi Olabiyi, Zhehuai Chen 

# NVIDIA, USA 

ABSTRACT 

Full-duplex speech-to-speech (S2S) models provide natural, low-latency conversational interaction and would benefit from the ability to use external tools and complete voice-agent tasks. We propose a frontend-backend architecture where a duplex speech-to-text fron-tend learns to emit a delegation token and forwards streaming ASR transcripts to a text-based backend LLM for tool calls. Tool-call re-sults from the backend are injected back into the frontend through a lightweight prefill-and-repeat mechanism and then synthesized us-ing streaming TTS to the user. Our approach largely preserves reg-ular duplex turn-taking, interruption handling, and low-latency in-teraction as it requires minimal modifications to the frontend model. In a single-turn tool-call evaluation, our system achieves 92-97% tool-call recall, competitive tool-call prediction performance, and 81.2% accuracy in rejecting irrelevant calls. When equipped with a larger backend (e.g., Qwen3-235B-A22B), our system achieves competitive results on Full-Duplex-Bench-V3 compared to open and closed source models, and significantly outperforms GPT-realtime-mini and Qwen3-Omni-30B-A3B-Instruct on EVA-Bench. These results demonstrate that backend delegation is an effective and mod-ular approach for combining natural duplex speech interaction with strong agentic tool-call capabilities. 

1. INTRODUCTION 

Full duplex speech-to-speech (S2S) models are natural and desirable interfaces for conversational AI [1, 2, 3, 4, 5, 6, 7, 8, 9]. By operating directly in the speech modality, these models eliminate cascade la-tency, preserve paralinguistic cues, and produce natural turn-taking and barge-in behavior [10, 11, 12, 13]. While duplex speech mod-els excel in natural conversation based on internal intelligence, text-based LLMs now serve as reliable tool-using agents: Instruction-tuned models decide when and how to invoke external tools, query knowledge bases, and act on the world [14, 15, 16, 17, 18, 19]. Although there is increasing effort enabling spoken tool calls in speech LLMs [20, 21, 22, 1, 23], tool-call capabilities in voice agents remain substantially behind those of text-based agents. As shown in τ -Voice [24], leading commercial duplex voice models complete only 31 −51% of grounded customer-service tasks under clean con-ditions, whereas text agents such as GPT-5 achieve 85% on the cor-responding text-mode tasks; the gap widens further under realistic noise and accented speech. This disparity suggests that natural spo-ken interaction and strong tool-call intelligence remain largely sep-arate strengths of current systems. A natural question is therefore whether duplex speech models should directly internalize tool-call capabilities or instead delegate such capabilities to a backend text agent that already benefits from mature instruction following, tool calls, and long-horizon reasoning abilities. Recent work investigates tool-call capabilities directly inside Moshi-style [4] duplex speech models by placing tool calls in a separate channel [25]. However, audio-native modeling imposes a fundamental capacity tradeoff: audio tokens consume parame-ters and context budget that text-only LLMs can devote to factual knowledge, instruction following, and tool-call capabilities [26]. In contrast, a delegation-style backend agent is more modular and consumes little modeling capacity from a frontend speech model. In this direction, hybrid approaches that keep an S2S frontend for interaction and delegating tool calling and reasoning to a text backend appear in several concurrent designs: KAME [26] injects backend “oracle” tokens into a Moshi-style [4] S2S frontend for knowledge integration; MoshiRAG [27] augments Moshi with re-trieval; Thinking Machines [28] proposes an interaction-background framework for user queries requiring deeper reasoning, tool calls, or long-horizon work. More recently, Qwen-audio-agent [29] and GPT-Live [30] also adopt similar frameworks. However, it remains unclear how the delegation signal in [28, 29, 30] is designed and background model interacts with the duplex frontend. In this work, we propose a frontend-backend architecture for ex-ecuting tool calls and agentic tasks for a full-duplex speech model. Our S2S model consists of a duplex speech-to-text (STT) component (based on [6, 31]) and a streaming TTS [32]. The duplex STT model takes user speech encoding, agent text and streaming user ASR tran-script as inputs [31]. We use the duplex STT model as the frontend: it predicts a delegation token for voice queries involving tool calls, and then sends the streaming ASR transcripts to the backend LLM, which handles the tool call in a LangGraph framework [33]. The backend’s natural-language result is then sent back to the frontend agent text channel via a prefill mechanism and the frontend is then trained to repeat the result. The frontend remains silent during the tool calls and resumes its normal duplex behavior after the tool call is completed. Our design imposes minimal changes on the frontend du-plex STT model by adding a tool-call control token to its prediction targets. In a single-turn tool-call evaluation based on a speech ver-sion of BFCL [34], our experiments show that the frontend-backend architecture reliably predicts the tool-call token with 92% to 97% recall, and rejects irrelevant calls up to 81.2%. On more natural-istic user queries with pauses, hesitations, and self-corrections in Full-Duplex-Bench-V3 [35], our model based on the Qwen3-235B-A22B backend achieves response quality similar to GPT-realtime-mini. On the more challenging EVA-Bench voice-agent task eval-uation [36], which involves multi-turn conversations and tool calls, our model significantly outperforms GPT-realtime-mini and Qwen3-Omni-30B-A3B-Instruct [37], and performs similarly to Gemini 3.1 Flash Lite [38]. Demos of our frontend-backend system can be found online. 1

2. RELATED WORK 

Cascaded frontend–backend systems, such as NVIDIA’s Nemotron Voice Agent [39] and LiveKit’s EXA Deep Researcher [40], com-bine ASR, LLM, and TTS into a frontend and use a separate back-end for planning and tool execution. Recent systems increasingly explore tool use and backend agents into real-time voice interaction.     

> 1https://huggingface.co/spaces/frontend-backend-duplex/demo arXiv:2609.19334v1 [cs.CL] 16 Sep 2026 Streaming Speech Encoder
> Agent text
> User speech
> Decoder-only LLM
> <silence>
> <silence>
> <silence> <turn-1>
> <turn-1> <turn-2>
> Pooling
> Agent Speech
> Agent Text
> Text head
> Streaming TTS
> <silence> <turn-1> <turn-2>
> ASR head
> User text
> <silence> <silence>
> User text <silence> <silence> <turn-1>
> Tool Call?
> No (<agent_bos>)
> Backend LLM
> Yes (<tc_bos>)
> Prefill
> <turn_1>

Fig. 1 . The proposed frontend-backend system for a duplex speech-to-speech model with tool-call capability. Commercial systems [30, 1, 41] expose tool calls inside an end-to-end voice API, but their architectures are not disclosed. Recent work focuses on pairing a low-latency full-duplex speech frontend with a more capable text backend. KAME [26] runs a Moshi-style fron-tend and conditions its output on “oracle” tokens streamed from a backend LLM updated every 100–500 ms; the frontend is explicitly trained to anchor its speech on these oracle tokens. MoshiRAG [27] augments Moshi with retrieval, injecting retrieved context into the inner-monologue stream. Thinking Machines adopts an interaction-background style system to delegate deep reasoning, tool calls, or longer-horizon jobs to the background model [28]. Recently, Qwen-audio-agent [29] is proposed as a realtime voice model with backend agents which can execute various tasks while the frontend is in a con-versation with the user. However, in [28, 29], it is unclear how the delegation signal is designed or backend results are integrated into the frontend duplex model. Compared to the aforementioned approaches, we propose a straightforward approach to predict a delegation token in the agent text channel to assign tool calls to a backend. The backend exe-cutes multiple rounds of tool calls, and then prefills the response text to the frontend as context for further generation. The pro-posed frontend-backend framework is modular and does not need significant architecture changes for the frontend. 

3. ARCHITECTURE 3.1. Speech-to-text frontend 

As shown in Fig. 1, our frontend model is a duplex speech-to-text (STT) model which consists of a streaming speech encoder and the LLM backbone (similar to [42]). The duplex STT model takes three input streams: user speech, user transcript, and agent text. User speech is encoded by a 600M-parameter Parakeet streaming speech encoder [43] and fed to a backbone LLM (NVIDIA Nemotron-Nano-9B-v2-Base [44]). The streaming ASR head consists of a separate embedding layer and prediction head, and share the same LLM backbone as the agent text head. A single decoding pass jointly produces both user and agent text. To delegate tool calls to the backend, we train the frontend model to fire a tool-call token <tc bos> in the agent text channel if the user speech naturally requires a tool call (e.g., “what is the weather in New York?”). We then train the model to first generate a short filler followed by the special token <tc eos> . Actual tool calls will be performed by the backend, and we just prefill the tool call response in the training data into the agent text channel and train the frontend to reproduce it exactly. The tool-call tokens, filler, and reproduced regions are used for loss computation in the same way as for regular agent text, while prefill regions are masked and excluded from loss computation. During inference, we send the streaming ASR tran-script to the backend for tool call execution when <tc eos> fires. The backend returns the acutual natural-language text to the fron-tend to prefill (see Sect. 3.2 for details). Our frontend is connected to VoiceChat-TTS [32] to generate agent speech, which accepts explicit turn start and interruption control tokens and incrementally generates codec-based speech tokens. 

3.2. Backend agent 

We use LangGraph [33], a tool-augmented ReAct agent [16], as our backend. Our graph is a state machine over a running message list, comprising an agent node that employs an instruction-following LLM and a tools node that executes the model’s tool calls. The two nodes are linked by a conditional edge that routes to tool execution whenever the model emits calls and terminates the turn once the turn contains no tool calls. When the user query requires a tool call, e.g., “what is the weather in New York?”, the user ASR transcript is sent to the graph as a message, and the agent is invoked to call tools to respond. Multi-turn context is maintained automatically by a thread-keyed checkpointer that persists and restores the accumulating con-versation state across turns, so each turn only takes the current user ASR transcript as input. If the backend generates a syntactically invalid tool-call request or a regular text response, we return the text directly to the fron-tend as prefill. This means the frontend may have misfired a tool-call request, while the larger backend reliably falls back to natural-language explanations for irrelevant queries. 

4. TRAINING AND INFERENCE 

For the frontend duplex STT model, our training includes a pretrain-ing stage followed by supervised fine-tuning (SFT) [42]. To enable tool-call token prediction in the frontend, we generate multi-turn conversation data with tool calls for SFT. First, we use various LLMs (e.g., Nemotron 3 Nano [45], Gemma-4-31B-IT [46], and Qwen3.5-397B-A17B [47]) to generate user, agent, tool call and tool response turns in text, and use LLM judges to filter conversations with in-consistent turns or incorrect tool invocations. These textual conver-sations are then synthesized into audio conversations using a data pipeline involving filtering (e.g., removing math-heavy and code-heavy conversations not suitable for audio), TTS (VoiceChat-TTS [32] with diverse voices), ASR (Parakeet-tdt-0.6b-v2 [48]) based quality filtering using WER/CER. The multi-turn tool call data has variants targeting voice-agent decisions about when to call a tool, when to ask follow-up questions, and when not to call an unavailable or inappropriate tool, similar to When2Call [49] but with multi-turn speech user input. Our pipeline also simulates realistic multi-turn interactions with backchannels, pauses, and interruptions. We have also constructed domain-based multi-turn tool call data (e.g. simu-lated airline and retail databases) with entities distinct from evalua-tion. We generate conversation and tool-calling trajectories by hav-ing two text-only LLMs interact with each other (both are Qwen3.5-235B-A22B [50]) to achieve diverse user goals. Samples that failed to complete the task are discarded, and we synthesize the user turns into speech using Chatterbox [51]. In total, our training data con-sists of around 530k hours of pretraining data, 111k hours of SFT data, around 16k hours of ASR transcription data, and 8.5k hours of multi-turn conversation data with tool calls. When training the fron-tend, we randomly choose either the full tool definition as the system prompt or a partial prompt with only function name and description for generalization. Table 1 . BFCL AST accuracy (%) and irrelevance score. UV0.6-8B and UV0.6-32B denote Ultravox-v0.6 with Llama-3.1-8B and Qwen-3-32B backbones, respectively. Ours-7B uses Qwen2.5-7B, Ours-30B uses Qwen3-30B-A3B.                                 

> Model Simple Multiple Parallel Para. Multi. Irrelev. Avg GPT-realtime 82.9 83.6 72.5 74.0 90.8 80.8
> UV0.6-8B 61.4 63.5 48.5 43.0 0.0 43.3 UV0.6-32B 78.9 81.1 75.2 65.4 82.5 76.6
> Ours-7B-intASR 80.2 79.6 69.5 52.7 76.7 71.7 Ours-30B-intASR 79.4 78.3 71.1 55.1 81.2 73.0 Ours-30B-extASR 80.1 78.7 72.0 61.1 81.2 74.6

In training, if a user query leads to a tool call, we label the fol-lowing agent turn as a tool-call turn, along with the final natural-language tool-call response. We first place a <tc bos> token to re-place a regular <agent bos> token, which is usually 320 ms after the end of the user turn (as in [42, 6, 31]), then followed by the filler (∼1s duration) and then <tc eos> . The natural-language tool re-sponse is then enclosed by <pf bos> and <pf eos> and prefilled into the agent text channel around 1s after the <tc eos> to simulate the tool call executation time, and the frontend model is trained to generate the exact prefilled text starting with <agent bos> . Dur-ing training, we insert pad tokens to the ASR channel and silence to speech regions as inputs with prefilled agent text, and these regions are not used for loss computation. An example training sequence looks like:              

> User: What is the weather in New York?
> Agent: <pad >320 ms [<tc bos >Give me a moment. <pad >... <tc eos >]1 s
> <pad >1 s <pf bos >The weather in New York is 72 degrees <pf eos ><agent bos >The weather in New York is 72 degrees <pad >...

At inference, once the frontend fires the tool-call detection to-ken, the user transcript, endpointed by <user eos> , is sent to the backend to complete the tool-call request. The resulting natural-language response is then injected into the agent-text channel before further generation. During the function call, we suppress the agent output by inserting pad tokens in the agent text channel. 

5. RESULTS 5.1. Tool Calls 

5.1.1. Single-Turn Tool Calls 

We use the audio version of the Berkeley Function-Calling Leader-board (BFCL) [52] single-turn datasets from ServiceNow-AI [34] for evaluation. The single-turn datasets include Simple, Multiple, Parallel, Parallel-multiple, and Irrelevance subsets. We use the Ab-stract Syntax Tree (AST) score to evaluate the structural correctness of the generated tool-call request. We follow the original AST im-plementation [53], which tolerates argument order, spacing and for-matting differences, and default parameters in AST scoring. As shown in Table 1, we evaluate our system with backend LLMs of two different sizes: Qwen2.5-7B [54] and Qwen3-30B-A3B [37]. We also compare two methods for transcribing user speech: our internal ASR (intASR) transcripts and an external ASR (extASR) model [43]. For external ASR, we transcribe the user speech once the streaming ASR detects the user end. As shown in Table 1, we first find that the two Qwen3 backends with intASR perform similarly on Simple and Multiple, and the larger backend performs better on Parallel and Parallel Multiple subsets, and rejects irrelevant speech more reliably. When we switch to external ASR transcripts for the Qwen3-30B-A3B, the average score improves from 73 .0% to 74 .6% , with the biggest gain on Parallel-Multiple (55 .1% → 61 .1% ) due to more accurate ASR transcripts. We will use the external ASR setup in later experiments. We also compre to GPT-realtime [1], Ultravox-v0.6 with Llama-3.1-8B [55] and Qwen-3-32B [56] backbones in Table 1. We orga-nize Ultravox models in a different section since they are turn-based 

Table 2 . FDB3 results; “–” is N/A. Boldface marks group bests. Ours-235B-extASR denotes our system with the Qwen3-235B-A22B backend and external ASR. ∗Judged with no timing.                                                      

> Model Tool-acc ↑Arg-acc ↑Pass@1 ↑Res-Q ↑TT ↑Inter ↓Filler ↓
> GPT-realtime-mini [57] 77.4 58.2 51.0 62.0 93.0 14.2 17.2 GPT-realtime 88.7 71.6 61.0 74.7 95.0 13.5 12.1
> Gemini-3.5 Flash [19] 97.0 72.5 65.0 88.0 ––1.0 ∗
> UV0.6-8B 59.1 39.0 11.0 19.0 ––2.0 ∗
> UV0.6-32B 84.3 52.2 45.0 71.0 –– 12.0 ∗
> Ours-30B-extASR 74.6 52.8 44.0 54.0 100.0 54.0 84.8 Ours-235B-extASR 71.7 55.2 48.0 67.0 100.0 51.0 83.3

models, and we label the best score in bold for each group. Our sys-tem with the 7B backend substantially outperforms Ultravox-v0.6 Llama-3.1-8B in average score (71.7 vs. 43.3). For Ultravox-v0.6-Llama-3.1-8B, we use post-hoc type normalization to coerce string-typed JSON arguments to their schema types. The score of 0.0 on Irrelevance is because it invokes the single offered function on all 240 prompts. For GPT-realtime, we use semantic VAD and feed the user speech turn to generate the tool-call request. As shown in Table 1, our 30B backend setup performs close to GPT-realtime on some subsets, however the largest gap is on Parallel-Multiple, followed by Irrelevance. We also compute tool-call token recall as the fraction of positive tool-call utterances for which the frontend correctly fires the delegation token. Our frontend achieves recall of 97.2%, 92.0%, 95.0%, and 93.5% on Simple, Multiple, Parallel, and Parallel Multi-ple, respectively. 

5.1.2. Full Duplex Bench v3 

Whereas BFCL contains continuous and fluent TTS-generated user speech, Full-Duplex-Bench v3 (FDB3) [58] consists entirely of nat-uralistic real human recordings. The corpus contains 100 scenarios from 12 speakers, recorded with everyday built-in microphones in environments ranging from quiet rooms to mild background noise. Mock APIs are used to generate tool-call results. In evaluation, we follow the official FDB3 setup to use GPT-4o as the LLM judge [35], along with the original system prompts and tool descriptions. In this evaluation, we stream our user speech to the frontend chunk by chunk as in live conversation. Since our frontend is an STT model, we use agent text for computing Res-Q, TT and Inter. We also re-produced other model’s results using their text outputs in Table 2. Table 2 reports tool selection accuracy (Tool-acc), argument accuracy (Arg-acc), response quality (Res-Q), end-to-end task-completion (Pass@1), turn-taking rate (TT), interruption rate (Inter), and filler rate (Filler). We remove end-to-end latency because our backends may run either as local vLLM instances or from cloud APIs, and latencies are not comparable. The turn-taking latency of our frontend model is separately evaluated in Sect. 5.3. As shown in Table 2, our systems fulfill the user’s request (Res-Q) at around 54%-67%. A larger backend (Qwen3-235B-A22B) performs better as expected. Our model achieves 100% turn-taking. Our high Filler rate is by design: the frontend emits a short hold phrase (“Let me pull that up”) before the backend executes the tool call. Our high Inter rate has a different cause: the frontend typically responds at pauses during the user’s disfluency with a backchannel (e.g., “Okay, I am here.”) before any tool call. Because it keeps listening while speaking, the eventual tool call usually still sees the complete user request ( Res-Q up to 67.0). We compare our systems to open-source and closed-source mod-els in Table 2, where the Ultravox and Gemini models are grouped into a section representing turn-based speech models, and therefore the TT and Inter metrics are not applicable. Our models perform signficantly better than Ultravox-v0.6 Llama-3.1-8B since the model produced no response in many scenarios. Ultravox-v0.6 Qwen-3-32B performs better than our systems since it is a turned based model and will not emit premature tool calls. For GPT-realtime and mini we use the semantic VAD and take their text output for evaluation. Table 3 . EVA-Bench [36] results. Values are percentages except TT (ms). Q3O-30B and G3.1-FL denote Qwen3-Omni-30B-A3B-Instruct and Gemini 3.1 Flash Lite, respectively.                                                                                

> Model EVA-A ↑EVA-X ↑Task ↑Faith ↑Prog. ↑Concise ↑Speak ↑TT ↓
> GPT-5.2 (text) 63.3 80.8 78.4 48.1 73.2 77.6 91.6 –GPT-RT-mini 33.1 71.2 37.1 29.1 46.5 77.5 89.7 535 GPT-RT2 59.4 75.7 68.1 50.7 63.9 72.7 90.5 435
> Q3O-30B 29.1 72.7 32.5 25.7 46.9 77.5 93.7 –G3.1-FL 45.6 75.3 57.1 34.2 53.1 78.9 93.8 –UV0.6-8B [55] 8.3 58.8 11.8 4.7 16.5 68.5 91.5 –UV0.6-32B [56] 31.2 54.5 36.2 26.3 55.2 68.2 40.1 –Ours-30B-extASR 32.4 63.5 40.4 24.4 39.2 65.7 85.6 151
> Ours-235B-extASR 46.6 74.7 57.3 35.9 53.5 77.6 93.0 352

Compared to them, our results with Qwen3-235B-A22B backend are similar to GPT-realtime-mini based on Pass@1 and Res-Q metrics, while GPT-realtime performs best on almost all metrics. 

5.2. Voice Agent Tasks 

Finally, we evaluate on EVA-Bench [36], an end-to-end frame-work for evaluating voice agent tasks on grounded, multi-domain customer-service tasks. Our model’s EVA results are generated using the following setup. We simulate the caller using EVA’s 

LiteLLMClient and use GPT-5.2 to role-play the caller. To adapt to our infrastructure and improve inference availability, we synthesize user text turns with Chatterbox-TTS [59], and the syn-thesized user speech is streamed chunk-by-chunk to the frontend implemented using Triton Inference Server [60]. We use GPT-5.2 as the LLM judge for all metrics. To focus on evaluating our frontend STT text output quality and also compare to other text and STT models, we directly return agent text to the simulated user instead of transcribed synthesized speech for our models and compared models in Table 3. For metrics, we report all 213 EVA-Bench scenarios across the airline, ITSM, and medical HR domains. Since we use text outputs from the agent, we adapt EVA-A and EVA-X metrics for evaluation. EVA-A is the average of Task and Faith (fulness), and we remove 

Speech Fidelity , which measures TTS quality, since our frontend out-puts agent text. EVA-X is the average of Prog (ress), Concise (ness), and Speak (ability). We add Speakability to measure how friendly the agent text is as input to the TTS model. We also report Turn-Taking 

separately as the last column in Table 3, but it is not used to compute EVA-X because different backends and models use different imple-mentations and their latencies are not strictly comparable. In Table 3, we compare EVA performance across the text-only model (GPT 5.2), GPT-realtime-mini [57], GPT realtime2 [61], turn-based speech models, and our frontend–backend systems. GPT-5.2 text-only is presented as a topline model, where we directly pass user text to the model to obtain the agent text response. This ideal set-ting performs best, with the highest EVA-A and EVA-X scores and a task-completion ratio of 78.4%. We also run GPT-realtime-mini and GPT-realtime2 in streaming mode using the semantic VAD setup and return text outputs directly to the simulated user. GPT realtime2 per-forms significantly better than the mini on EVA-A. Our model with Qwen3-30B-A3B backend performs similarly to GPT-realtime-mini on EVA-A, and the Qwen3-235B-A22B backend achieves signifi-cantly better EVA-A and EVA-X scores than GPT-realtime-mini but still lags behind GPT realtime2 on EVA-A. For turn-based speech models in Table 3, we directly use the full user speech as input. Our system with the Qwen3-235B-A22B backend performs similarly to Gemini 3.1 Flash Lite and signif-icantly outperforms Ultravox-v0.6 Qwen-3-32B [56]. Compared with Qwen3-Omni-30B-A3B-Instruct, our system with the Qwen3-30B-A3B backend achieves higher EVA-A and task completion. For Qwen3-235B-A22B, the conciseness and speakability scores are better than GPT realtime2, but task completion and faithfulness lag behind. Actually in a more detailed analysis, we find that the Qwen3-235B-A22B backend achieves higher task completion in the airline domain than GPT realtime2 (72% vs. 54%) but under-

Table 4 . Turn-taking and barge-in (BI) on our internal benchmark.             

> Model Pr ↑/Rec ↑(%) Lat. ↓(ms) BI Acc. ↑/Lat. ↓(%/ms) Baseline (no TC) 85 /94 423 100 /403 Ours (frontend) 82/91 438 99/ 395

Table 5 . Spoken-language intelligence. Scores are in a 5-point scale.          

> Model OpenbookQA (%) ↑AE (/5) ↑CE (/5) ↑
> Baseline (no TC) 65.0 3.54 2.87
> Ours (frontend) 64.7 3.61 2.36

Table 6 . ASR word error rate for the Open ASR Leaderboard.                            

> Model LS-C LS-O TED Vox Earn AMI Giga SPGI Avg Baseline (no TC) 3.93 8.59 5.89 8.90 20.00 20.45 13.45 5.15 10.80
> Ours (frontend) 4.16 8.18 6.71 9.90 21.52 21.66 14.64 4.98 11.47

Table 7 . Full-Duplex-Bench v1 [10] results. Pause reports Candor TOR. GPT score is out of 5.                     

> Smooth TT (Candor) Pause User Interruption Model TOR (%) ↑Lat. (ms) ↓TOR (%) ↓TOR (%) ↑GPT score ↑Lat. (ms) ↓
> Baseline (no TC) 93 221 53.2 95.5 3.94 590 Ours (frontend) 100 92 68.2 89.5 4.04 349

performs in the ITSM (56.3% vs. 75%) and Medical HR (49.4% vs. 69.9%) domains. In the latter two scenarios, the model needs a chain of up to 6–8 successful tool calls to complete a task. This requires reliable tool-call detection from the frontend over a long conversation. 

5.3. Turn-Taking and Intelligence 

Lastly, we measure whether adding tool-call prediction abilities to the frontend model impacts regular duplex conversation quality. We evaluate the frontend on an internal multi-turn conversation set [42], VoiceBench tasks [62], the Open ASR Leaderboard [63], and FDB-v1 [10]. In Tables 4, 5, 6, and 7, we compare the proposed model’s performance on turn-taking, intelligence, streaming ASR, and FDB-v1 against baseline models without tool-call (TC) training. We fol-low [42] for computing the metrics for the internal set. Overall, our frontend model performs slightly worse in turn-taking precision and recall while performing similarly on the other metrics in Table 4. It performs similarly on OpenbookQA and AlpacaEval (AE) while degrades a little on CommonEval (CE) as shown in Table 5. The streaming ASR WER degrades from 10.80% to 11.47% (Table 6), presumably because the added tool-call training data are mostly syn-thetic. On FDB-v1 in Table 7, the model generally becomes more responsive, with a higher TOR rate for smooth TT and lower la-tency, but it also performs worse on pause handling. This can be addressed by adding more training data with natural pauses, which are not present in the current SFT training. 

6. CONCLUSION 

We propose a frontend-backend architecture that enables a full-duplex model to perform tool calls and agentic tasks. Our frontend model is trained to emit a tool-call token that signals the need for backend delegation, and the tool-response text from the backend is prefilled to the frontend as context for the further conversation. Our results show that this architecture works with different back-end LLMs and achieves competitive results on tool-calling and voice-agent benchmarks. 

# Acknowledgment 

We thank Lily Lee, Nourchene Ferchichi, Harishchandra Dubey, Yuanhang Su, Aditya Malte, Zijia Chen, Travis Bartley, Praise Manzi, Hayley Ross, and Yoshi Suhara for their efforts, contribu-tions, and support throughout this project. Claude Opus 4.8 and Codex with GPT-5.5 are only used to for-mat tables and references and to fix grammatical errors throughout the paper. 7. REFERENCES 

[1] OpenAI, “Introducing gpt-realtime and realtime api updates for production voice agents,” OpenAI, 2025, Accessed: 2026-06-05. [2] Google, “Gemini Live API overview,” Google AI documenta-tion, 2026, Accessed: 2026-06-09. [3] xAI, “Grok: Truth-seeking ai chatbot with voice and image generation,” xAI, 2026, Accessed: 2026-06-09. [4] A. D´ efossez et al., “Moshi: a speech-text foundation model for real-time dialogue,” 2024, arXiv:2410.00037. [5] NVIDIA et al., “PersonaPlex: Voice and role control for full duplex conversational speech models,” in Proc. ICASSP , 2026. [6] Ke Hu et al., “SALM-duplex: Efficient and direct duplex mod-eling for speech-to-speech language model,” in Proc. Inter-speech , 2025. [7] Wenfu Wang et al., “Covo-Audio technical report,” 2026, arXiv:2602.09823. [8] NVIDIA, “Nemotron 3 VoiceChat,” NVIDIA model card, Mar. 2026, Accessed: 2026-09-11. [9] NVIDIA, “NVIDIA NemotronLabs VoiceChat 11B,” Hugging Face, Aug. 2026, Accessed: 2026-09-11. [10] G.-T. Lin et al., “Full-Duplex-Bench: A benchmark to evalu-ate full-duplex spoken dialogue models on turn-taking capabil-ities,” in Proc. IEEE ASRU , 2025. [11] G.-T. Lin et al., “Full-Duplex-Bench v2: A multi-turn evalua-tion framework for duplex dialogue systems with an automated examiner,” in Proc. ACL , 2026. [12] Guojian Li et al., “Easy Turn: Integrating acoustic and lin-guistic modalities for robust turn-taking in full-duplex spoken dialogue systems,” 2025, arXiv:2509.23938. [13] HumDial Organizers, “The ICASSP 2026 HumDial chal-lenge: Benchmarking human-like spoken dialogue systems in the LLM era,” in Proc. ICASSP , 2026. [14] T. Schick et al., “Toolformer: Language models can teach themselves to use tools,” in Proc. NeurIPS , 2023. [15] S. G. Patil et al., “Gorilla: Large language model connected with massive APIs,” in Proc. NeurIPS , 2024. [16] S. Yao et al., “ReAct: Synergizing reasoning and acting in language models,” in Proc. ICLR , 2023. [17] S. Yao et al., “ τ -bench: A benchmark for tool-agent-user in-teraction in real-world domains,” 2024, arXiv:2406.12045. [18] OpenAI, “GPT-5.5 model,” OpenAI documentation, 2026, Ac-cessed: 2026-06-09. [19] Google, “Gemini 3.5 Flash model,” Google AI documentation, 2026, Accessed: 2026-06-09. [20] Ramit Pahwa et al., “Audio2Tool: Speak, call, act – a dataset for benchmarking speech tool use,” 2026, arXiv:2604.22821. [21] Kimi Team et al., “Kimi-Audio technical report,” 2025, arXiv:2504.18425. [22] Qwen Team, “Qwen3-Omni technical report,” 2025, arXiv:2509.17765. [23] Google, “Gemini 3.1 Flash Live Preview,” Google AI docu-mentation, 2026, Accessed: 2026-06-09. [24] S. Ray et al., “ τ -Voice: Benchmarking full-duplex voice agents on real-world domains,” 2026, arXiv:2603.13686. [25] Haoyang Zhang et al., “DuplexSLA: A full-duplex spoken lan-guage model with synchronized speech, language, and action,” 2026, arXiv:2605.20755. [26] S. Kuroki et al., “KAME: Tandem architecture for enhancing knowledge in real-time speech-to-speech conversational AI,” 2025, arXiv:2510.02327. [27] Chung-Ming Chien et al., “MoshiRAG: Asynchronous knowl-edge retrieval for full-duplex speech language models,” 2026, arXiv:2604.12928. [28] Thinking Machines, “Interaction models: A scalable approach to human-ai collaboration,” Thinking Machines, 2026, Ac-cessed: 2026-06-09. [29] Qwen Audio Team, “Qwen Audio Agent: A real-time voice runtime for AI agents,” GitHub, 2026, Accessed: 2026-09-11. [30] OpenAI, “Getting started with GPT-Live,” OpenAI documen-tation, 2026, Accessed: 2026-09-15. [31] Edresson Casanova et al., “Open full-duplex voice agent with speech-to-speech language model,” in Proc. IEEE ASRU ,2025. [32] Edresson Casanova et al., “VoiceChat-TTS: A low-latency continuous speech synthesis model for interactive agents,” 2026, arXiv:2608.13831. [33] LangChain, Inc., “LangGraph,” GitHub, 2024. [34] ServiceNow-AI, “BFCL v3 audio dataset,” Hugging Face, 2025. [35] Daniel Lin et al., “Full-duplex-bench v3,” GitHub, 2025. [36] T. Bogavelli et al., “EVA-Bench: A new end-to-end framework for evaluating voice agents,” 2026, arXiv:2605.13841. [37] Qwen Team, “Qwen3-30B-A3B,” Hugging Face, 2025. [38] Google DeepMind, “Gemini 3.1 Flash-Lite model card,” Google DeepMind, Mar. 2026, Accessed: 2026-09-11. [39] NVIDIA, “Frontend/backend agent cascaded example,” GitHub, Accessed: 2026-09-11. [40] LiveKit, “EXA Deep Researcher,” GitHub, Accessed: 2026-09-11. [41] Neel Joshi, “Turn your voice into action with new productivity features in Gemini Live,” Google Blog, Aug. 2026, Accessed: 2026-09-11. [42] Ke Hu et al., “Enabling streaming user transcription in full-duplex speech-to-speech models,” 2026, arXiv:2609.15759. [43] NVIDIA NeMo Team, “Parakeet-TDT-0.6B-v3: A multi-lingual streaming speech recognition model,” Hugging Face, 2025. [44] NVIDIA, “Nemotron-Nano-9B-v2-Base: A 9B Parameter Language Model for Reasoning and Instruction Following,” 2025, Hugging Face Model Hub. [45] NVIDIA et al., “Nemotron 3 Nano: Open, efficient mixture-of-experts hybrid mamba-transformer model for agentic rea-soning,” 2025, arXiv:2512.20848. [46] Google DeepMind, “Gemma-4-31B-it,” Hugging Face, 2026, Accessed: 2026-09-14. [47] Qwen Team, “Qwen3.5-397B-A17B,” Hugging Face, 2026, Accessed: 2026-09-14. [48] NVIDIA, “Parakeet TDT 0.6B V2,” Hugging Face, 2025, Ac-cessed: 2026-09-14. [49] NVIDIA, “When2Call Dataset,” Hugging Face, Accessed: 2026-06-09. [50] Qwen Team, “Qwen3-235B-A22B,” Hugging Face, 2025. [51] Resemble AI, “Chatterbox-TTS,” GitHub, 2025. [52] S. G. Patil et al., “Berkeley function calling leaderboard,” BFCL leaderboard, 2024. [53] Shishir G. Patil et al., “Berkeley function calling leaderboard,” GitHub, 2024. [54] Qwen Team, “Qwen2.5-7B-Instruct,” Hugging Face, 2024. [55] Fixie.ai, “Ultravox-v0.6 Llama-3.1-8B,” Hugging Face, 2025, Accessed: 2026-09-09. [56] Fixie.ai, “Ultravox-v0.6 Qwen-3-32B,” Hugging Face, 2025, Accessed: 2026-09-09. [57] OpenAI, “GPT-Realtime mini Model,” OpenAI documenta-tion, Accessed: 2026-06-09. [58] Guan-Ting Lin et al., “Full-Duplex-Bench-v3: Benchmarking tool use for full-duplex voice agents under real-world disflu-ency,” 2026, arXiv:2604.04847. [59] Deokjin Seo, Gangin Park, and Kihyun Nam, “Chatterbox-Flash: Prior-calibrated block diffusion for streaming zero-shot TTS,” 2026, arXiv:2605.30748. [60] NVIDIA Corporation, “Triton Inference Server: An optimized cloud and edge inferencing solution,” GitHub, 2026, Accessed: 2026-06-08. [61] OpenAI, “GPT-Realtime-2 Model,” OpenAI documentation, Accessed: 2026-06-09. [62] Y. Chen et al., “VoiceBench: Benchmarking LLM-based voice assistants,” 2024, arXiv:2410.17196. [63] Vaibhav Srivastav et al., “Open ASR Leaderboard: Towards re-producible and transparent multilingual and long-form speech recognition evaluation,” 2025, arXiv:2510.06961.