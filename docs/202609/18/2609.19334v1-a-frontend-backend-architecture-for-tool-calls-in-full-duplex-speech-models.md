---
title: A frontend-backend architecture for tool calls in full-duplex speech models
title_zh: 面向全双工语音模型中工具调用的前端-后端架构
authors: "Ke Hu, Slyne Deng, Chen Chen, Elena Rastorgueva, Edresson Casanova, Punit Kumar, Dharmendra Choudhary, Nikhil Srihari, Ameya Sunil Mahabaleshwarkar, Viet Anh Trinh, Slim Essid, Oluwatobi Olabiyi, Zhehuai Chen"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.19334v1"
tags: ["query:duplex-agent"]
score: 9.0
evidence: 全双工语音模型与LLM工具调用，保持话轮转换和低延迟
tldr: "全双工语音模型需要工具调用能力但现有方案易损害交互自然性。本文提出前端-后端架构：前端双工语音转文本发出委托令牌，将流式ASR转录转发给文本后端LLM执行工具调用，结果经预填充和重复机制注入并合成语音。系统实现92-97%工具调用召回率和81.2%无关调用拒绝准确率，搭配大后端在Full-Duplex-Bench-V3和EVA-Bench表现优异。该后端委托方法模块化且有效，兼顾自然双工交互与强大代理能力。"
source: arxiv
selection_source: fresh_fetch
motivation: 全双工语音模型需要工具调用以完成语音代理任务，但现有方法容易破坏低延迟交互和自然打断处理。
method: 提出前端-后端架构：前端双工语音转文本发出委托令牌，将ASR转录转发给文本后端LLM执行工具调用，结果经预填充和重复机制注入合成语音。
result: "单轮工具调用召回率92-97%，无关调用拒绝准确率81.2%，搭配Qwen3-235B-A22B在多个基准上取得竞争力或更优性能。"
conclusion: 后端委托是一种模块化有效方法，能结合自然全双工语音交互与强大代理工具调用能力。
---

## 摘要
全双工语音到语音（S2S）模型提供自然、低延迟的对话交互，并将受益于使用外部工具和完成语音代理任务的能力。我们提出一种前端-后端架构，其中双工语音转文本前端学会发出一个委托令牌，并将流式 ASR 转录转发给基于文本的后端大语言模型以进行工具调用。后端返回的工具调用结果通过轻量级预填充与重复机制注入回前端，然后使用流式 TTS 合成语音给用户。我们的方法在很大程度上保持了常规双工轮流发言、打断处理和低延迟交互，因为它只需对前端模型进行最小修改。在单轮工具调用评估中，我们的系统实现了 92-97% 的工具调用召回率、有竞争力的工具调用预测性能，以及在拒绝无关调用方面 81.2% 的准确率。当配备更大的后端（例如 Qwen3-235B-A22B）时，与开源和闭源模型相比，我们的系统在 Full-Duplex-Bench-V3 上取得了有竞争力的结果，并在 EVA-Bench 上显著优于 GPT-realtime-mini 和 Qwen3-Omni-30B-A3B-Instruct。这些结果表明，后端委托是一种有效且模块化的方法，可将自然双工语音交互与强大的智能体工具调用能力结合起来。

## Abstract
Full-duplex speech-to-speech (S2S) models provide natural, low-latency conversational interaction and would benefit from the ability to use external tools and complete voice-agent tasks. We propose a frontend-backend architecture where a duplex speech-to-text frontend learns to emit a delegation token and forwards streaming ASR transcripts to a text-based backend LLM for tool calls. Tool-call results from the backend are injected back into the frontend through a lightweight prefill-and-repeat mechanism and then synthesized using streaming TTS to the user. Our approach largely preserves regular duplex turn-taking, interruption handling, and low-latency interaction as it requires minimal modifications to the frontend model. In a single-turn tool-call evaluation, our system achieves 92-97% tool-call recall, competitive tool-call prediction performance, and 81.2% accuracy in rejecting irrelevant calls. When equipped with a larger backend (e.g., Qwen3-235B-A22B), our system achieves competitive results on Full-Duplex-Bench-V3 compared to open and closed source models, and significantly outperforms GPT-realtime-mini and Qwen3-Omni-30B-A3B-Instruct on EVA-Bench. These results demonstrate that backend delegation is an effective and modular approach for combining natural duplex speech interaction with strong agentic tool-call capabilities.

---

## 论文详细总结（自动生成）

# 论文总结：面向全双工语音模型中工具调用的前端-后端架构

## 1. 核心问题与整体含义

- **研究背景**：全双工语音到语音（S2S）模型能提供自然、低延迟的对话交互，擅长轮流发言、打断处理和保留副语言信息，但其工具调用能力仍明显落后于文本大语言模型（LLM）。
- **核心问题**：是否应当让双工语音模型直接内化工具调用能力，还是将工具调用委托给成熟的文本后端智能体？
- **动机依据**：论文引用 τ-Voice 的结果，商业双工语音模型在干净条件下仅完成 31–51% 的客服任务，而文本智能体（如 GPT-5）可达 85%；音频原生建模会消耗参数和上下文预算，削弱知识、指令跟随和工具调用能力。
- **整体含义**：提出一种模块化的前端-后端架构，让前端双工语音模型专注自然语音交互，后端文本 LLM 负责工具调用和复杂推理，从而在保持低延迟、可打断的双工体验的同时，获得接近文本智能体的工具调用能力。

## 2. 方法论

### 2.1 核心思想

- 前端是双工语音转文本（STT）模型，学习在语音查询需要工具调用时发出一个**委托令牌**（delegation token）。
- 前端将流式 ASR 转录发送给后端文本 LLM，由后端执行工具调用。
- 后端返回的自然语言结果通过**预填充+重复（prefill-and-repeat）**机制注入前端 agent 文本通道，再由前端合成语音回复给用户。
- 工具调用期间前端保持静默，工具调用结束后恢复正常双工行为。

### 2.2 前端结构

- 前端由 **600M 参数 Parakeet 流式语音编码器**和 **NVIDIA Nemotron-Nano-9B-v2-Base 大语言模型骨干**组成。
- 骨干 LLM 同时服务两条输出路径：
  - **ASR 头**：输出用户流式转录。
  - **agent 文本头**：输出代理文本。
- 一次解码同时产生用户文本和代理文本。

### 2.3 工具调用令牌设计

- 在 agent 文本通道中预测 `<tc_bos>` 表示触发工具调用。
-
