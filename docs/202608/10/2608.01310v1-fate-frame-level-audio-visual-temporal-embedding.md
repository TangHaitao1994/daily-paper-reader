---
title: "FATE: Frame-Level Audio-Visual Temporal Embedding"
title_zh: "FATE: 帧级音视频时序嵌入"
authors: "Kaisi Guan, Bingzi Zhang, Xihua Wang, Ying Ba, Xin Cheng, Yijing Chen, Ruihua Song"
date: 2026-08-02
pdf: "https://arxiv.org/pdf/2608.01310v1"
tags: ["query:speech-model"]
score: 9.0
evidence: 帧级音视频时间嵌入，实现视觉与语音的语义和时间对齐。
tldr: 现有音视频模型通常要么侧重语义匹配而忽略时间信息，要么专注时间同步但缺乏语义理解，难以同时捕捉“什么声音”与“何时发生”。为此，本文提出FATE帧级音视频时序嵌入，保留帧级序列并在物理时间线上严格对齐，通过跨视频语义和视频内时序的联合对比学习，建模细粒度同步关系。实验表明，FATE在时序与语义检索、零样本事件定位和生成质量评估上均大幅超越现有方法，零样本定位性能媲美全监督方案，且与人类判断的相关性最优，为音视频理解提供了统一的同步感知嵌入空间。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01310v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 749, \"height\": 1128, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01310v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1799, \"height\": 804, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01310v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 869, \"height\": 510, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01310v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1791, \"height\": 732, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01310v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1844, \"height\": 858, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01310v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 809, \"height\": 515, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01310v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 886, \"height\": 341, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01310v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 793, \"height\": 612, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01310v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 896, \"height\": 216, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01310v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 948, \"height\": 874, \"label\": \"Table\"}]"
motivation: 现有嵌入模型丢失时序信息，同步模型缺乏语义理解，无法同时回答“什么声音”与“何时发生”。
method: 提出FATE帧级时序嵌入，保留音视频帧级序列并在时间线上严格对齐，通过跨视频语义与视频内时序对比学习联合训练。
result: 在时序/语义检索、零样本事件定位和生成评估上均显著超越基线，零样本定位媲美全监督，与人类判断相关性最优。
conclusion: FATE统一语义与同步，为音视频理解提供帧级同步感知嵌入，在多个任务上验证其有效性。
---

## 摘要
当一只狗张嘴吠叫时，人类能自然地识别出是什么声音以及何时发生。构建具有同样能力的音视频模型所需的表征需同时捕捉语义和时间上的对齐。现有的方法各有所偏颇：嵌入模型匹配语义却丢失时间信息；同步模型捕获时间偏移但缺乏语义理解。为弥合这一差距，我们提出FATE，即帧级音视频时序嵌入。不同于先前将每个模态池化为单个嵌入并丢弃时间信息的嵌入模型，FATE保留帧级序列，在物理时间轴上对齐它们，并在严格对齐的帧对上计算相似度。不同于只输出偏移预测的同步模型，FATE将同步编码到可复用的嵌入空间中，通过联合跨视频语义对比学习和视频内时序对比学习的目标进行训练，以同时捕获什么声音和何时发生。在三个任务上，FATE在时序和语义检索上大幅超越最强基线，在零样本设置下的事件定位上匹配全监督方法，并作为生成评估指标实现了与人类判断的最佳相关性。源代码可在 \texttt{https://github.com/guankaisi/FATE} 找到。

## Abstract
When a dog opens its mouth and barks, humans naturally recognize what the sound is and when it occurs. Building audio-visual models with this same ability requires representations that capture both semantic and temporal alignment. Current approaches fall short on one side or the other: embedding models match semantic but lose temporal information; synchronization models capture temporal offsets but lack semantic understanding. To bridge this gap, we propose FATE, Frame-level Audio-visual Temporal Embedding. Unlike prior embedding models that pool each modality into a single embedding and discard temporal information, FATE retains frame-level sequences, aligns them on the physical timeline, and computes similarity over strictly aligned frame pairs. Unlike synchronization models that output only an offset prediction, FATE encodes synchronization in a reusable embedding space, trained with a joint objective combining cross-video semantic and within-video temporal contrastive learning to capture both what sounds and when it occurs. Across three tasks, FATE surpasses the strongest baseline on temporal and semantic retrieval by a large margin, matches fully supervised methods on event localization in a zero-shot setting, and achieves the best correlation with human judgments as a generation evaluation metric. The source code can be found at \texttt{https://github.com/guankaisi/FATE}.

---

## 论文详细总结（自动生成）

好的，以下是对论文《FATE: Frame-Level Audio-Visual Temporal Embedding》的结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**: 现有的音视频模型在“语义理解”（*什么*声音）和“时序同步”（声音发生在*何时*）两个关键能力上存在割裂，难以兼顾。
    *   **嵌入模型**：擅长跨模态语义匹配，但通常因全局池化操作丢弃了细粒度的时间信息。
    *   **同步模型**：能精确估计音视频间的时间偏移，但任务专用性强，缺乏通用的语义理解能力。
*   **整体含义**: 本文旨在弥合这一差距，提出一个**统一的、能同时编码语义和时序对齐信息的帧级嵌入空间**，使单一模型即可胜任需要同时理解“是什么”和“在何时”的下游任务。

### 2. 论文提出的方法论

核心思想是抛弃传统的全局池化，在**帧级别**上对齐和比较音视频特征。

*   **核心思想与架构**:
    *   **帧级特征提取**：保持双编码器输出的完整时空序列特征 \( F_V \in \mathbb{R}^{T_v \times D} \) 和 \( F_A \in \mathbb{R}^{T_a \times D} \)，不做全局池化。
    *   **时间对齐**：以音频序列长度为基准，使用**最近邻插值**将更长的视频特征序列降采样至与音频相同长度 \( T_a \)。这一操作基于视频帧间冗余度高、而音频包含关键瞬态信息的假设。
    *   **帧级相似度计算**：在对齐后的两个序列上，沿时间轴计算各帧对的内积，并取**均值**作为最终的音视频相似度 \( S_{AV} \)。这种设计使得时间偏移会直接导致对齐帧对不匹配，从而降低相似度。

*   **联合训练策略**:
    *   **语义对比损失 (\( \mathcal{L}_{\text{sem}} \))**：采用跨视频的对称InfoNCE损失，使来自同一视频的音频和视频嵌入在特征空间中靠近，不同视频的则远离。为扩大负样本池，使用了跨GPU的特征收集。
    *   **时序软对比损失 (\( \mathcal{L}_{\text{temp}} \))**：针对同一视频，构造多个不同时间偏移的音频片段作为候选。使用一个**升余弦核函数**生成软标签（而非0/1硬标签），对完美对齐的片段给予最高权重，对偏移微小、内容高度重叠的片段给予中等权重，对严重偏移的片段给予零权重。模型通过交叉熵损失来匹配这种平滑的相似度分布。
    *   **总目标**: \( \mathcal{L}_{\text{total}} = \lambda_1 \mathcal{L}_{\text{sem}} + \lambda_2 \mathcal{L}_{\text{temp}} \)。

### 3. 实验设计

*   **数据集与场景**:
    *   **训练**: 使用 **VGGSound** 数据集（约18.3万个10秒视频），切分为2秒片段进行训练。
    *   **评估基准与方法**:
        1.  **时序跨模态检索**: 在 **AVSync15** 和 **VGG-Sync** 数据集上进行。分“视频内”（纯时序判断）和“视频间”（需同时完成语义与时序判断）两种设定。使用 `R@k` 和 `NDCG@k` 指标。
        2.  **音视频事件定位**: 在 **AVE** 数据集上进行零样本评估，报告分段准确率。
        3.  **生成效果评估**: 使用五个生成模型生成的 **750个视频**，收集人类评估的平均意见分（MOS），再用斯皮尔曼相关系数（\( \rho \)）衡量各自动指标（包括FATE）与人类判断的一致性。

*   **对比方法**:
    *   **音视频基础模型**: ImageBind, LanguageBind, CAV-MAE Sync, PE-AV。
    *   **专用同步/质量评估模型**: CAVP, PEAVS, Synchformer, DeSync, AV-Align。
    *   **事件定位监督模型**: DCCA, AVDLN, DAM。

### 4. 资源与算力

*   **硬件**: 使用**8块NVIDIA A800 GPU**进行训练。
*   **框架与精度**: 未明确说明框架，但提及使用 `bfloat16` 混合精度训练。
*   **训练时长**: 骨干网络冻结，仅更新2.3%的参数（使用LoRA），共训练**5个epoch**。单视频推理时间约为10.8秒，与基础的PE-AV模型（9.6秒）接近。

### 5. 实验数量与充分性

*   **实验数量**: 论文进行了相当数量的实验，覆盖了多个维度，非常充分。
    *   **主要实验**: 在3个差异化的下游任务上进行了评估，其中检索任务还细分了“视频内/视频间”两种设定。
    *   **消融研究**: 针对核心设计进行了详尽的消融实验，包括：
        *   时序标签设计（软标签 vs. 硬标签及不同窗口大小）。
        *   对齐策略（最近邻 vs. 线性插值/重复填充，以及下采样方向）。
        *   损失函数构成（单独 \( \mathcal{L}_{\text{sem}} \)， 单独 \( \mathcal{L}_{\text{temp}} \)， 联合）。
        *   移除帧级设计的对比（等同于仅微调PE-AV）。
    *   **分析性实验**: 包括模型时序敏感度曲线分析、与通用多模态LLM的案例对比。

*   **公平性**: 对比方法广泛且有代表性，均使用官方发布的模型权重。消融实验中，控制变量的做法（如“w/o Frame-level”变体）非常严谨，确保了对比的公平性。

### 6. 论文的主要结论与发现

*   **帧级结构是捕捉时序信息的关键**：保留帧级特征并沿时间轴对齐，天然地将时序同步性内化到了相似度计算中，性能远超所有基于全局池化的嵌入模型。
*   **语义与时序学习相辅相成**：联合训练目标中的语义和时序损失函数表现出**超加性效应**，二者结合的效果远超各自单独使用的效果之和。
*   **统一表示空间具有强大的泛化能力**：FATE无需任何任务特定的微调，即可在检索、零样本定位、生成评估等多个下游任务上取得最优或极具竞争力的性能。
*   **作为生成评估指标与人类判断高度一致**：FATE在所有对比指标中，与人类对生成视频同步性的感知相关性最高。

### 7. 优点

*   **方法简洁有效**: “保留帧级序列并对齐”的核心思想直观且高效，从根源上解决了信息瓶颈问题。
*   **统一的嵌入空间**: 成功将语义和时序两种能力统一到一个可复用的嵌入空间中，避免了任务专用模型。
*   **精巧的软标签设计**: 使用时序软对比损失来处理时间偏移样本间的高重叠问题，符合数据的内在结构。
*   **实验设计严谨**:
    *   “视频内/视频间”的检索设定巧妙地将模型的两个能力解耦评估。
    *   零样本事件定位实验有力地证明了模型的泛化性。
    *   与人类评价的生成评估实验具有高度的实际应用价值。

### 8. 不足与局限

*   **帧级嵌入的存储开销**: 保留全帧序列特征导致每视频存储需求（3.4 MB）远高于全局池化模型（136 KB），这可能限制其在超大规模检索库上的应用。
*   **训练数据的潜在风险**: 训练集VGGSound和下游检索/生成评估的测试集可能存在类别或域的重叠，导致模型对该特定音视频分布过拟合的风险。
*   **生成评估的局限性**: 尽管FATE与人类判断的相关性最高，但样本级的绝对相关性数值（17.24）仍较为有限，表明对生成内容的细粒度同步评估仍是一个开放性问题。
*   **对齐策略的依赖**: 模型性能对齐策略（锚定音频、最近邻下采样）敏感，可能在极端或特定领域场景中需要调整。

（完）
