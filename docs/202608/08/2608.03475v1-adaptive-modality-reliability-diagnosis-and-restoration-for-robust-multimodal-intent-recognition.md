---
title: Adaptive Modality Reliability Diagnosis and Restoration for Robust Multimodal Intent Recognition
title_zh: 自适应模态可靠性诊断与修复用于鲁棒多模态意图识别
authors: "Suraj Kumar, Mohnish Raj, Soumi Chattopadhayay, Chandranath Adak, Ayan Dutta"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03475v1"
tags: ["query:speech-model"]
score: 8.0
evidence: 通过自适应模态可靠性诊断实现鲁棒多模态意图识别，融合语音、文本、视觉特征改善语音交互
tldr: 多模态意图识别融合语言、声学、视觉信息，但单一模态可能发生噪声、缺失、冲突或主导偏差等问题。现有方法仅隐式推断重要性然后重新加权或抑制输入，未确定退化模态能否修复和再次信任。本文提出PRIME框架，通过诊断证据（预测置信度、认知分歧等）估计模态弱点，并采用原型条件变分修复模块重建退化表示，修复后重新评估可靠性，进而指导逆方差融合。实验表明PRIME在保持干净数据性能的同时，显著提升了缺失、噪声、冲突及模态不平衡条件下的鲁棒性。贡献了一种闭环的模态可靠性诊断与修复机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.03475v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1853, \"height\": 679, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.03475v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 895, \"height\": 200, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03475v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1582, \"height\": 685, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03475v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 862, \"height\": 270, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03475v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 801, \"height\": 476, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03475v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 796, \"height\": 347, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03475v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 711, \"height\": 520, \"label\": \"Table\"}]"
motivation: 多模态意图识别常面临模态噪声、缺失等问题，现有方法无法判断退化模态是否可修复并重新信任。
method: PRIME利用预测置信度、共识等诊断证据估计模态弱点，通过原型条件变分修复重建退化表示，并重估可靠性实现逆方差融合。
result: 在多模态意图识别基准上，面对缺失、噪声、冲突和不平衡条件，PRIME鲁棒性显著提升，且干净数据性能无损失。
conclusion: PRIME通过诊断-修复-重估闭环实现了自适应多模态融合，提升了意图识别在复杂环境下的鲁棒性。
---

## 摘要
多模态意图识别融合了语言、声学和视觉证据，但单个模态可能存在噪声、缺失、语义冲突或不成比例地占据主导。现有方法通常隐式地推断模态重要性，并通过对不可靠输入重新加权或抑制来处理，但未判断退化模态是否能被修复并随后信任。我们提出 PRIME（精度加权可靠性推断与模态恢复），一个闭环的可靠性引导框架，在样本层联合诊断、恢复和重新评估模态质量。PRIME 通过从互补诊断证据（包括预测置信度、认知分歧、跨模态一致性和特征退化）估计的上下文对数方差来表示每个模态的弱点。由于模态可靠性标注不可用，该估计器通过使用已知退化严重程度的受控模态损坏以及异方差不确定性目标进行显式训练。与直接丢弃不可靠模态不同，PRIME 利用其估计的弱点来控制原型条件变分恢复模块，从互补模态重建退化表示。至关重要的是，在恢复后重新估计可靠性，使模型能够判断修复后的表示是否已足够可信以用于预测。得到的恢复后精度用于逆方差多模态融合。在多模态意图识别基准上的实验表明，PRIME 保持了有竞争力的干净数据性能，同时在缺失、噪声、冲突和模态不平衡条件下提高了鲁棒性。

## Abstract
Multimodal intent recognition combines linguistic, acoustic, and visual evidence, but individual modalities may be noisy, missing, semantically conflicting, or disproportionately dominant. Existing methods typically infer modality importance implicitly and either reweight or suppress unreliable inputs, without determining whether a degraded modality can be repaired and subsequently trusted. We propose PRIME (Precision-weighted Reliability Inference and Modality rEstoration), a closed-loop reliability guided framework that jointly diagnoses, restores, and reassesses modality quality at the sample level. PRIME represents the weakness of each modality through a contextual log-variance estimated from complementary diagnostic evidence, including predictive confidence, epistemic disagreement, cross-modal consensus, and feature degeneracy. Because modality-reliability annotations are unavailable, the estimator is explicitly trained using controlled modality corruption with known degradation severity, together with a heteroscedastic uncertainty objective. Rather than directly discarding an unreliable modality, PRIME uses its estimated weakness to control a prototype-conditioned variational restoration module that reconstructs the degraded representation from complementary modalities. Crucially, reliability is re-estimated after restoration, allowing the model to determine whether the repaired representation has become sufficiently trustworthy to contribute to prediction. The resulting post-restoration precisions are used for inverse-variance multimodal fusion. Experiments on multimodal intent-recognition benchmarks show that PRIME maintains competitive clean-data performance while improving robustness under missing, noisy, conflicting, and modality-imbalanced conditions.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义

- **研究背景**：多模态意图识别（MIR）旨在融合文本、语音、视频等多源信息预测交际意图，在对话分析、情感计算等领域具有重要应用。
- **核心痛点**：现实场景中单个模态常出现噪声、缺失、冲突或被某一主导模态（尤其是文本）所淹没，现有方法主要隐式地学习模态权重或直接抑制不可靠模态，但未回答一个根本问题——“一个退化的模态能否被修复并重新获得信任？”
- **整体含义**：论文提出一个闭环式“诊断-修复-重评估”框架，显式地估计模态可靠性，尝试修复受损模态，并在修复后重新评判其可信度，从而在恶劣条件下实现更鲁棒、更可靠的融合预测。

### 2. 方法论

**核心思想**：  
将模态可靠性显式建模为可学习的样本级变量，并通过“可靠性估计→跨模态修复→可靠性重估→精度加权融合”形成闭环，而非简单地丢弃或抑制弱模态。

**关键技术细节**：

- **可靠性诊断**  
  使用冻结的 Qwen2.5-Omni-7B 编码器提取各模态嵌入，经掩码注意力池化得到固定长度向量。对每个模态计算四种互补诊断探针：
  - 能量分数置信度 \(F_{\text{en}}\)
  - 基于轻量集成的认知不确定性 \(F_{\text{ep}}\)
  - 与跨模态共识的 JS 散度 \(F_{\text{co}}\)
  - 特征退化（嵌入范数）\(F_{\text{de}}\)

  探针结果经层归一化后形成诊断描述符 \(\mathbf{D}_m\)。

- **上下文可靠性估计器（CRE）**  
  将三个模态的诊断描述符堆叠并通过 Transformer 编码器（沿模态维度）进行交互，预测对数方差 \(s_m\)，进而得到模态弱点 \(\omega_m = \sigma(s_m)\) 和精度 \(\pi_m^{\text{pre}} = \exp(-s_m)\)。

- **可靠性引导的恢复**  
  - 对每个目标模态，以可靠互补模态的表示为查询，从可学习的类别原型记忆 \(\mathbf{P}\) 中检索语义信息。
  - 采用变分生成器从源模态修复目标模态的残差 \(\Delta \hat{\mathbf{h}}_m\)，并通过 KL 散度正则化。
  - 利用批量内可靠源筛选（基于精度中位数阈值）选择可靠的互补模态参与残差合成，避免不可靠源引入噪声。

- **闭环可靠性更新**  
  对增强后的表示重新运行诊断探针和 CRE，获得恢复后精度 \(\pi_m^{\text{post}}\)。

- **精度加权融合**  
  利用逆方差加权融合增强后表示，得到最终多模态表征用于意图分类。

- **训练目标**  
  由于缺乏可靠性标注，通过受控模态损坏生成监督信号。损失函数包括：
  - 分类损失（类别平衡交叉熵）
  - 异方差不确定性损失 \(L_{\text{het}}\)（同时优化分类与对数方差）
  - 损坏严重性回归损失 \(L_{\text{sev}}\)（弱点预测与注入损坏程度对齐）
  - 残差重构损失 \(L_{\text{res}}\)、语义一致性损失 \(L_{\text{gc}}\)
  - 原型负载均衡 \(L_{\text{lb}}\) 和类一致性约束 \(L_{\text{pc}}\)
  - KL 散度损失 \(L_{\text{kl}}\)

### 3. 实验设计

- **数据集与场景**：
  - MIntRec（单轮对话，20类）
  - MIntRec2.0（多轮多人，30类）
  - 针对缺失、噪声、冲突、文本主导等退化条件进行了鲁棒性评估（具体条件在正文中说明）。

- **基准对比方法**：  
  共对比 11 个代表性方法，包括：
  - 经典融合模型：MISA、MulT、MAG-BERT、TCL-MAP、MVCL-DAF、WDMIR
  - 可靠性/鲁棒性方法：MIntOOD、ECFMIR、CDPR、CR-3WD
  - MLLM 框架：HIER

- **评价指标**：Accuracy、Precision、Recall、F1、加权精度（WP）、加权 F1。

### 4. 资源与算力

- **编码器**：冻结的 Qwen2.5-Omni-7B，并通过 LoRA 适配。
- **优化器**：AdamW，早停机制（耐心值=5）。
- **训练细节**：嵌入维度 256，训练时随机选择模态进行合成损坏。
- **报告方式**：取 5 个随机种子（1–5）的测试结果，给出均值和标准差（Table II 标注了标准差）。
- **算力信息**：论文未提供 GPU 型号、显存、训练时长或批量大小等具体算力资源数据，仅在附录中提及补充细节，但正文未明确说明算力消耗。

### 5. 实验数量与充分性

- **实验门类**：
  - 主对比实验：在两个数据集上与 11 个基线对比（Table II）。
  - 组件消融（PRIME 各模块作用）：Table III。
  - 诊断探针消融与单探针实验：Table IV。
  - 恢复模态选择研究：Table V。
  - 损失函数消融：Table VI。
- **统计验证**：Table II 给出了标准差；附录提及显著性检验（但正文未详细呈现）。
- **充分性与客观性**：  
  - 对比基线丰富，覆盖经典融合、可靠性方法及最新 MLLM 方法。
  - 消融实验系统拆解了可靠性估计器、恢复模块、探针组合和损失项，设计较为严谨。
  - 未展示针对不同噪声强度、数据比例均衡性的鲁棒性量化，但该部分在正文中有所提及并在附录补充。
  - 总体实验数量充足，且未发现刻意偏向自身方法的严重偏差。

### 6. 主要结论与发现

- PRIME 在两个基准上均取得最优性能，尤其在 MIntRec 上 WF1 达到 **81.53%**，在 MIntRec2.0 上 WF1 达到 **64.57%**，超越先前最优 HIER。
- 去除任一模块（CRE、恢复、精度融合）均导致性能下降，验证闭环设计各部分互补有效。
- 四种可靠性探针缺一不可，融合所有探针效果最佳，表明单靠置信度不足以估计可靠性。
- 对所有模态进行恢复比仅恢复部分模态效果更好，文本与视频恢复贡献更大。
- 异方差损失和损坏严重性回归损失对学习可靠性最为关键；恢复相关的损失项（重构、语义一致性、KL）也均正向贡献。
- 框架在不显著牺牲干净数据性能的前提下，显著提升在缺失、噪声等退化条件下的鲁棒性。

### 7. 优点

- **问题定义新颖**：从“修复退化模态并重估其可信度”的角度突破传统丢弃/加权思路，闭环架构逻辑清晰。
- **诊断驱动可靠性**：设计四种互补探针（能量置信度、认知不确定性、共识分歧、表示质量），捕捉多维度可靠性证据。
- **无监督可靠性学习**：通过受控损坏 + 异方差不确定性损失，在无标签条件下学习有意义的样本级可靠性。
- **修复而非丢弃**：原型条件变分残差修复保留原始信息，并通过源筛选防止不可靠模态扩散噪声。
- **实验全面且严谨**：多个数据集、大量基线、系统消融，以及多种子报告标准差。

### 8. 不足与局限

- **算力透明度不足**：未说明 GPU 类型、训练时间等，难以评估实际部署的轻量化程度。
- **数据集规模与多样性**：MIntRec 和 MIntRec2.0 仍是相对受控的对话场景，在更极端的真实世界噪声或开放性意图下的泛化能力未验证。
- **只涉及三模态**：缺乏对其他模态（如生理信号、深度信息）的扩展实验。
- **损失项较多**：平衡多个超参数 λ 可能对调参较为敏感，论文未详细讨论超参数影响。
- **修复效果的上限**：严重内容缺失的模态可能难以通过原型和残差完全修复，末端实用性存疑。
- **对文本主导性的依赖**：框架虽缓解了文本主导问题，但文本修复的重要性较高，可能仍隐式依赖文本。

（完）
