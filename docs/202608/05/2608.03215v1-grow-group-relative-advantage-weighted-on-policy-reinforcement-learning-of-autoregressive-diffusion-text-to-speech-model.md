---
title: "GROW: Group-Relative Advantage-Weighted On-Policy Reinforcement Learning of Autoregressive-Diffusion Text-to-Speech model"
title_zh: GROW：面向自回归扩散文本到语音模型的基于组相对优势的在线策略强化学习
authors: "Guanrou Yang, Tian Tan, Qian Chen, Ziyang Ma, Yakun Song, Zhikang Niu, Qi Chen, Wenming Tu, Haitao Li, Shan Yang, Xie Chen"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03215v1"
tags: ["query:speech-model"]
score: 9.0
evidence: 针对流匹配TTS的强化学习，提升清晰度和说话人相似度，避免随机扰动。
tldr: 流匹配TTS的强化学习因确定性ODE采样难以应用，现有方法引入随机SDE导致扰动和开销。GROW提出组相对优势加权在线策略RL，直接作用于流匹配目标，通过组内奖励标准化、Wasserstein-2速度惩罚和零均值有符号优势实现有效信用分配。在DiTAR模型上，GROW将平均词错率从2.016降至1.558，说话人相似度从0.676提升至0.715，同时训练速度比基线快2.9倍。该方法为流匹配语音合成提供了一种高效强化学习范式，并将开源完整代码与模型。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.03215v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1683, \"height\": 439, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03215v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 808, \"height\": 494, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03215v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 781, \"height\": 497, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03215v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 891, \"height\": 463, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.03215v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 755, \"height\": 498, \"label\": \"Table\"}]"
motivation: 现有流匹配TTS的强化学习方法需将确定性ODE转为随机SDE并跟踪似然比，引入扰动和高计算开销。
method: GROW采用组相对优势加权在线策略RL，对每组样本分别标准化奖励后重新加权回归，并利用Wasserstein-2速度惩罚与组平均基线转化为零均值有符号优势。
result: 在DiTAR上评估LibriSpeech和Seed-TTS，词错率降低0.458，说话人相似度提高0.039，UTMOS不变，训练速度是GRPO的2.9倍。
conclusion: GROW无需SDE转换，直接在标准流匹配目标上进行高效RL，取得性能提升与训练加速，并承诺开源全部代码与模型。
---

## 摘要
基于流匹配的文本到语音的强化学习因确定性ODE采样而变得复杂：轨迹级策略梯度方法通常将ODE转换为SDE并跟踪每步似然比，引入随机扰动和大量开销。我们提出GROW，一种直接在标准流匹配目标上操作的组相对优势加权在线策略强化学习方法。对于每个提示，GROW采样一组在线策略话语，分别对组内可理解性和说话人相似度奖励进行标准化，并将其组合以重新加权流匹配回归。Wasserstein-2速度惩罚将更新后的模型锚定到冻结的预训练参考模型。引入组均值奖励基线将奖励加权转换为优势加权。对于具有集中奖励的强预训练TTS模型，正指数加权由与奖励无关的自我模仿主导，而零均值带符号优势则保留了有效的组内信用分配。在DiTAR上实例化并在LibriSpeech和Seed-TTS EN/ZH上评估，GROW将平均WER从2.016降低到1.558，将说话人相似度从0.676提高到0.715，同时保持UTMOS。使用10-NFE训练rollout和32-NFE评估，GROW在保持可比性能的同时，训练速度比32-NFE DiTAR-GRPO快2.9倍。我们将开源完整的GROW代码、忠实的DiTAR复现以及所有模型检查点。

## Abstract
Reinforcement learning for flow-matching text-to-speech is complicated by deterministic ODE sampling: trajectory-level policy-gradient methods typically convert the ODE into an SDE and track per-step likelihood ratios, introducing stochastic perturbations and substantial overhead. We propose GROW, a group-relative advantage-weighted on-policy RL method that acts directly on the standard flow-matching objective. For each prompt, GROW samples a group of on-policy utterances, separately standardizes intelligibility and speaker-similarity rewards within the group, and combines them to reweight flow-matching regression. A Wasserstein-2 velocity penalty anchors the updated model to a frozen pretrained reference. A group-mean reward baseline is introduced to convert reward weighting into advantage weighting. For strong pretrained TTS models with concentrated rewards, positive exponential weighting is dominated by reward-agnostic self-imitation, whereas a zero-mean signed advantage preserves effective within-group credit assignment. Instantiated on DiTAR and evaluated on LibriSpeech and Seed-TTS EN/ZH, GROW reduces average WER from 2.016 to 1.558 and raises speaker similarity from 0.676 to 0.715 while keeping UTMOS. With 10-NFE training rollouts and 32-NFE evaluation, GROW retains comparable performance while training 2.9x faster than 32-NFE DiTAR-GRPO. We will open-source complete GROW codes, faithful DiTAR reproduction, and all model checkpoints.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：基于流匹配（Flow Matching）的文本到语音（TTS）模型（如自回归扩散模型DiTAR）在生成过程中依赖确定性常微分方程（ODE）采样，直接应用强化学习（RL）变得困难。
- **现有方法局限**：主流的轨迹级策略梯度方法（如GRPO）需要将采样ODE转换为随机微分方程（SDE），跟踪每步似然比，这会引入随机扰动并带来巨大的计算与存储开销，尤其在自回归逐块生成时问题更为严重。
- **整体含义**：GROW旨在解决“如何在确定性流匹配TTS模型中高效、稳定地进行RL微调”这一问题，提出一种直接作用于标准流匹配回归目标的组级优势加权方法，规避SDE转换，同时保留奖励信号的有效分配。

### 2. 论文提出的方法论
- **核心思想**：采用在线策略（on-policy）生成一组话语，对组内奖励进行标准化形成**零均值带符号的优势权重**，直接重新加权流匹配回归损失，并用冻结的预训练模型作为Wasserstein‑2速度锚点来稳定训练。
- **关键技术细节**：
  - **组采样与奖励**：对每个提示（prompt）采样G个候选音频，计算可理解度奖励 \( R_{\text{wer}} = 1 - \text{WER} \) 和说话人相似度奖励 \( R_{\text{sim}} = \cos(\text{嵌入相似度}) \)。
  - **奖励标准化与融合**：在组内对每类奖励分别进行Z‑score标准化，再按权重 \( \lambda_{\text{wer}}, \lambda_{\text{sim}} \) 求和得到每个样本的权重 \( w^{(k)} \)。
  - **优势权重的进化**：将传统指数奖励权重 \( \exp(\tau r) \) 通过组均值基线转换为中心化的优势权重 \( \exp(\tau A) \)，进一步利用**小优势**下的泰勒展开，去除常数项的自我模仿成分，得到线性且带符号的 \( A/\sigma \) 形式。该权重零均值、单位方差，强化组内正负样本的信用分配。
  - **损失函数**：在流匹配插值噪声上，使用加权均方误差逼近期望速度场，同时加入当前策略与冻结参考模型速度场的L2距离作为Wasserstein‑2上界罚项（权重 \( \beta_{W2} \)），防止因负权重导致的发散。
  - **训练流程**：仅对条件速度预测 \( v_c \) 计算损失（不包含CFG引导速度），并在采样时使用分类器自由制导（CFG）。
- **算法流程**（pseudocode描述）：
  - 从数据集抽取提示，使用当前策略生成G个音频。
  - 计算各音频的可理解度与相似度奖励，组内标准化后融合成权重。
  - 将提示与生成音频拼接，通过VAE得到目标隐变量，采样噪声与时间得到扰动隐变量。
  - 计算加权流匹配损失 + W2参考锚定损失 → 反向传播更新策略参数。
  - 遇全组奖励方差为零时重采样。

### 3. 实验设计
- **数据集与场景**：
  - **预训练**：Emilia语料库的英文与中文部分（约95K小时语音），使用Semantic‑VAE提取40Hz 64维隐变量。
  - **RL微调**：从Emilia英文/中文各10K条、LibriTTS train‑clean‑100英文10K条组成的30K多语言提示集，每步动态采样训练对。
  - **评估基准**：LibriSpeech‑PC test‑clean（1127条）、Seed‑TTS英文测试集（1088条）和中文测试集（2020条），均为零样本TTS。
- **对比方法**：
  - 预训练DiTAR模型（基线）。
  - DiTAR‑GRPO（强化版基线，结合MixGRPO的混合ODE‑SDE与CPS保持噪声系数）。
- **评估指标**：词错误率（WER）、说话人相似度（SIM）、UTMOS（感知质量）。
- **额外消融**：权重函数形式（线性优势 vs. 指数优势/原始奖励）、rollout时的CFG尺度与训练速度类型、学习率与W2锚定强度、奖励组合（单独或联合）。

### 4. 资源与算力
- 文中**未明确提及**具体GPU型号、数量或总训练时长。
- 提供了相对速度数据：在匹配的训练rollout NFE下，GROW比DiTAR‑GRPO快约1.1×；当GROW采用10‑NFE rollout时，可比32‑NFE的GRPO快2.9×。可见其对算力要求更低，但绝对算力资源未公开。

### 5. 实验数量与充分性
- **实验组数**：包含1个主对比表（含不同NFE设置）、4个消融表（权重、CFG/速度类型、学习率与锚定、奖励组合），总计约20+种不同配置。
- **充分性**：实验覆盖多语言、多测试集；主结果与对比、超参数敏感性、设计选择均通过消融验证；在3个测试集上指标一致向好。对比基线选取合理（强化版GRPO），训练与评估使用统一的说话人/识别模型，指标与奖励来源一致，公平性较好。但未测试其他流匹配TTS架构或奖励模型带来的泛化偏差，也未报告多次运行的方差。

### 6. 论文的主要结论与发现
- GROW在全部测试集上一致地**降低WER、提升SIM**，同时UTMOS保持在预训练水平，优于DiTAR‑GRPO。
- 在10‑NFE训练rollout下即可获得与32‑NFE相当的性能，训练效率大幅提升（最高2.9×快于GRPO 32‑NFE）。
- 权重设计是核心：**零均值带符号的线性优势**（\( A/\sigma \)）显著优于正指数权重，因为后者在奖励集中时由自我模仿主导，弱化了组内信用分配。
- 适度的W2锚定（\( \beta_{W2}=0.025 \)）对稳定训练至关重要，过弱（0）导致偏移，过强（1）抑制提升。
- 可理解性与说话人相似度联合奖励能同时取得两方面的单目标最佳增益，且两者存在不对称耦合（提升相似度附带改善WER）。

### 7. 优点
- **方法简洁高效**：无需ODE→SDE转换、无需轨迹似然计算，直接复用标准流匹配回归，训练开销小。
- **有效的信用分配**：分析并解决了强预训练模型下奖励集中时的“自我模仿陷阱”，提出的线性带符号优势权重创新性强且效果显著。
- **实际应用友好**：支持低NFE采样训练仍保持高性能，利于快速迭代。
- **实验扎实**：多语言、多指标、全面消融，结果稳健，并承诺开源全部代码与模型。

### 8. 不足与局限
- **奖励模型的固有问题**：奖励计算使用的外部ASR和说话人编码器冻结不变，可能引入偏好偏差，且与人类听感未必完全一致。
- **模型局限性**：仅基于DiTAR验证，虽可迁移至F5‑TTS等，但未在更多标准流匹配TTS模型上验证。
- **训练超参依赖**：学习率和W2锚定强度需小心调节，鲁棒性范围较窄。
- **未报告安全或伦理风险**：对于语音合成可能被滥用于伪造声音等风险未作讨论。
- **实验方差缺失**：无多次随机种子下的标准差，难以判断提升的统计显著性。

（完）
