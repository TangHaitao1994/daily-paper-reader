---
title: "ParaASR: Multi-Token Prediction for Fast and Long-Context LLM-Based Speech Recognition"
title_zh: ParaASR：面向快速长上下文大语言模型语音识别的多令牌预测
authors: "Qingjian Lin, Yuxin Li, Haoyang Zhang, Jun Chen, Yechang Huang, Feng Tian, Xie Li, Xiangyu Tony Zhang, Daijiao Liu, Yuxin Zhang, Jinglan Gong, Bo Zhao, Fei Tian, Xuerui Yang, Gang Yu, Xiangyu Zhang, Daxin Jiang"
date: 2026-07-31
pdf: "https://arxiv.org/pdf/2607.29279v1"
tags: ["query:speech-model"]
score: 9.0
evidence: 多token预测加速基于大语言模型的自动语音识别，同时保持准确性。
tldr: "LLM语音识别受制于自回归解码延迟与模型规模的矛盾，但ASR输出强依赖声学输入，适合并行解码。ParaASR创新引入多token预测，让4B LLM解码器每步预测6个token；先训练自回归基础模型，再分阶段优化对齐未来token分支。推理时验证前缀，平均采纳5.0/6 token，词错误率低至中文2.97%、英语3.68%、长格式3.70%，RTF 0.0053，支持32K上下文单次转录30分钟音频。该工作证明，声学锚定可打破解码器扩展、低延迟与长文本转录的传统权衡。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.29279v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1188, \"height\": 432, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29279v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1172, \"height\": 658, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.29279v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1272, \"height\": 96, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.29279v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1269, \"height\": 656, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.29279v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1263, \"height\": 144, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.29279v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1238, \"height\": 231, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.29279v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 996, \"height\": 922, \"label\": \"Table\"}]"
motivation: LLM语音识别的高质量与低延迟难以兼得，但ASR输出锚定语音信号，天然适合并行解码。
method: 提出ParaASR，采用多token预测让4B解码器每步生成6个候选token，经分阶段优化对齐未来分支，并以自回归验证仅接受确认前缀。
result: "平均接受5.0/6 token，中文WER 2.97%，英语3.68%，长格式3.70%，RTF低至0.0053，支持30分钟音频单次转录。"
conclusion: 证明通过声学锚定与验证，可同时实现解码器扩展、低延迟和长上下文转录，拓宽了LLM-ASR的部署边界。
---

## 摘要
音频编码器-大语言模型解码器架构已成为现代自动语音识别（ASR）的主导范式，通过大规模语言建模提升了转录质量。然而，自回归解码的成本随解码器规模线性增长，在识别质量与服务延迟之间形成了根本性的权衡。我们认为这种权衡并非固有：与开放式文本生成不同，ASR的输出与输入语音信号强相关，这为高并行度解码提供了自然的归纳偏置。基于此，我们提出ParaASR，一种利用多令牌预测（MTP）使40亿参数大语言模型解码器在每个前向步骤中输出多个令牌的ASR系统。从公开可用的音频-语言基础模型出发，系统首先建立一个稳健的自回归识别器，然后通过分阶段优化策略对齐五个未来令牌分支。在推理时，每个步骤提出六个令牌的延续，并仅将验证后的前缀纳入转录文本，从而保持了标准自回归解码的安全性。平均接受长度达到所提议六个令牌中的5.0个，证实了语音的确定性结构使ASR成为多令牌解码的天然场景。ParaASR进一步保留了原生32K上下文窗口，可在单次前向传递中转录长达30分钟的音频。在多个基准测试中，它在中文、英文和长篇评估上分别取得了2.97%、3.68%和3.70%的平均错误率，而实时因子（RTF）低至0.0053。这些结果表明，当未来令牌提议由声学信号锚定并经由自回归验证保护时，解码器扩展、低延迟推理和长上下文转录无需成为相互竞争的目标。

## Abstract
Audio-encoder-LLM-decoder architectures have become the dominant paradigm for modern automatic speech recognition (ASR), improving transcription quality through large-scale language modeling. However, the cost of autoregressive decoding scales with decoder size, creating a fundamental trade-off between recognition quality and serving latency. We argue this trade-off is not inherent: unlike open-ended text generation, ASR outputs are strongly anchored to the input speech signal, providing a natural inductive bias toward high-parallelism decoding. Building on this, we introduce ParaASR, an ASR system that leverages Multi-Token Prediction (MTP) to let a 4B LLM decoder emit multiple tokens per forward step. Starting from a publicly available audio-language foundation, the model first establishes a robust autoregressive recognizer and then aligns five future-token branches through a staged optimization recipe. At inference, it proposes a six-token continuation per step and admits only the verified prefix into the transcript, preserving the safety of standard autoregressive decoding. The average accepted length reaches 5.0 out of 6 proposed tokens, confirming that the deterministic structure of speech makes ASR an especially natural setting for multi-token decoding. ParaASR further retains a native 32K-context window and transcribes up to 30 minutes of audio in a single pass. Across diverse benchmarks, it attains average error rates of 2.97%, 3.68%, and 3.70% on Chinese, English, and long-form evaluations, respectively, while reaching a real-time factor (RTF) as low as 0.0053. These results show that decoder scaling, low-latency inference, and long-context transcription need not be competing goals when future-token proposals are anchored by the acoustic signal and guarded by autoregressive verification.

---

## 论文详细总结（自动生成）

好的，以下是基于论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义
*   **核心矛盾**：现代基于大语言模型的语音识别系统，其转录质量与推理延迟之间存在根本性权衡。解码器规模越大，语言理解能力越强，但逐令牌的自回归解码方式会导致计算成本线性增加，尤其在需要处理长音频时，延迟问题更为突出。
*   **核心洞察**：作者认为这种权衡并非ASR任务所固有。与开放式的文本生成不同，语音识别的输出（转录文本）与输入的语音信号存在强锚定关系，即给定音频和前置文本，后续内容在很大程度上是确定性的延续，而非开放式选择。这种确定性为高并行度解码提供了天然的归纳偏置。
*   **整体含义**：论文旨在证明，通过利用ASR任务的确定性结构，引入多令牌预测（MTP）机制，可以打破“解码器规模-推理延迟”的僵局，使得在保持大型解码器高精度的同时，实现极低的推理延迟，并原生支持长音频转录。

### 2. 论文提出的方法论
*   **核心思想：多令牌预测 (MTP)**
    *   将标准自回归解码（一次预测一个令牌）转变为并行预测多个未来令牌，从而大幅提升解码吞吐量。
    *   推理时，并行提议的多个令牌并非全部采纳，而是通过自回归验证机制，只接受与标准解码路径一致的、最长的“已验证前缀”，确保最终转录结果的准确性与标准自回归解码无异。这是一种安全的加速原语。

*   **模型架构**
    *   **基础骨干**：采用经典的“音频编码器-适配器-大语言模型解码器”架构。
        *   **音频编码器**：一个冻结的6亿参数Transformer，每80毫秒产生一个声学嵌入。
        *   **适配器**：线性投影层，将声学嵌入映射到解码器的隐藏空间。
        *   **大语言模型解码器**：一个40亿参数的密集Transformer，支持原生32K上下文窗口。
    *   **多令牌预测模块**：在解码器主干的最后一层之上，并联附加5个MTP模块块，每个模块负责预测一个未来的转录令牌。

*   **训练流程 (分阶段后训练)**
    1.  **音频-语言联合预训练**：基于公开的预训练配方，在大量文本和音频数据上进行多阶段训练，建立多模态基础。此阶段为ParaASR提供了强大的初始化。
    2.  **ASR监督微调 (SFT)**：在不包含MTP的情况下进行。
        *   **数据**：混合使用10万小时的人工标注短音频和5万小时通过多系统验证管线生成的长音频伪标签数据，以兼顾短句精度和长文一致性。
        *   **目标**：训练出一个可靠的自回归识别器基础模型。
    3.  **MTP训练 (两阶段优化)**
        *   **第一阶段：冻结分支对齐**。仅优化新增的MTP模块，骨干网络全部冻结。让MTP分支学习预测一个稳定的未来令牌分布，避免干扰已收敛的识别器。
        *   **第二阶段：联合校准**。解冻适配器和LLM解码器，与MTP模块以较低学习率进行联合优化，减少骨干网络与预测分支之间的残差，将MTP校准为可靠的提议机制。

*   **损失函数**
    *   总损失 \( L_t \) 由标准下一令牌预测损失和加权MTP损失组成：
        \[
        L_t = \text{CE}(p_t, x_{t+1}) + \sum_{h=1}^{H} w_h \cdot \text{CE}(p_{t,h}, x_{t+1+h})
        \]
    *   其中，\( H=5 \) 为MTP分支数，\( w_h = \alpha^{h-1} / \sum_{j=1}^H \alpha^{j-1} \) 为指数衰减权重，\( \alpha = 0.9 \)，以反映串行预测中位置越远不确定性越高的特性。

### 3. 实验设计
*   **数据集与场景**
    *   **短音频中文**：AISHELL-1, AISHELL-2, WenetSpeech, FLEURS zh。
    *   **短音频英语**：LibriSpeech, Common Voice v11 en, FLEURS en, VoxPopuli cleaned AA。
    *   **长音频**：LibriSpeech long variants, Earnings22 cleaned AA, 以及一个自建的WenetSpeech long数据集。

*   **对比基准**
    *   论文全面对比了当前主流的、有竞争力的基于LLM的ASR系统：**VibeVoice-ASR**， **FunASR-Nano**, **Doubao-ASR-2603**, **Qwen3-ASR-1.7B**。

*   **评估指标**
    *   **准确性**：中文任务使用**字错误率 (CER)**，英文和长音频任务使用**词错误率 (WER)**。
    *   **效率**：**实时因子 (RTF)**，在单张NVIDIA H800 GPU上，使用100个30秒音频片段进行测试。

### 4. 资源与算力
*   论文提及了ASR SFT和MTP训练的详细超参数，如训练步数（10K步）、批次大小（全局批次为32）、峰值学习率（2e-5）等，并指明推理效率测试是在**单张NVIDIA H800 GPU**上进行的。
*   然而，对于模型预训练阶段（如文中提到的使用120万+文本和音频Tokens的多阶段训练）或在多GPU/多节点上进行的**大规模SFT和MTP训练的总时长、GPU总数或总计算量（如GPU hours / PF-days）**，文中**并未明确说明**。

### 5. 实验数量与充分性
论文进行了多组实验，实验设计较为充分、客观且公平。
*   **主实验**：包含在**12个多样化的中英文短/长音频测试集**上与4个主流模型的系统性比较，覆盖面广。
*   **效率分析**：专门进行了**RTF对比实验**，验证了MTP带来的实际加速效果。
*   **分析与消融实验**：
    1.  **MTP接受率分析**：在WenetSpeech会议上对比了MTP-3、MTP-5、MTP-7三种配置的**逐位置令牌接受率**，以确定最佳的“效率-复杂度”权衡点，并解释了为何选择5个分支。
    2.  **MTP安全性消融**：对比了**加入MTP前后**模型在所有基准上的性能，直接证明了MTP机制几乎不影响识别准确率。
*   **公平性**：比较时统一了硬件环境（除API调用的模型），对于不支持长音频的模型采用了标准VAD分段策略，确保了对比的公平性。

### 6. 论文的主要结论与发现
*   **可行性**：ParaASR的成功证明，ASR中解码器规模、低延迟和长上下文转录并非不可兼得的“不可能三角”。
*   **高效率**：通过MTP，一个40亿参数的解码器可以实现RTF为**0.0053**的极低延迟，远超参数量更小的1.7B模型基线，将解码效率推向新高度。
*   **高准确率**：该加速几乎无损。模型在中文（平均CER 2.97%）、英语（平均WER 3.68%）和长音频（平均WER 3.70%）等多个基准上达到或超越了最优水平。
*   **机制有效性**：MTP在ASR中应用效果显著，平均每步能接受**5.0/6个**提议令牌。声音信号的确定性使得未来令牌预测极为可靠。
*   **安全性**：自回归验证和分阶段训练策略保证了MTP是一个安全的加速原语，错误的提议会被截断，不会影响最终转录质量。

### 7. 优点
*   **创新性的应用**：首次将多令牌预测与ASR的“声学锚定”特性深刻结合，并系统性地论证了其在该领域的天然优势。
*   **完整的技术方案**：从模型架构、训练数据构造（尤其是长音频伪标签的精细处理管线）、分阶段训练策略到推理验证，形成了一整套可复现的高质量方案。
*   **实证坚实**：实验覆盖了中英文、长短音频等多个维度，并且消融实验清晰地剖析了每个设计选择（如分支数量）的影响和MTP的安全性，论证逻辑严密。
*   **工程价值高**：在单卡上实现了商业级速度与长上下文能力，直接解决了LLM-based ASR实际部署中的核心痛点，具有很强的实用性。

### 8. 不足与局限
*   **算力细节缺失**：未披露模型训练的完整计算资源开销，使得其他研究者难以精确评估复现成本。
*   **领域局限性**：论文的核心前提（语音转录的确定性）更适用于朗读式或相对标准的语音。在极度口语化、充满迟疑/修正/不流畅的对话场景中，这种“确定性”可能会减弱，MTP分支的接受率可能因此降低，而论文未对此类场景进行专门测试。
*   **语言覆盖面**：实验主要集中在中英双语，对多语言（特别是低资源语言）的泛化性有待验证。
*   **对比局限性**：虽然对比了多个系统，但部分基线模型可能是快照版本，其性能在时间上可能不代表其最新状态。
*   **延迟报告范围**：RTF测试在单一并发环境下进行，未探讨高并发、动态batch下的推理效率表现，而这在实际部署中同样关键。

（完）
