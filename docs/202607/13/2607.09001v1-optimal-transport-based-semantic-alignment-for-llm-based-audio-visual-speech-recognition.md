---
title: Optimal Transport-based Semantic Alignment for LLM-based Audio-Visual Speech Recognition
title_zh: 基于最优传输的语义对齐用于基于大语言模型的音视频语音识别
authors: "Xugang Lu, Peng Shen, Yu Tsao, Hisashi Kawai"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09001v1"
tags: ["query:speech-model"]
score: 10.0
evidence: 通过最优传输对齐音频、视觉与文本模态，提升多模态语音识别鲁棒性。
tldr: 基于大语言模型的音视频语音识别中，声学、视觉与文本模态间存在表示差异，影响多模态融合效果。本文提出最优传输语义对齐框架，将音视频特征对齐至LLM语言空间，并用OT耦合矩阵监督对比学习，实现语义一致的跨模态表示。在LRS3-TED数据集上，该方法在多种信噪比条件下均取得最优性能，有效提升了识别鲁棒性与准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09001v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1554, \"height\": 929, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09001v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 621, \"height\": 363, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09001v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 771, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 771, \"height\": 248, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 785, \"height\": 137, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 724, \"height\": 271, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 859, \"height\": 314, \"label\": \"Table\"}]"
motivation: 现有LLM-AVSR方法未显式处理音频、视觉与文本模态间的表示差异，限制了多模态集成的有效性。
method: 通过最优传输计算音视频特征与语言嵌入的概率耦合矩阵，并作为伪标签指导对比学习，将多模态表示锚定到LLM语言空间。
result: 在LRS3-TED基准上，于干净和噪声环境中均超越强基线，达到当前最优性能，尤其低信噪比下提升显著。
conclusion: 最优传输语义对齐可有效弥合模态鸿沟，促进LLM-AVSR中多模态信息的协同集成，为鲁棒音视频识别提供了新范式。
---

## 摘要
基于大语言模型的音视频语音识别(LLM-AVSR)最近通过在恶劣声学环境中利用互补的音频和视觉信息，展现出强大的鲁棒性。现有方法通常采用独立预训练的音频和视觉编码器，其输出被投影并融合为软提示，以条件化大语言模型进行语音识别。然而，大多数方法在执行多模态融合时没有明确处理音频、视觉和文本模态之间的表示差异，这可能限制跨模态集成的有效性。本文中，我们为 LLM-AVSR 提出了一种基于最优传输(OT)的语义对齐框架。该方法通过在多模态融合之前，参照大语言模型的语言嵌入空间来对齐声学和视觉表示，从而显式地弥合模态差距。具体而言，利用最优传输来估计概率耦合矩阵，这些矩阵刻画了模态特定特征和语言嵌入之间的结构化对应关系。生成的 OT 耦合进一步用作对比学习的软伪标签，以监督对比学习，鼓励提取语义连贯且跨模态一致的音视频表示。通过将多模态特征锚定到大语言模型的语言空间，所提出的框架有助于更有效的多模态融合和解码。我们使用基于 Whisper 的声学编码器、基于 AV-HuBERT 的视觉编码器和 LLaMA3.2-3B 解码器实现了所提出的框架。在 LRS3-TED 基准上进行的实验表明，相对于强基线取得了持续改进，并在各种信噪比(SNR)的干净和噪声评估条件下达到了最先进的性能。

## Abstract
Large language model (LLM)-based audio-visual speech recognition (LLM-AVSR) has recently demonstrated strong robustness in adverse acoustic environments by leveraging complementary audio and visual information. Existing approaches typically employ independently pretrained acoustic and visual encoders, whose outputs are projected and fused as soft prompts to condition an LLM for speech recognition. However, most methods perform multimodal fusion without explicitly addressing the representational discrepancy between audio, visual and text modalities, potentially limiting the effectiveness of cross-modal integration. In this paper, we propose an optimal transport (OT)-based semantic alignment framework for LLM-AVSR. The proposed method explicitly bridges the modality gap by aligning the acoustic and visual representations with reference to the linguistic embedding space of the LLM before multimodal fusion. Specifically, OT is used to estimate probabilistic coupling matrices that characterize structured correspondences between modality-specific features and linguistic embeddings. The resulting OT couplings are further utilized as soft pseudo-labels to supervise contrastive learning, encouraging the extraction of semantically coherent and cross-modal consistent audio-visual representations. By anchoring multimodal features to the linguistic space of the LLM, the proposed framework facilitates more effective multimodal fusion and decoding. We implement the proposed framework using a Whisper-based acoustic encoder, an AV-HuBERT-based visual encoder, and a LLaMA3.2-3B decoder. Experiments conducted on the LRS3-TED benchmark demonstrate consistent improvements over strong baselines and achieve state-of-the-art performance under both clean and noisy evaluation conditions across a wide range of signal-to-noise ratios (SNRs).

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有基于大语言模型的音视频语音识别（LLM‑AVSR）系统在多模态融合时，普遍忽略音频、视觉与文本三种模态之间的表示差异（representational discrepancy）。这些模态的编码器往往独立预训练，特征空间存在结构性不匹配，简单的投影与拼接很难充分挖掘跨模态互补信息。
- **研究动机**：直接融合未对齐的多模态特征会妨碍 LLM 的语言能力被有效利用，尤其在噪声环境下，视觉模态本身存在歧义（如相同口型对应不同音素），而语音信号又容易受损。因此，迫切需要一种在融合前显式实现语义对齐的机制，将声学与视觉表示锚定到 LLM 的语言空间。
- **整体含义**：论文提出以最优传输（OT）作为桥梁，在特征层面建立音频‑文本、视觉‑文本的软对应关系，从而学习语义连贯、跨模态一致的表征，最终提升 LLM‑AVSR 的识别鲁棒性和准确性。

### 2. 论文提出的方法论
- **总体框架**：引入一个 OT‑based 语义对齐模块，位于模态编码器与 LLM 投影层之间。先通过 OT 计算音视频特征与目标文本 token 嵌入的耦合矩阵，再以此监督对比学习，使多模态特征在进入 LLM 之前就对齐到同一个语言语义空间。
- **关键技术细节**：
  - **编码器选择**：声学编码器用 Whisper（冻结），视觉编码器用 AV‑HuBERT（冻结），LLM 使用 LLaMA3.2‑3B（通过 LoRA 微调）。
  - **特征预处理**：音频与视觉特征分别经线性投影（Proj_a, Proj_v），目标文本嵌入经全连接层（FC）变换为 z_t，得到三组特征序列。
  - **OT 对齐与耦合**：
    - 将三组序列视为经验概率分布，定义音频‑文本和视觉‑文本的熵正则化最优传输问题，使用余弦相似度作为代价，通过 Sinkhorn 算法快速求解概率耦合矩阵 Q^{a→t} 和 Q^{v→t}。
    - 引入“虚拟桶（virtual bucket）”处理非信息性帧（如噪声、静音），允许部分质量不被匹配，从而过滤无效对齐。
  - **对齐损失**：将 OT 耦合矩阵作为软伪标签，监督一个温度缩放后的 softmax 对比学习损失（L_align），促使模型提取与语言 token 分布一致的跨模态特征。
  - **融合与解码**：选择通道拼接（C_Concat）作为主要融合方式（实验表明在噪声训练下最优），融合后经投影层送入 LLM，结合指令嵌入进行自回归转录。
  - **总损失**：L_Total = L_AR（LLM 自回归交叉熵） + α·L_align。

### 3. 实验设计
- **数据集**：LRS3‑TED 大规模音视频演讲数据集。使用官方 433 小时训练集，1200 条验证集，1321 条测试集。
- **评估场景**：
  - 干净语音测试。
  - 多信噪比（‑5 dB, 0 dB, 5 dB）噪声测试，噪声来自 NOISEX 并添加至音频。
  - 训练时采用数据增强（75% 概率添加噪声，SNR 范围 [‑5, 0, 5, 10, 15, 20] dB）。
- **评价指标**：词错误率（WER）。
- **对比方法**：
  - 强基线：Whisper‑Flamingo、LLaMA‑AVSR、MMS‑LLaMA。
  - 消融对象：不同融合策略（C_Concat， Add， Cross‑att， Q‑former， T_Concat）、是否有虚拟桶、对齐损失权重 α 等。

### 4. 资源与算力
- 论文**未明确提及**具体的 GPU 型号、数量或训练时长。
- 仅给出一些训练超参数：Adam优化器，初始学习率 1e‑4，5000 步预热，总共 30k 训练步，LoRA 秩 16、缩放因子 32。缺少算力细节使得复现成本不透明。

### 5. 实验数量与充分性
- 实验设计较为系统，至少包含以下几组：
  - 干净与噪声训练下 5 种融合方式的对比（表 I、II）；
  - 有无虚拟桶的对齐效果（表 III）；
  - 对齐损失权重 α 的调优（表 IV，7 个取值）；
  - 与 3 个前沿方法的性能对比，且给出了自身方法的 3 种超参数设置（表 V）。
- 此外，文中还提到考察了初始相似度边缘 ˜s、OT 熵正则系数 λ、温度参数 τ 的交互影响，但未列表，仅说明其非独立且未完全优化。
- 整体实验**比较充分且公平**：均使用相同数据集和评价标准，包含了必要的消融研究和多组条件对比，能够支撑论文的核心主张。

### 6. 论文的主要结论与发现
- OT 语义对齐框架能够**显式弥合音视频与文本模态之间的表示鸿沟**，在 LRS3‑TED 基准上取得了最先进的性能。
- 相较于无对齐的相同模型（Our method no align），加入对齐后 WER 大幅降低，尤其在**低信噪比**场景下提升更明显（如 SNR‑5 时从 8.34% 降至 7.16%）。
- 虚拟桶、合适的对齐权重 α（0.1~0.3）能进一步提升鲁棒性；通道拼接是在噪声训练下的最优融合策略。
- 结果表明，将多模态特征事先对齐到 LLM 的语言空间，对提升 LLM 作为解码器的利用效率至关重要。

### 7. 优点
- **方法论新颖**：首次将最优传输用于 LLM‑AVSR 的语义对齐，并用 OT 耦合作为对比学习的软监督信号，逻辑自洽。
- **模块化设计**：对齐模块插入即插即用，不修改预训练编码器和 LLM 本体，便于集成到现有框架。
- **强鲁棒性**：在多种 SNR 下均明显超过强基线，证明了方法的实际价值。
- **消融充分**：对融合方式、虚拟桶、对齐系数等关键因素进行了深入分析，结论可靠。

### 8. 不足与局限
- **超参数未系统优化**：λ、τ、˜s 等组合依赖经验选择，作者也承认存在交互、优化困难，可能尚未触及性能上限。
- **计算开销不明**：引入 Sinkhorn 迭代会增加训练复杂度，但缺少对训练速度或内存占用的分析。
- **数据集单一**：仅在 LRS3‑TED 上测试，无法证实在其他场景或语言下的泛化能力。
- **虚拟桶研究较浅**：仅做了有无对比，未探讨其与不平衡 OT 的内在关联。
- **编码器和 LLM 固定**：没有探索不同规模编码器或 LLM 对对齐效果的敏感性，结论可能局限于所选模型组合。

（完）
