---
title: "FdAudio: MeanFlow-Anchored Fréchet-Distance Post-Training for One-Step Text-to-Audio Generation"
title_zh: "FdAudio: 基于MeanFlow锚定的Fréchet距离后训练用于一步式文本到音频生成"
authors: "Kuan-Po Huang, Bo-Ru Lu, Ho-Lam Chung, Shih-Hsin Wang, Hung-yi Lee"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10421v1"
tags: ["query:speech-model"]
score: 9.0
evidence: 后训练方法提升一步式文本到音频生成质量
tldr: "尽管基于平均速度场的单步文本到音频生成模型（如MeanAudio）显著提升了推理效率，但严格单步输出的分布质量仍远逊于多步模型，成为实时高保真应用的阻碍。为此，提出FdAudio，一种在预训练多表示嵌入空间利用Fréchet距离损失直接优化单步分布的后训练方法，并创新地使用MeanFlow一致性目标作为结构锚，以规避朴素FD后训练造成的多步退化灾难。实验表明，相较于MeanAudio，FdAudio的一步生成FD分数降低11.4%，FAD提升28.8%，夺得单步系统最优；而得益于MeanFlow锚点，25步采样恢复高保真，性能媲美甚至超过强多步模型，推理时延仅为后者的几十分之一。这项工作以轻量后训练平衡了生成质量与计算开销，为实时交互式音频合成提供了实用化路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10421v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1861, \"height\": 649, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10421v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1767, \"height\": 786, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10421v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10421v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 837, \"height\": 374, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10421v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 921, \"height\": 343, \"label\": \"Table\"}]"
motivation: 单步文本到音频生成模型（如MeanAudio）虽快，但输出质量远逊于多步模型，亟需提升以支持实时应用。
method: FdAudio后训练通过多表示Fréchet距离损失优化单步分布，并引入MeanFlow一致性目标作为结构锚点防止多步退化。
result: "FdAudio单步FD降低11.4%，FAD提升28.8%，夺单步SOTA；MeanFlow锚点使25步质量匹敌强多步模型且延迟极低。"
conclusion: FdAudio以分布优化与运动一致性锚定的简洁后训练，实现快速高保真音频合成，为实时交互系统提供实用方案。
---

## 摘要
尽管近期如MeanAudio等少步采样的文本到音频生成模型通过建模平均速度显著加速了生成，但它们严格的一步生成质量仍显著落后于多步模型。我们提出FdAudio以弥补这一差距。与单纯依赖目标速度场回归的MeanAudio不同，我们的后训练方法通过多表示Fréchet距离（FD）损失直接在预训练嵌入空间中优化最终的一步分布。关键的是，为防止使用FD损失进行朴素后训练所导致的多步退化，我们引入了一个MeanFlow一致性目标作为结构性锚定。结果表明，FdAudio在少步系统中建立了一流的文本到音频生成质量，相对于基线MeanAudio框架，FD分数降低了11.4%，FAD分数提升了28.8%。值得注意的是，我们通过提出MeanFlow锚定解决了FD后训练的朴素多步退化问题，使得25步采样路径能够保持高保真音频合成，在计算延迟仅为强大多步模型的一小部分的情况下，达到或超越这些模型。

## Abstract
While recent few-step sampling text-to-audio generation models like MeanAudio substantially accelerate generation by modeling average velocities, their strict one-step generation quality still lags significantly behind multi-step counterparts. We propose FdAudio to bridge this gap. Unlike MeanAudio, which relies solely on regression against target velocity fields, our post-training approach optimizes the final one-step distribution directly across pre-trained embedding spaces via a multi-representation Fréchet-distance (FD) loss. Crucially, to prevent the multi-step degradation that naive post-training with FD-loss causes, we introduce a MeanFlow consistency objective as a structural anchor. Results demonstrate that FdAudio establishes state-of-the-art one-step T2A generation quality among few-step systems, yielding an 11.4% reduction in FD score and a 28.8% improvement in FAD score relative to the baseline MeanAudio framework. Notably, we solve FD post-training's naive multi-step degradation issue by proposing the MeanFlow anchor, enabling a 25-step sampling path to maintain high-fidelity audio synthesis that matches or surpasses strong multi-step models at a fraction of their computational latency.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：文本到音频（T2A）生成在游戏、影视等领域有广泛应用，但主流扩散/流匹配模型需要数十步甚至数百步迭代采样，推理延迟极高。近期提出的少步（甚至一步）生成方法（如MeanAudio）虽然大幅提速，但其严格一步生成的分布质量仍显著落后于多步模型，形成“质量‑速度”脱节。
- **核心问题**：能否在保留单步采样效率的前提下，将一步生成质量提升到与强多步模型相当的水平，同时不丢失多步采样的灵活性和高保真度。
- **整体含义**：FdAudio提出一种后训练框架，利用多表示Fréchet距离（FD）损失直接优化最终单步输出分布，并引入MeanFlow一致性目标作为结构锚，既获得一流的一步质量，又防止朴素FD后训练导致的多步退化崩溃，从而在不同采样预算下均实现高竞争力性能。

### 2. 论文提出的方法论
- **核心思想**：将预训练好的少步T2A生成器（以MeanAudio为基座）在冻结编码器特征空间下进行分布级后训练，通过组合多个预训练音频编码器的FD损失对齐生成分布与真实分布；同时加入MeanFlow一致性损失正则化，防止单步优化覆盖掉模型原有的连续速度场，从而保留多步采样的能力。
- **关键技术细节（用文字说明关键公式与流程）**：
  - **多表示FD损失**：设预训练编码器集合\(\{\psi_j\}\)，对真实音频\(A_{gt}\)和生成音频\(A_g\)计算Fréchet距离：
    \[
    FD_{\psi_j}(A_{gt}, A_g) = \|\mu_{gt} - \mu_g\|^2_2 + \operatorname{Tr}\left(\Sigma_{gt} + \Sigma_g - 2(\Sigma_{gt}\Sigma_g)^{1/2}\right)
    \]
    为避免不同编码器数值尺度差异，使用动态归一化求和：
    \[
    \mathcal{L}_{\text{Multi-FD}} = \sum_{j} w_j \frac{FD_{\psi_j}(A_{gt}, A_g)}{\text{sg}(FD_{\psi_j}(A_{gt}, A_g)) + c}
    \]
    采用队列（队列长度\(N=20000\)）缓存最近生成样本的表示，只对当前批次样本的表示进行梯度反传，预计算真实统计量。
  - **MeanFlow锚定**：MeanFlow目标迫使模型学习任意区间\([r, t]\)上的平均速度，其损失为：
    \[
    \mathcal{L}_{\text{MF}} = \left\| u_\theta(z_t, r, t) - \text{sg}\left(\hat{v} - (t - r)\frac{d}{dt}u_\theta\right) \right\|_a^2
    \]
    其中\(\hat{v}\)融合了无分类器引导。该损失作为正则项与多表示FD损失联合优化：
    \[
    \mathcal{L}_{\text{total}} = \mathcal{L}_{\text{Multi-FD}} + \lambda \mathcal{L}_{\text{MF}}
    \]
  - **后训练流程**：每次迭代分别在两个采样轨迹上计算损失：一是单步（\(r=0, t=1\)）生成用于FD损失；二是随机采样中间时刻对\(r<t\sim\mathcal{U}(0,1)\)计算MeanFlow损失。平衡系数\(\lambda=0.25\)。

### 3. 实验设计
- **数据集**：
  - 训练：AudioCaps和WavCaps共387,438条音频，因预算限制随机采样80,000条训练一个epoch。
  - 评估：AudioCaps测试集的10秒片段。
- **对比基线**：
  - **多步模型**：Tango 2、TangoFlux、EzAudio-XL、Make‑an‑Audio 2、AudioLDM2‑L、AudioMNTP、IMPACT、Resonate‑GRPO、MeanAudio‑S‑Full。
  - **少步/一步模型**：ConsistencyTTA、SoundCTM、AudioLCM、AudioTurbo、AudioDEAR、MeanAudio‑S‑Full。
- **评估指标**：分布级指标FD_PANN、FAD；语义相关CLAP分数；事件分布KL散度、Inception Score；主观评测（REL/OVL）9人×90样本；推理延迟（V100 GPU生成8段10秒音频所需时间）。

### 4. 资源与算力
- 文中提到训练使用**NVIDIA V100 32GB RAM**进行推理速度测试，但未明确说明训练使用的GPU型号、数量及总训练时长。只提到“因预算限制仅采样8万条数据训练一个epoch”，未给出具体的算力配置或wall‑clock时间，因此算力细节在论文中不够透明。

### 5. 实验数量与充分性
- **主要实验**：
  - 系统级比较：在AudioCaps上对比多步和少步模型，分1步和25步两种采样方式，涵盖约15个基线系统。
  - 消融实验：
    - MeanFlow锚定权重λ（0.0, 0.25, 0.50, 1.0）在1步和25步下的全指标对比（表III）。
    - 多表示FD损失中编码器组合的消融（PANNs+PaSST，再加BEATs、AudioMAE，三者或四者组合）在1步和25步下的各项指标（表IV）。
  - 主观评测：对MeanAudio和FdAudio在1步和25步下进行REL/OVL打分的用户测试。
- **充分性与公平性**：实验对比了当前主流多步和少步系统，基线覆盖全面。消融实验清晰验证了MeanFlow锚定的关键作用和多编码器组合的收益。主观评价进一步支撑客观结果。但训练仅使用8万子集且仅一个epoch，可能会限制泛化程度的充分展示；训练资源细节缺失影响可复现性。总体设计较为充分、对比公平。

### 6. 论文的主要结论与发现
- FD后训练应用于音频生成可显著提升一步生成分布质量，但会严重破坏多步采样能力（无锚定时25步FAD从1.27骤升至3.61）。
- 引入MeanFlow一致性锚定可解决上述冲突：结合FD损失与MeanFlow正则，模型在一步下取得一流质量，在25步下恢复高保真度，优于同等大小模型并接近甚至超过强多步系统。
- 在少步系统中，FdAudio以一步采样达到FD 12.71、FAD 1.26的最优水平，相对MeanAudio基线FD降低11.4%，FAD提升28.8%；在25步采样下FD为12.56，与强多步模型相当且延迟极低（仅3.21秒生成8条10秒音频）。
- 多表示编码器组合优于单一编码器，能防止分布优化过拟合特定特征空间。

### 7. 优点
- **创新性地解决一步与多步采样的权衡**：首次在音频生成中揭示分布级后训练破坏速度场的问题，并用MeanFlow一致性巧妙锚定，实现单一权重模型在不同采样步数下均高质量生成。
- **方法简洁直接**：无需教师蒸馏、对抗训练或逐样本回归目标，仅利用队列估计与少量正则项即可将现有少步模型提升至最优。
- **效果提升显著**：在少步系统中全面最优，且主观评测一致性改善。
- **实验丰富**：包括多指标、多基线、多步数对比及消融，验证充分。

### 8. 不足与局限
- **训练数据与计算效率描述不详**：仅使用8万子集一个epoch，且未报告具体训练耗时和GPU数量，影响对训练成本和可复现性的判断。
- **泛化到其他架构尚待验证**：实验基于单一基座模型（MeanAudio‑S‑Full），未在其他少步框架（如一致性模型）上验证方法的通用性。
- **评估仍受限**：分布指标（FD、FAD）虽主流但与主观感知存在差距，且主观评测规模较小（9人，90样本）。
- **可能的重合风险**：多表示FD损失所用编码器与评估所用特征可能存在间接重叠，虽有VGGish的隔离，但其他编码器仍可能引入轻微偏差。
- **超参敏感度**：λ=0.25是平衡点，更大或更小的权重均会导致部分指标下降，对超参数选择存在一定依赖。

（完）
