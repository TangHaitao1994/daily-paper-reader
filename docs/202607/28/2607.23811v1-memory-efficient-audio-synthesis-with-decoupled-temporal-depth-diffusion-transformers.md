---
title: Memory Efficient Audio Synthesis with Decoupled Temporal Depth Diffusion Transformers
title_zh: 基于解耦时间深度扩散Transformer的记忆高效音频合成
authors: "Dongseong Hwang, Prasanth Yadla, Kaan Elgin, Shifas Padinjaru Veettil, Sivanand Achanta, Dipjyoti Paul, Ramya Rasipuram, Tyler Johnson, Emad Soroush, Chung-Cheng Chiu, Zhifeng Chen"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23811v1"
tags: ["query:speech-model"]
score: 9.0
evidence: 用于Siri的设备端内存高效音频合成，解耦时间与深度处理
tldr: 在设备端实时合成高保真语音面临内存与计算挑战，现有方法内存随序列长度线性或二次增长。本文提出解耦时间与深度处理的可重用扩散变压器，通过单个深度解码器自回归生成所有RVQ级别，并采用因果滑动窗口注意力实现恒定内存复杂度。在AMX上每步约10ms（16倍实时），峰值运行时内存仅21MB，支持20至320秒流式合成。该架构应用于AFM 3 Core Advanced，将语音合成MOS提升0.28至4.15，尤其对话语音提升0.42，显著增强设备端语音表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23811v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1732, \"height\": 585, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23811v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 785, \"height\": 883, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23811v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 796, \"height\": 872, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23811v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 827, \"height\": 912, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23811v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 788, \"height\": 881, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23811v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 799, \"height\": 877, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23811v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 790, \"height\": 872, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23811v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1077, \"height\": 286, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23811v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1756, \"height\": 572, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23811v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1353, \"height\": 326, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23811v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1718, \"height\": 309, \"label\": \"Table\"}]"
motivation: 设备端语音合成需在极有限内存和计算下实现高保真流式输出，传统Transformer和GAN方法内存随序列线性或二次增长，无法满足。
method: 设计解耦时间解码器与深度解码器，可重用深度解码器以扩散变压器阶段条件自回归生成所有RVQ级，并用因果固定窗口键值缓存实现常数内存。
result: 在AMX上每生成步约10ms，峰值内存21MB，资产329MB，支持20-320秒音频流式合成，MOS提升至4.15（+0.28），对话语音达4.24（+0.42）。
conclusion: 该内存高效架构以恒定小内存实现高质量流式语音合成，显著优于前代系统，验证了解耦设计与窗口注意力的有效性，适用于大模型设备端部署。
---

## 摘要
Siri Expressive Voices 依托 AFM 3 Core Advanced（Apple 最强大的端侧基础模型），实现了完全在设备端实时合成丰富、可配置的语音。本文介绍了支持该能力的记忆高效音频合成架构：一个解标记器（detokenizer），它在 Apple 矩阵协处理器（AMX）有限的计算和内存预算内，将基础模型输出的语义音频标记转换为高保真音频。我们采用三组件设计——流式编码器、时间解码器和深度解码器——将语义音频标记转换为残差矢量量化（RVQ）表示，系统性地解耦了时间和深度处理。一个可复用的深度解码器，采用扩散 Transformer（DiT）风格的阶段条件控制，以自回归方式生成所有 RVQ 层级，取代了之前多解码器架构中每层专用的解码器；同时，带有固定窗口键值缓存的因果滑动窗口注意力机制，使内存复杂度与序列长度无关，保持恒定。该解标记器部署在 AMX 上，每生成步骤仅需约 10 毫秒，比实时快约 16 倍，运行时峰值内存仅 21 MB，设备端资源占用 329 MB，能够连续流式合成 20 至 320 秒的音频。这种恒定的小内存占用，取代了传统 Transformer 和 GAN 方法随序列长度线性或二次增长的内存消耗。消融研究验证了关键架构组件的有效性，音频质量评估证实该架构在保持合成保真度的同时，相比现有方法实现了效率提升。在 AFM 3 Core Advanced 中激活参数规模达 10 亿时，与之前的端侧文本到语音系统相比，整体平均意见得分提升 0.28（4.15 vs. 3.87），在对话语音上提升 0.42（4.24 vs. 3.82）。

## Abstract
Siri Expressive Voices synthesize rich, configurable speech in real time and entirely on device, powered by AFM 3 Core Advanced, Apple's most powerful on-device foundation model. This work presents the memory-efficient audio synthesis architecture behind that capability: a detokenizer that converts the semantic audio tokens emitted by the foundation model into high-fidelity audio within the tight compute and memory budget of the Apple Matrix Coprocessor (AMX). We convert semantic audio tokens to a residual vector quantization (RVQ) representation with a three-component design, a streaming encoder, a temporal decoder, and a depth decoder, that systematically decouples temporal and depth processing. A single reusable depth decoder with Diffusion Transformer (DiT)-style stage conditioning generates all RVQ levels autoregressively, replacing the dedicated per-level decoders of prior multi-decoder architectures, while causal sliding window attention with fixed-window key-value caching yields constant memory complexity independent of sequence length. Deployed on the AMX, the detokenizer sustains roughly 10 ms per generation step, about 16x faster than real time, with a peak runtime memory of only 21 MB and 329 MB of on-device assets, enabling continuous streaming synthesis of 20-320 seconds of audio. This constant, small footprint replaces the linear and quadratic memory scaling of conventional transformer- and GAN-based approaches. Ablation studies validate the key architectural components, and audio quality assessment confirms that the architecture maintains synthesis fidelity while achieving efficiency gains over existing methods. Operating at a 1-billion-parameter activation size within AFM 3 Core Advanced, it improves Mean Opinion Score by +0.28 overall (4.15 vs. 3.87) and by +0.42 on conversational speech (4.24 vs. 3.82) over the prior on-device text-to-speech system.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对您提供的论文进行结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：在资源极其受限的端侧设备（如手机、平板）上，如何将大模型生成的语义音频标记实时、高保真地合成为波形音频。
*   **研究背景与动机**：
    *   Apple 的 Siri Expressive Voices 功能完全在设备端运行，其背后的基础模型 AFM 3 Core Advanced 采用稀疏激活机制，虽然总参数量巨大（200亿），但每次生成仅在 DRAM 中加载 10-40 亿激活参数。这使得音频合成模块必须在与主模型共享的、极其有限的内存和算力预算下工作。
    *   传统的神经音频合成方法（如纯 Transformer 解码器、自回归 GAN）存在内存占用随生成序列长度线性或二次增长的问题，无法满足长时流式音频合成（如 20-320 秒）的需求。
    *   现有的最新方法（如 Moshi）虽然通过共享深度 Transformer 减少了参数量，但仍保留了每层独立的输入/输出投影层，参数量依然随量化层级数增长，未能实现彻底的参数量共享与内存恒定。

### 2. 论文提出的方法论

*   **核心思想**：设计一个三组件架构（流式编码器、时间解码器、深度解码器），系统性地解耦音频生成中的时间维度和深度（RVQ层级）维度处理，并通过一个参数完全共享的深度解码器以及固定窗口键值缓存，实现与序列长度无关的恒定内存消耗。
*   **关键技术与工作流程**：
    1.  **流式编码器 (Streaming Encoder, \(f_\theta\))**：
        *   采用因果滑动窗口注意力（Causal Sliding Window Attention）处理输入的语义标记 \(x_t\)。
        *   输出层次化的条件隐变量 \(h_t\)，用于捕捉时序和语义特征。
        *   **亮点**：引入“时序前瞻”（Temporal Lookahead）机制，允许编码器访问未来的语义标记信息（实验表明 6 个标记最优），以提升生成质量。
    2.  **时间解码器 (Temporal Decoder, \(g_{\phi_T}\))**：
        *   基于 Diffusion Transformer (DiT) 架构，结合自适应层归一化 (adaLN) 和因果滑动窗口注意力。
        *   输入为编码器的输出 \(h_t\) 以及上一时间步生成的所有 RVQ 层级嵌入的均值 \(\bar{q}_t\)。
        *   输出一个条件隐变量 \(y_t\)，作为深度解码器的指导信息。
    3.  **深度解码器 (Depth Decoder, \(g_{\phi_D}\))**：
        *   这是本文最核心的创新点。它是一个**参数完全共享的、可复用的** DiT 块。
        *   **工作流程**：对于每个时间步 \(t\)，它自回归地生成 \(K\) 个 RVQ 层级。
        *   **阶段条件控制 (Stage Conditioning)**：为了区分不同的 RVQ 层级，它将时间解码器的输出 \(y_t\) 与代表层级索引 \(k\) 的旋转位置编码 (RoPE) 结合，形成 \(y_{t,k}\)。这替代了 Moshi 中为每个层级设置独立输入/输出投影层的做法，实现了真正的参数统一。
        *   **自回归生成**：生成过程从可学习的起始标记 (BOS) 开始。每次迭代生成当前层的 RVQ 标记 \(r_{t,k}\) 及其嵌入 \(q_{t,k}\)，该嵌入被用作下一层（\(k+1\)）的输入。
    4.  **固定窗口键值缓存 (Fixed-Window KV Cache)**：
        *   在时间解码器和深度解码器的注意力计算中，仅维护一个固定大小的滑动窗口的键值对。这使得推理时的**内存占用与总序列长度完全无关，保持恒定**，这是实现超长音频流式合成的关键。

*   **算法流程简述**：
    1.  编码器处理语义标记 \(x_t\) 及前瞻标记，得到 \(h_t\)。
    2.  时间解码器处理 \(h_t\) 和上一步的平均嵌入 \(\bar{q}_{t-1}\)，得到 \(y_t\)。
    3.  对于 \(k = 1\) 到 \(K\)：
        *   将 \(y_t\) 与 RoPE(\(k\)) 结合，得到 \(y_{t,k}\)。
        *   深度解码器处理 \(y_{t,k}\) 和上一步的嵌入 \(q_{t,k-1}\)（首次为BOS），生成 \(r_{t,k}\) 和 \(q_{t,k}\)。
    4.  计算当前时间步所有 \(q_{t,k}\) 的均值 \(\bar{q}_t\)，作为下一时间步时间解码器的输入。

### 3. 实验设计

*   **数据集与场景**：
    *   **预训练**：使用一个未公开的、大规模的内部专有音频语料库进行训练，该语料库包含大量内部录制的语音助手交互数据。
    *   **评估场景**：针对 Siri、Conversational、Navigation 三种生产环境下的语音进行评测。
*   **基准 (Benchmark) 与评估指标**：
    *   **对比方法**：
        1.  **Moshi/Mimi 解码器**：作为最先进 (SOTA) 的基线。
        2.  **Transformer 解码器**：一个内部的纯 Transformer 基线。
        3.  **自回归 GAN**：另一个内部基线，类似于自回归 GAN 方法。
    *   **评估指标**：
        *   **计算效率**：生成时间、内存占用 (GB)、实时率 (RTF)。
        *   **重建质量**：评估损失 (Eval Loss)、RVQ 标记预测的 top-1 准确率。
        *   **音频质量**：音素区分度 (ABX 错误率)、感知质量 (VisQOL)、神经质量评估 (MOSNet、UTMOS)、尺度不变信噪比 (SI-SNR)。
        *   **人工评估**：众包平均意见得分 (MOS)。

### 4. 资源与算力

*   **设备端部署 (AMX)**：
    *   推理延迟：每生成步约 `10 ms`（生成 160ms 音频）。
    *   吞吐量：`16x 实时`。
    *   峰值运行时内存：`~21 MB`。
    *   设备端资源：`329 MB`。
*   **算法对比实验 (GPU)**：
    *   **平台与目的**：为在统一平台上进行公平对比，明确提到在**单个 NVIDIA H100 GPU** 上运行了与基线的横向扩缩实验（Table 2）。
    *   **算力详情缺失**：论文**未明确说明**用于大规模预训练的 GPU 型号、数量或训练总时长。这是该研究的核心算力黑箱。

### 5. 实验数量与充分性

*   **消融实验 (Ablation Studies)**：
    *   通过验证集损失和准确率，对多个关键组件进行了消融分析（Table 3, Figure 2-4），包括：
        1.  时序前瞻（有无及不同长度）
        2.  统一的深度解码器设计
        3.  DiT 条件控制机制（与交叉注意力对比）
        4.  输出归一化
        5.  固定窗口 KV 缓存
        6.  滑动窗口大小
        7.  编码器/时序/深度解码器的层数配比
*   **充分性与公平性评估**：
    *   **充分性**：消融实验设计相当详尽，系统地验证了论文提出的每一个关键架构选择的必要性，图表清晰，支撑了最终的架构设计。
    *   **客观性与公平性**：在与外部方法对比时（Moshi），明确指出架构存在差异（时间前瞻长度、目标采样率、码率等），但仍在相同条件下进行了音质评估，保持了相对的客观性。Table 2 在统一 GPU 上进行的扩缩性对比也体现了对比的公平性。然而，由于训练数据为专有语料，复现其与内部基线的对比存在困难。

### 6. 论文的主要结论与发现

*   提出的三组件、参数完全共享的深度解码器架构，成功**将运行时的峰值内存占用降低至约 21 MB 的恒定水平**，与序列长度无关，解决了传统方法的线性/二次内存扩展问题。
*   该架构在 AMX 上实现了 **16 倍实时的合成速度**，使其能够在设备端与大型基础模型并行运行。
*   **DiT 风格的阶段条件控制**在效果上与交叉注意力持平，但训练更快（1.36x），参数效率更高，是更优的选择。
*   **时序前瞻**对合成质量至关重要，最佳前瞻窗口为 6 个标记（约 240ms）。
*   **最终生产部署结果**：在 AFM 3 Core Advanced（10亿激活参数）中，Siri Expressive Voices 的整体 MOS 得分达到 **4.15**，较上一代系统提升 **0.28** 分，并在对话语音上取得了 **0.42** 分的显著提升。

### 7. 优点

*   **创新的架构设计**：通过“解耦-统一”的设计哲学，用单个共享参数的 DiT 深度解码器优雅地解决了 RVQ 多层级生成的参数膨胀问题，是该论文最大的亮点。
*   **极致的工程优化**：固定窗口 KV 缓存机制带来了 O(1) 的内存复杂度，是实际生产环境中处理不定长、超长音频的关键使能技术。
*   **理论联系实际**：论文不仅有详尽的算法消融验证，更有在真实商业产品（Siri）中部署的性能数据和显著提升的主观评测结果，实践价值极高。
*   **系统性的消融研究**：对模型的各个组件和超参数都进行了充分的分析和验证，论证过程严谨。

### 8. 不足与局限

*   **训练数据和算力不透明**：未公开预训练数据集的具体规模和组成，也未说明训练所需的 GPU 时数，这使得外界难以完全评估模型的复现成本和泛化能力的上限。
*   **与 SOTA 的对比不完整**：虽然与 Moshi 在音质上进行了对比，但在计算效率的核心对标上（Table 2），对比的基线是内部的 Transformer 和 GAN，而非 Moshi。缺少 Moshi 在相同 GPU/任务下的内存和速度数据，使“超越 Moshi”的效率优势断言略显不完整。
*   **评估的局限性**：音频质量评估（Table 4）是基于研究配置，而非已部署的生产配置。虽然论文明确指出了这一点，但两部分结果的差异让读者难以将性能评估与最终产品体验完全对应起来。
*   **“语义标记”依赖**：整个系统依赖于一个强大的上游基础模型提供的“语义音频标记”，论文并未探讨该标记本身的质量或压缩率对最终合成效果的上限影响。

（完）
