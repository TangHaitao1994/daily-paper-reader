---
title: "Listen, Think, Transcribe: Continuous Latent Test-Time Scaling for ASR"
title_zh: 听，思考，转写：连续潜在测试时扩展用于自动语音识别
authors: "Ho Lam Chung, Yiming Chen, Dau-Cheng Lyu, Hsiao-Tsung Hung, Hung-yi Lee"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05051v1"
tags: ["query:speech-model"]
score: 9.0
evidence: 潜在测试时扩展方法通过迭代细化直接提升ASR准确率
tldr: "端到端ASR模型单次解码缺乏重审难例的能力，传统微调在小数据下会退步。LatentASR冻结骨干，添加潜在适配器与价值头，以极少量训练数据激活测试时迭代优化和动态停止。在FLEURS和VoxPopuli上WER分别相对降低2.54%和0.47%，重口音与代码切换场景CER降低16%，多语言泛化均匀。该方法用约4M参数将固定计算转换为输入依赖的自适应计算，无需破坏原模型。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05051v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1614, \"height\": 942, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05051v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 830, \"height\": 455, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05051v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 833, \"height\": 182, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 829, \"height\": 160, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 833, \"height\": 108, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 832, \"height\": 165, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 834, \"height\": 193, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 830, \"height\": 174, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 824, \"height\": 203, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 831, \"height\": 179, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 829, \"height\": 186, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 833, \"height\": 238, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 829, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 829, \"height\": 123, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 831, \"height\": 447, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05051v1/table-014.webp\", \"caption\": \"\", \"page\": 0, \"index\": 14, \"width\": 829, \"height\": 132, \"label\": \"Table\"}]"
motivation: 端到端ASR单遍转录无法回溯困难输入，而标准自适应方法在极小数据下会损害模型性能，需要可控的测试时缩放能力。
method: 冻结Qwen3-ASR-0.6B，引入潜在适配器迭代更新少数前缀和预测是否需要额外计算的价值头，仅用500条多样话语训练约4M参数。
result: "LatentASR在FLEURS和VoxPopuli上相对WER降低2.54%与0.47%，ASCEND数据上相对CER降低16.0%，多语言泛化一致，动态停止节省近半计算。"
conclusion: 通过精心选择激活集，在冻结ASR模型内成功开启测试时缩放，将固定计算转为输入自适应分配，为模型注入推理时修正力而免受破坏。
---

## 摘要
端到端ASR模型在单次传递中进行转写，不给解码器留下回顾困难输入的机会。我们提出了LatentASR，一种参数高效的方法，为冻结的ASR骨干网络添加连续潜在测试时扩展。两个小型可训练模块驱动它：一个潜在适配器，通过有界且稳定的更新迭代地细化少数潜在前缀位置；以及一个价值头，预测额外计算是否有帮助并提前终止循环。Qwen3-ASR-0.6B骨干网络保持完全冻结，我们只训练约400万个额外参数。我们使用一个特意选择的小型、多样化的500条语音训练集来激活该循环。在这种最小数据体制下，标准的适应方法均出现性能退化：完全微调、LoRA和提示微调都导致WER增加。LatentASR是唯一在干净基准测试上降低WER的测试方法（FLEURS相对降低2.54%，VoxPopuli相对降低0.47%）。这些降低集中在本质上困难的输入上。在带口音和语码转换语音（ASCEND）上，LatentASR实现了16.0%的相对CER降低。在30种FLEURS语言（23,049条语音）上，多语言WER在不同资源层级上一致降低，证实了该适配器的泛化能力且没有过拟合。动态停止在仅需部分计算量的情况下保留了大部分干净集上的降低效果，在入口门处跳过了约一半的语音。我们的结果表明，一个小的、精心选择的激活集可以在冻结的ASR模型中开启测试时扩展，而不会破坏模型本身，将固定的每条语音计算量转化为依赖输入的计算量，用于最需要的地方。

## Abstract
End-to-end ASR models transcribe in a single pass, leaving no room for the decoder to revisit hard inputs. We propose LatentASR, a parameter-efficient method that adds continuous latent test-time scaling to a frozen ASR backbone. Two small trainable modules drive it: a Latent Adapter that iteratively refines a few latent prefix positions through bounded, stabilized updates, and a Value Head that predicts whether extra computation will help and halts the loop early. The Qwen3-ASR-0.6B backbone stays fully frozen, and we train only ~4M extra parameters. We activate this loop with a deliberately small, diverse 500-utterance training set. Under this minimal-data regime, standard adaptation methods all regress: full fine-tuning, LoRA, and prompt tuning each increase WER. LatentASR is the only tested method that reduces WER on both clean benchmarks (FLEURS -2.54% and VoxPopuli -0.47% relative). The reductions are concentrated on intrinsically hard inputs. On accented and code-switched speech (ASCEND), LatentASR achieves a 16.0% relative CER reduction. Across 30 FLEURS languages (23,049 utterances), the multilingual WER decreases uniformly across resource tiers, confirming that the adapter generalizes without overfitting. Dynamic halting preserves most of the clean-set reduction at a fraction of the compute, skipping roughly half of all utterances at the entry gate. Our results show that a small, carefully chosen activation set can switch on test-time scaling inside a frozen ASR model without corrupting the model itself, converting fixed per-utterance compute into input-dependent compute where it is most needed.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：端到端语音识别（ASR）模型在单次前向传递中直接输出文本，无法像人类一样对困难的声学片段进行“再次思考”或回溯修正。传统的测试时扩展（如链式思维）依赖文本中间推理，而将连续潜在状态反馈回冻结模型会导致表征崩溃。
- **整体含义**：本文探索**在不修改预训练ASR骨干网络的前提下，以极低的参数和数据成本，为模型注入可智能调度的“深思”能力**，将固定的每条语音计算量转化为按需分配的自适应计算。

### 2. 论文提出的方法论
- **核心思想**：冻结已有的ASR骨干（Qwen3-ASR-0.6B），插入两个小型可训练模块——**潜在适配器**和**价值头**，仅通过少量训练数据“激活”潜在空间的测试时迭代优化能力。
- **关键技术细节**：
    - **潜在前缀优化**：在解码器输入中插入 $N$ 个连续潜在位置（不输出任何文本），适配器在第 $k$ 步根据解码器隐状态产生一个有界更新 $\delta_k$，并通过加法残差注入到固定的锚点嵌入 $e_{\text{LT}}$ 上，构成 $x_k = e_{\text{LT}} + g_k \cdot \delta_k$。
    - **三稳定机制**：
        - *有界增量*：对 $\delta_k$ 进行 L2 归一化，并乘以可学习的步幅尺度 $s_k$，防止单步更新过大。
        - *Sigmoid 门控*：学一个门 $g_k$ 来调节更新的应用比例，极端时可完全关闭（$g_k=0$），退化为锚点嵌入。
        - *固定嵌入锚点*：始终围绕词汇表中一个真实 token 的嵌入进行微调，确保输入始终保持在解码器可理解的流形附近。
    - **价值头与动态停止**：一个小型线性层（Value Head）根据当前潜在状态预测该话语的额外计算“效用”（tanh 压到 $[-1,1]$）。推理时先判断入口门 $v_0$，若低于阈值 $\theta$ 则完全跳过潜在循环；否则逐步计算并持续检查，一旦 $v_k < \theta$ 即提前停止。
    - **训练损失**：包括交叉熵（掩蔽潜在位置）、循环轨迹正则化（使潜在状态平滑变化）、价值头均方误差回归，并加入输入噪声和潜在循环随机丢弃以增强鲁棒性。
- **参数规模**：仅增加约 **4M** 可训练参数（骨干的约 0.67%）。

### 3. 实验设计
- **数据集与场景**：
    - **训练集**：从 7 个语料库（Common Voice, FLEURS, VoxPopuli, LibriSpeech, GigaSpeech, The People’s Speech, ASCEND）精心混合的 **500 条**多语言、多口音语音，刻意保持多样性和小规模。
    - **测试集**：
        - 干净语音：FLEURS (en_us) 647 条，VoxPopuli (en) 1842 条。
        - 多语言泛化：FLEURS 30 种语言共 23049 条。
        - 噪声鲁棒性：对 FLEURS 和 VoxPopuli 加性高斯噪声至 SNR=0dB，另在 6 个语料库的噪声版本上测试。
        - 口音/语码转换：ASCEND（重口音中英混合）。
- **对比方法**：冻结骨干（Baseline）、全参数微调、LoRA (rank=16)、提示微调（4 个软 token），均在相同的 500 条数据下训练。
- **评估指标**：词错误率（WER）、字符错误率（CER）及其相对变化（$\Delta$），并分析动态停止的步数分布、难度分箱下的效果、价值头决策质量等。

### 4. 资源与算力
- **计算资源**：使用 **一张 NVIDIA RTX 5090 GPU**。
- **训练设置**：有效批次大小 16，训练 10 个 epoch，优化器 AdamW。未明确报告具体训练时长，但强调参数效率极高（~4M 可训参数），训练成本应较低。

### 5. 实验数量与充分性
- **实验组数丰富**：
    - 主对比实验（1 组，包含 4 种基线）。
    - 多语言泛化（1 组，30 种语言）。
    - 鲁棒性测试（噪声、口音等至少 3 类场景）。
    - 消融实验：3 种稳定机制分别去除、损失项去除 4 种、潜在预算 $N$ 扫描（1,2,4,8）、激活集大小扫描（100~800 共 8 个点）、阈值 $\theta$ 扫描（帕累托曲线）、难度分箱分析、价值头反事实决策分析、细化路径诊断。
- **充分性与客观性**：实验设计周到，对比基线全面，消融覆盖关键组件，反事实检查验证了门控决策的正确性，各结论均有数据支撑。所有方法在相同极小数据体制下训练比较，客观公平。尤其凸显了 LatentASR 在标准适应方法全面退化的前提下依然有效的独特性。

### 6. 论文的主要结论与发现
- **极小数据激活**：仅用 500 条多样话语即可在冻结骨干上成功激活潜在测试时扩展，避免了全量微调或 LoRA 等传统方法带来的灾难性遗忘和 WER 上升。
- **主要性能**：在干净集上，LatentASR 是**唯一**降低 WER 的方法（FLEURS 相对降低 2.54%，VoxPopuli 降低 0.47%）。在困难场景提升显著：ASCEND 口音/语码转换上 CER 相对降低 **16.0%**，强噪声 (0dB) 下亦有降低。
- **自适应性**：价值头实现了输入依赖的算力调度——约一半干净语音被入口门跳过；而困难语音（ASCEND）有 34.1% 用满全部 4 步迭代，算力分配与难度正相关。
- **泛化性**：在 30 种语言的 FLEURS 上 WER 一致下降，证明适配器未过拟合到单一语言或数据集。

### 7. 优点
- **创新性强**：首次将连续潜在测试时扩展成功应用于冻结的 ASR 骨干，且证明了在“最小数据激活”框架下的有效性。
- **设计精巧**：三稳定机制（有界增量、门控、锚点）分别解决了表征崩溃的不同子问题，消融实验证实缺一不可，理论扎实。
- **实用性强**：参数增量极小（4M），不修改原模型，天然保留原有泛化能力，且动态停止可灵活平衡精度与算力。
- **实验充分**：多维度对比、反事实分析、帕累托前沿等评估手段，清晰展示了方法的适用边界和内部工作原理。

### 8. 不足与局限
- **绝对增益微小**：在干净测试集上 WER 绝对降幅仅 0.12~0.04 个百分点，主要价值体现在极端困难样本，对整体指标提升有限。
- **数据敏感**：激活集大小和组成对结果影响很大（400/600/800 条可能退化），方法的鲁棒性对训练数据的选择依赖较强，实际部署需仔细调参。
- **场景局限性**：所有实验基于单一骨干模型 Qwen3-ASR-0.6B，迁移到其他架构或更大模型的效果未知。
- **价值头校准需额外机制**：为防止训练后期所有样本皆为正效用，引入了负采样（$p_{\text{neg}}=0.3$），这引入了新的超参数，且其泛化性未深入探讨。
- **未提供实际吞吐量/延迟数据**：虽报告了平均步数节省，但缺少与传统方法在推理延迟上的定量对比，影响对实用效率的判断。

（完）
