---
title: Efficient Audio-Visual Event Recognition via Knowledge Distillation and Dynamic INT8 Quantization of a Hybrid Cross-Attention Network
title_zh: 通过知识蒸馏和混合交叉注意力网络动态INT8量化实现高效视听事件识别
authors: "Parinaz Binandeh Dehaghani, Danilo Pena, A. Pedro Aguiar"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16980v1"
tags: ["query:speech-model"]
score: 10.0
evidence: 通过知识蒸馏和量化实现高效音视频事件识别
tldr: "针对音视频事件识别中Transformer模型计算复杂度高、难以边缘部署的问题，提出结合知识蒸馏与动态INT8量化的压缩框架。该框架通过缩减隐藏维度与注意力头数构建轻量学生模型，并利用教师模型进行知识迁移，最后量化。在AVE数据集上，融合模块参数减少59.06%，精度仅下降2.14%，模型大小压缩至2.04MB。实验表明该方法在精度与效率间取得良好平衡，为边缘AI部署提供了可行方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16980v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1270, \"height\": 847, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16980v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1227, \"height\": 758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16980v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1225, \"height\": 756, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16980v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1240, \"height\": 1167, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16980v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1505, \"height\": 765, \"label\": \"Table\"}]"
motivation: 大型音视频事件识别模型因计算复杂度和高内存需求，难以在资源受限的边缘设备上部署。
method: 通过缩减隐藏维度与注意力头数构建轻量学生模型，利用教师模型进行知识蒸馏，最后应用动态INT8量化。
result: "在AVE数据集上，融合模块参数减少59.06%，精度仅下降2.14%，量化后模型大小从10.71MB压缩至2.04MB。"
conclusion: 所提压缩框架实现了识别精度与计算效率的有效权衡，适合边缘AI平台。
---

## 摘要
视听事件识别（AVER）通过基于Transformer的多模态架构取得了显著的性能提升。然而，这些模型的高计算复杂度、大内存占用和推理成本阻碍了它们在边缘和资源受限设备上的部署。本文提出了一种高效的压缩框架，用于基于混合交叉注意力的视听事件识别，结合了架构模型压缩、知识蒸馏和动态INT8量化。一个高容量教师模型集成了用于视觉表示学习的VideoMAE、用于音频特征提取的音频频谱图Transformer（AST）以及用于多模态特征融合的混合交叉注意力融合网络。通过减少隐藏特征维度、注意力头数和前馈网络大小，同时保留整体网络架构，构建了一个轻量级学生模型。该学生模型使用知识蒸馏进行训练，以有效地从教师模型转移判别性知识。最后，应用动态INT8训练后量化进一步减少模型大小，以实现高效部署。在视听事件（AVE）数据集上的实验结果表明，所提出的框架将多模态融合模块的可训练参数数量减少了59.06%，与教师模型相比分类准确率仅下降2.14%。此外，动态INT8量化将模型大小从10.71 MB减少到2.04 MB，同时保持有竞争力的识别性能。这些结果表明，所提出的框架在识别准确率和计算效率之间提供了有效的权衡，使其成为在资源受限的边缘AI平台上部署的有前景的解决方案。

## Abstract
Audio-visual event recognition (AVER) has achieved significant performance improvements through transformer-based multimodal architectures. However, the high computational complexity, large memory footprint, and inference cost of these models hinder their deployment on edge and resource-constrained devices. This paper presents an efficient compression framework for hybrid cross-attention-based audiovisual event recognition by combining architectural model compression, knowledge distillation, and dynamic INT8 quantization. A high-capacity teacher model integrates VideoMAE for visual representation learning, the Audio Spectrogram Transformer (AST) for audio feature extraction, and a hybrid cross-attention fusion network for multimodal feature integration. A lightweight student model is constructed by reducing the hidden feature dimension, the number of attention heads, and the feedforward network size while preserving the overall network architecture. The student model is trained using knowledge distillation to effectively transfer discriminative knowledge from the teacher. Finally, dynamic INT8 post-training quantization is applied to further reduce the model size for efficient deployment. Experimental results on the Audio-Visual Event (AVE) dataset show that the proposed framework reduces the number of trainable parameters in the multimodal fusion module by 59.06%, with only a 2.14% decrease in classification accuracy compared with the teacher model. Furthermore, dynamic INT8 quantization reduces the model size from 10.71 MB to 2.04 MB while maintaining competitive recognition performance. These results demonstrate that the proposed framework provides an effective trade-off between recognition accuracy and computational efficiency, making it a promising solution for deployment on resource-constrained edge AI platforms.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前的视听事件识别（AVER）虽受益于 Transformer 等多模态架构，但计算复杂度高、内存占用大、推理成本昂贵，难以部署在边缘设备和资源受限平台（如自主机器人、可穿戴设备、智能交通系统）。
- **研究动机**：已有工作多聚焦于设计更复杂的融合架构，鲜有针对混合交叉注意力的多模态 Transformer 的轻量化压缩策略。现有压缩方法多为通用视觉/语言模型的后处理，未考虑多模态交互机制的特殊性。
- **整体含义**：本文旨在提出一种架构感知的压缩框架，在保留教师模型多模态推理能力的前提下，显著减少可训练参数和模型存储，为边缘 AI 应用提供高效、可部署的 AVER 方案。

### 2. 论文提出的方法论
- **核心思想**：结合三种压缩技术形成统一管线：架构压缩 → 知识蒸馏 → 动态 INT8 量化。仅压缩可训练的多模态融合与分类网络，保持预训练的 VideoMAE 和 AST 特征提取器冻结不变。
- **教师模型结构**：
  - 视觉特征：预训练 VideoMAE 提取视频段时空表示。
  - 音频特征：预训练 AST 将对数梅尔谱图转换为声学嵌入。
  - 融合模块：双向交叉注意力 + 模态时序注意力（Modality Temporal Attention），采用接近零初始化的残差缩放以稳定初期训练。
- **学生模型构建（架构压缩）**：
  - 隐藏特征维度：512 → 256
  - 注意力头数：8 → 4
  - 前馈网络维度：1024 → 512
  - 保留原有网络拓扑和单个 Transformer 细化层。
- **知识蒸馏**：
  - 教师参数冻结，仅优化学生。
  - 损失函数：`L = (1 - α)L_CE + αT² * L_KD`，其中 `L_CE` 为交叉熵损失，`L_KD` 为蒸馏损失（KL 散度），温度 `T=4`，加权系数 `α=0.8`。
- **动态 INT8 量化**：
  - 对线性层执行训练后动态量化：权重存为 INT8，激活值根据运行时分布动态量化，其余操作保持浮点精度。
  - 无需重新训练，显著削减存储需求。

### 3. 实验设计
- **数据集**：Audio-Visual Event (AVE) 数据集，包含 4,143 个 YouTube 视频片段，覆盖 28 类事件（乐器、动物、车辆、人类活动等），每段约 10 秒，含同步音频和视频。
- **划分与预处理**：按官方协议划分为训练/验证/测试集；视频均匀切为 10 个 1 秒片段，离线提取 VideoMAE 和 AST 特征并缓存，保证时序同步。
- **对比方法**：
  - 教师模型（原始高容量混合交叉注意力网络）
  - 知识蒸馏后的学生模型（压缩架构）
  - 动态 INT8 量化后的学生模型（进一步压缩）
- **评价指标**：测试准确率、测试损失、加权精确率、加权召回率、加权 F1 值、可训练参数量、模型大小、检查点大小、压缩比、参数量减少百分比、准确率下降等。

### 4. 资源与算力
- 文中未明确提及所使用的 GPU 型号、数量及具体训练时长。仅说明使用 PyTorch 实现，优化器为 AdamW，初始学习率 `1e-4`，权重衰减 `1e-4`，批次大小为 4，最多训练 80 轮，采用 ReduceLROnPlateau 调度器和早停策略。显存和算力需求未提供，这可能限制了对其边缘部署可行性的全面评估。

### 5. 实验数量与充分性
- 主要实验为单数据集（AVE）上一组核心对比：教师、蒸馏学生、量化学生。未进行多数据集验证，也未见消融实验（如单独测试各压缩步骤的影响、不同蒸馏温度或损失权重的敏感性分析）。实验侧重最终压缩效果，虽能展示参数和模型尺寸的显著降低及精度小幅损失，但缺乏对方法组件贡献度的系统性拆解，在实验充分性和泛化性方面存在不足。

### 6. 论文的主要结论与发现
- 所提压缩框架使多模态融合模块可训练参数减少 59.06%（从 6.86M 降至 2.81M），分类准确率仅下跌 2.14%（84.19% → 82.05%）。
- 知识蒸馏有效弥补了架构压缩带来的容量损失，训练曲线表明学生模型收敛稳定，未出现严重过拟合。
- 动态 INT8 量化进一步将模型大小从 10.71 MB 压缩至 2.04 MB，检查点大小从 32.19 MB 降至 4.25 MB，准确率仍保持 81.20%（相比教师下降 2.99%）。
- 量化后推理延迟略有增加，归因于动态量化在 CPU 上的额外开销，但显著的存储压缩使其适合内存受限的边缘设备。
- 整体框架在识别性能与部署效率之间取得了良好的权衡。

### 7. 优点
- **架构感知压缩**：不改变跨模态交互拓扑，针对融合模块进行有针对性的轻量化，保留了多模态推理能力。
- **多技术协同**：将知识蒸馏与训练后量化无缝衔接，无需重新训练，降低了部署门槛。
- **显著压缩效果**：参数量和模型存储减少幅度大，精度损失小（参数减少近 60%，量化后模型仅 2 MB）。
- **保持特征提取器不变**：VideoMAE 和 AST 骨干冻结且离线提取特征，简化了训练流程，聚焦融合网络的压缩。
- **实验透明性**：提供完整的性能指标对比和压缩比，清晰展示各阶段模型的变化。

### 8. 不足与局限
- **单一数据集评估**：仅在 AVE 上测试，未在其他更大规模或多场景视听事件基准上验证泛化能力。
- **缺乏消融研究**：未单独分析架构缩减、蒸馏和量化各自的贡献，难以判断最佳组合或各步骤对精度的边际影响。
- **未探讨其他压缩策略**：未与剪枝、低秩分解等常见压缩方法进行对比，缺乏相对优势的实证证据。
- **推理延迟不降反升**：量化后 CPU 推理速度反而变慢，论文归因为动态量化开销，但未给出解决方案或在不同硬件上（如移动 GPU、NPU）的延迟测试。
- **算力信息缺失**：未报告训练所用的 GPU、显存消耗和收敛时间，难以评估实际资源需求。
- **量化方式单一**：仅采用训练后动态 INT8 量化，未尝试量化感知训练或更低比特量化（如 INT4），压缩潜力可能未完全挖掘。
- **不压缩骨干网络**：只压缩融合模块，对于整体部署来说，骨干特征提取器（VideoMAE、AST）仍然庞大，边缘设备上实时提取特征仍有困难。

（完）
