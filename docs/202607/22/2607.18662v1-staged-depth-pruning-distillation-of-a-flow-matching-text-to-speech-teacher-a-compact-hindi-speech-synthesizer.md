---
title: "Staged Depth-Pruning Distillation of a Flow-Matching Text-to-Speech Teacher: A Compact Hindi Speech Synthesizer"
title_zh: 分阶段深度剪枝蒸馏流匹配文本到语音教师模型：一种紧凑的印地语语音合成器
authors: Sivateja Trikutam
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.18662v1"
tags: ["query:speech-model"]
score: 9.0
evidence: 通过深度剪枝蒸馏压缩TTS教师模型提升合成效率
tldr: 针对极低数据量(17.6小时)下印地语TTS训练难题，提出分阶段深度剪枝蒸馏法，将337M流匹配教师模型压缩。仅剪枝Transformer块数，逐步微调并利用ASR词错误率门控确保质量。190M学生模型在未见数据上词错误率为0.00，可在6GB GPU实时运行，131M仍鲁棒。方法为资源受限语言提供高效数据高效的压缩方案，并修复了库一致性问题。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18662v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1121, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18662v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1481, \"height\": 259, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18662v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1489, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18662v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1644, \"height\": 261, \"label\": \"Table\"}]"
motivation: 极低数据量（17.6小时）下从头训练印地语TTS失败，需从大模型压缩出可用小模型。
method: 分阶段深度剪枝蒸馏，仅减Transformer块数，逐步微调并以ASR词错误率门控，保持宽度不变。
result: 190M模型在未见数据词错误率0.00，6GB GPU实时运行，131M鲁棒，102M容量悬崖。
conclusion: 方案为数据稀缺语言提供高效TTS压缩策略，模型紧凑，并修复库一致性问题。
---

## 摘要
我们提出了一种在严重数据预算（约17.6小时）下，通过蒸馏大型流匹配教师模型（IndicF5，337M参数的DiT）来构建紧凑印地语文本到语音（TTS）模型的实用方法。在此数据量上从头训练小型模型完全失败。相反，我们通过仅进行深度剪枝来从教师模型热启动学生模型：保持教师模型的宽度、文本维度、注意力头和梅尔/文本I/O固定，以便所有非块张量一对一复制，并保留均匀间隔的子集变压器块。我们首先测量教师模型能容忍多少深度（在减少27%块时仍近功能，但超过50%则崩溃），然后逐步下降（22→16→12→8→6个块），每次剪枝后重新微调，每一步都由客观ASR词错误率（WER）检查把关。所得学生模型在未见句子上的WER达到0.00，参数量为249M和190M，并稳健地下探至131M；在102M处我们观察到一个明显的容量悬崖，我们将其归因于数据预算而非方法。我们还记录了两个训练/推理特征和库对等失败（梅尔滤波器组和旋转嵌入库版本），它们会无声地降低音频质量，并提供了一个版本无关的修复。该方法生成高质量的印地语语音，可在6 GB笔记本电脑GPU上实时运行。一个独立的50句FLEURS基准对比了发布的190M学生模型、其教师模型和MMS-TTS-hin。

## Abstract
We present a practical recipe for building a compact Hindi text-to-speech (TTS) model by distilling a large flow-matching teacher (IndicF5, 337M-parameter DiT) under a severe data budget (~17.6 hours). Training a small model from scratch on this much data fails outright. Instead we warm-start the student from the teacher by pruning depth only: keeping the teacher's width, text dimension, attention heads, and mel/text I/O fixed so all non-block tensors copy one-to-one, and retaining an evenly-spaced subset of transformer blocks. We first measure how much depth the teacher tolerates (it remains near-functional at -27% blocks but collapses past -50%), then descend gradually (22 -> 16 -> 12 -> 8 -> 6 blocks), re-fine-tuning after each prune, with each step gated by an objective ASR word-error-rate (WER) check. The resulting students reach WER 0.00 on unseen sentences at 249M and 190M parameters, and remain robust down to 131M; at 102M we observe a clear capacity cliff that we attribute to the data budget rather than the recipe. We also document two train/inference feature- and library-parity failures (mel filterbank and rotary-embedding library versions) that silently degrade audio, and a version-independent fix. The method yields a high-quality Hindi voice that runs in real time on a 6 GB laptop GPU. An independent 50-sentence FLEURS benchmark compares the released 190M student against its teacher and MMS-TTS-hin.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的结构化深度总结。

### 1. 核心问题与研究背景

*   **核心问题**：如何在极低的训练数据预算（约17.6小时）下，构建一个高质量、紧凑型的印地语文本到语音（TTS）模型，以便在消费级设备上部署。
*   **研究动机**：
    *   **大模型与小模型鸿沟**：当前印地语TTS生态呈现两极分化。强大的模型（如IndicF5，约3.5亿参数）体积庞大，难以在设备端部署；而小型模型（如MMS-TTS，3600万参数）的语音质量显著较低。论文旨在填补“约1亿-1.9亿参数”这一中间地带的高质量模型空白。
    *   **数据稀缺挑战**：从头开始训练一个小型流匹配TTS模型需要海量数据（通常远超17.6小时）。论文作者的初步尝试（用相同数据训练一个7900万参数的模型）完全失败，生成了无法理解的语音，证明了在该数据预算下“从零训练”的不可行性。

### 2. 方法论：分阶段深度剪枝蒸馏

论文提出了一种名为“分阶段深度剪枝蒸馏”的实用方法，其核心思想是利用大型教师模型的权重作为优越的起点，通过逐步“瘦身”而非“重塑”来压缩模型。

*   **核心思想：仅深度热启动**
    *   **固定宽度，仅减深度**：为了使教师模型的权重能被直接复用，学生模型继承了教师模型的所有维度（宽度、文本维度、注意力头等），仅减少Transformer块的层数（深度）。这使得所有非块张量可以实现1:1复制。
    *   **均匀间隔剪枝**：从教师模型的22个Transformer块中，按照均匀间隔原则，选取一个子集来构建学生模型，并确保首尾块始终被保留。
    *   **剪枝容限分析**：在微调前，通过测量**未训练**剪枝模型的输出幅度（RMS），来评估Transformer块减少的极限，发现移除约27%的块模型仍能“发声”，但移除超过50%则输出崩溃。这为“逐步下降”的策略提供了依据。

*   **关键技术细节：分阶段蒸馏阶梯**
    *   **逐步下降**：遵循预定义的路径（22→16→12→8→6个块），每次剪枝后都在教师模型生成的语料库上进行重新微调，以恢复性能。
    *   **ASR-WER门控**：每个阶段的微调是否达标，由客观的ASR词错误率（WER）指标决定。只有WER恢复到接近前一阶段水平的模型，才会被用于下一步剪枝。这构成了一个质量保证的闭环。
    *   **优化细节**：微调采用条件流匹配目标，使用学习率预热、动态批次、指数移动平均（EMA）权重。热启动效果显著，训练损失起点低，数千步内即可产生可理解的语音。

### 3. 实验设计与评估

*   **数据集**：
    *   **训练/验证数据**：一个约17.6小时（9999条话语）的内部语料库，完全由教师模型IndicF5合成。文本来源于真实印地语文本和模板生成的句子。
    *   **独立基准测试数据**：从FLEURS数据集的印地语测试集中抽取50个未见过的句子（10-22个词），用于最终的比较基准测试。

*   **基准与对比方法**：
    *   **自身阶段对比**：对比不同深度（249M, 190M, 131M, 102M参数）学生模型的性能。
    *   **外部模型对比**：在50句FLEURS基准上，将发布的190M学生模型与教师模型（IndicF5，337M）和一个强基线小型模型（MMS-TTS-hin，36M）进行比较。

*   **评估指标**：
    *   **词/字符错误率**：使用两个开源ASR模型（IndicWav2Vec-Hindi用于阶段门控，Vakyansh Hindi wav2vec2用于独立基准）转录合成语音，计算与原始文本的WER和CER。
    *   **说话人相似度**：计算合成语音与参考语音的ECAPA-TDNN嵌入的余弦相似度。
    *   **预测自然度**：使用UTMOS（一种预测MOS的模型）评估语音的自然度。
    *   **实时率**：在MPS后端或CPU上测量合成时间与音频时长的比率。

### 4. 资源与算力

*   **算力细节**：论文未明确提及训练使用的GPU型号、数量或具体训练时长。但提到了190M学生模型可以在**6 GB显存的笔记本电脑GPU上实时运行**，独立基准测试中的RTF是在Apple M4芯片上测量的，这体现了模型部署时的轻量化优势。

### 5. 实验数量与充分性

*   **实验组数**：论文的核心实验是“分阶段蒸馏阶梯”，包含了4个深度级别（16, 12, 8, 6）的模型，构成了一个完整的消融研究，用以观察参数量与性能的变化关系。
*   **实验充分性与客观性**：
    *   **充分性**：实验设计逻辑清晰，从剪枝容限分析开始，到逐步蒸馏、门控检查，再到最终的独立基准测试，步骤完整且有说服力。同时，论文还记录并解决了两个关键的部署一致性问题，增强了结论的实用价值。
    *   **客观性**：评估大量使用客观指标（WER/CER, SIM, RTF）和可复现的预测指标（UTMOS），避免了主观听力测试带来的偏差。使用FLEURS标准数据集和多种ASR系统进行交叉验证，增强了结果的可靠性。在对比MMS-TTS时，能客观地指出其语音克隆功能缺失导致的相似度低，而非模型缺陷。

### 6. 主要结论与发现

*   **方法有效**：提出的分阶段深度剪枝蒸馏法成功地从大型流匹配TTS教师模型中蒸馏出高质量的学生模型，解决了极低数据量下从头训练失败的问题。
*   **模型性能**：190M参数的学生模型在未见句子上的WER达到0.00，在50句FLEURS基准测试中，保留了教师模型约96%的自然度和说话人相似度，而参数量仅为56%，推理速度快1.8倍。
*   **容量悬崖**：在131M参数时模型依然鲁棒，但在102M参数处观察到了明显的性能“悬崖”，论文将此归因于数据预算（17.6小时）的限制，而非方法本身失效。这表明对于给定的数据量，模型容量存在一个下限。
*   **部署教训**：识别并修复了两个关键的“训练/推理不一致”问题（梅尔滤波器组和旋转位置嵌入的库版本差异），并提出了版本无关的修复策略，这对TTS模型的工程化部署具有普遍参考意义。

### 7. 优点

*   **强数据效率**：在仅17.6小时训练数据这个极具挑战性的条件下，实现了从教师到学生的有效知识迁移，这是最大的亮点。
*   **方法简洁有效**：“仅深度热启动”设计巧妙，让教师权重能最大程度复用，降低了训练难度和计算开销。“分阶段下降”策略结合ASR-WER门控，确保了压缩过程的稳定性和最终质量。
*   **实验扎实**：剪枝容限分析提供了理论依据；阶段门控提供了质量控制；独立的公开基准测试确保了评估的客观性。
*   **工程价值高**：详细记录了部署中的实际问题和解决方案，对产业界应用很有帮助。最终模型在消费级硬件上实时运行，兼具高质量和实用性。

### 8. 不足与局限

*   **数据局限性**：训练数据完全由教师模型合成，且仅使用单一参考说话人。这可能导致模型继承了教师模型的偏差，并在语音表现力、风格多样性上存在天花板。
*   **评估局限性**：评估均基于自动指标（ASR, UTMOS）而非人类主观评测，对自然度、韵律等感知维度的评估不够全面。
*   **语言单一性**：方法仅在印地语上验证，其在其他语言、尤其是不同语系语言上的泛化能力有待考证。
*   **已知瑕疵**：论文坦承模型存在句尾杂音（可通过能量裁剪缓解）和句首Token处理困难等流匹配TTS的常见问题。
*   **容量悬崖未解决**：论文指出了102M模型失败的“容量悬崖”现象，并将其归因于数据，但并未提出在此极低数据量下突破该悬崖的解决方案，仅将此作为未来工作方向（增加语料）。

（完）
