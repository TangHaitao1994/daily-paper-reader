---
title: Jointly Improving Dialect Identification and ASR in Indian Languages using Multimodal Feature Fusion
title_zh: 使用多模态特征融合联合改进印度语言中的方言识别与自动语音识别
authors: "Saurabh Kumar, Amartyaveer, Prasanta Kumar Ghosh"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.02862v1"
tags: ["query:speech-model"]
score: 9.0
evidence: 多模态融合改进方言辨识与ASR
tldr: "针对印度语言低资源且方言差异显著的特点，现有独立优化方法导致 ASR 与方言识别性能妥协。本文提出多模态特征融合框架，联合训练瓶颈编码器提取方言特征和 RoBERTa 处理 CTC 嵌入，经门控融合与注意力精炼后增强 Conformer 表示。在 8 种语言 33 种方言测试中，方言识别准确率 81.63%，ASR 平均 CER 4.65%、WER 17.73%，证明了联合建模的有效性，为低资源多方言场景提供参考。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02862v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 741, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02862v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1743, \"height\": 474, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02862v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 768, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02862v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1592, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02862v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 765, \"height\": 194, \"label\": \"Table\"}]"
motivation: 印度多语言方言场景独立优化 ASR 和 DID 存在性能权衡，需联合建模方法。
method: 融合框架用瓶颈编码器提取方言特征、RoBERTa 处理 CTC 嵌入，经门控与注意力融合精炼后拼接 Conformer 输出增强 ASR。
result: "在 8 种语言 33 种方言上，方言识别准确率达 81.63%，ASR 平均 CER 4.65%、WER 17.73%。"
conclusion: 多模态特征融合有效联合提升 ASR 与 DID，缓解性能妥协，为低资源方言处理提供新思路。
---

## 摘要
自动语音识别（ASR）和方言识别（DID）对于许多低资源且方言差异显著的印度语言至关重要。现有方法通常单独优化ASR或DID，导致性能上有所权衡。本文提出了一种联合改进ASR和DID的多模态框架。该方法使用瓶颈编码器从基于Conformer的语音表示中提取方言特征，并使用RoBERTa编码器处理ASR生成的CTC嵌入。门控机制融合这些特征，随后由注意力编码器精化表示。学习到的嵌入与Conformer输出拼接，以增强ASR特征。在包含三十三种方言的八种印度语言上评估，我们的方法实现了平均81.63%的DID准确率，以及平均4.65%的字符错误率和17.73%的词错误率。这些结果凸显了我们的方法在联合ASR-DID建模中的有效性。

## Abstract
Automatic Speech Recognition (ASR) and Dialect Identification (DID) are crucial for Indian languages, many of which are low-resource and exhibit significant dialectal differences. Existing methods often optimize ASR or DID individually, resulting in performance trade-offs. In this work, we propose a multimodal framework that jointly improves ASR and DID. Our method employs a Bottleneck Encoder to extract dialectal features from Conformer-based speech representations and a RoBERTa encoder to process ASR-generated CTC embeddings. A gating mechanism merges these features, followed by an attention encoder to refine the representations. The learned embeddings are concatenated with Conformer outputs to enhance ASR features. Evaluated on eight Indian languages with thirty-three dialects, our method achieves an average DID accuracy of 81.63% and average CER and WER of 4.65% and 17.73%, respectively. These results highlight the effectiveness of our method for joint ASR-DID modeling.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：在低资源、方言差异显著的印度语言中，自动语音识别（ASR）与方言识别（DID）通常被独立优化，导致一方性能提升时另一方下降，存在明显的性能权衡。
- **整体含义**：论文旨在设计一个多模态特征融合框架，通过联合训练，同时提升ASR转录质量和DID准确率，避免传统方法中因方言识别错误而拖累ASR的弊端。

### 2. 提出的方法论
- **总体架构**：分为 ASR 模块和 DID 模块。语音先经 SSL 编码器和 Conformer 编码器提取表示；DID 模块利用两种编码器分别处理语音和文本线索，经门控融合与注意力精炼后，将方言感知嵌入反馈给 ASR 模块，最后通过 CTC/Attention 解码器输出识别结果。
- **关键组件**：
  - **瓶颈编码器（Bottleneck Encoder）**：从 Conformer 输出中捕捉语音方言特征，采用 1D 卷积 + 瓶颈 Transformer 保留时序动态。
  - **RoBERTa 编码器**：将 Conformer 输出经线性投影+Softmax得到的 CTC 嵌入映射到低维空间，提取文本侧方言线索。
  - **门控融合机制**：通过可学习权重动态融合两种模态特征：
    - 门控权重 \( G = \sigma(W_g [H_{\text{bottleneck}}, H_{\text{rob}}] + b_g) \)
    - 融合输出 \( H_{\text{fused}} = G \odot H_{\text{rob}} + (1 - G) \odot H_{\text{bottleneck}} \)
  - **注意力编码器**：对融合后的表示进一步精炼，得到帧级别嵌入，再经平均池化和线性分类器输出方言类别。
- **融合与梯度隔离**：方言嵌入与 Conformer 输出拼接后进入 ASR 后续处理，但拼接前对嵌入进行 `detach` 操作，阻断从 DID 分支回传的梯度，保证联合训练稳定。
- **损失函数**：\( L = \lambda_{\text{CTC}} L_{\text{CTC}} + (1 - \lambda_{\text{CTC}}) L_{\text{ATT}} + \gamma_{\text{CE}} L_{\text{CE}} \)，其中 \(\lambda_{\text{CTC}}\) 平衡 CTC/Attention 损失，\(\gamma_{\text{CE}}\) 控制 DID 交叉熵损失的权重。
- **与以往工作的差异**：不再将方言 ID 作为文本前缀预置到训练文本中，避免了错误方言标签对 ASR 的负面影响。

### 3. 实验设计
- **数据集**：RESPIN 数据集中的 8 种印度语言（Bhojpuri、Bengali、Chhattisgarhi、Kannada、Magahi、Maithili、Marathi、Telugu），共 33 种方言。训练集约 140–175 小时，开发集约 2 小时，测试集约 6–8 小时，均为朗读语音。数据划分遵循已有工作 [11]。
- **Benchmark 与对比方法**：
  - `Base-ASR`：纯 ASR 基线，无 DID 辅助任务。
  - `ASR-DID`：在文本前加入方言 ID 进行训练。
  - `ASR-DID-SC`：增加 CTC 自条件（ASR-DID 变体）。
  - `ASR-DID-AUX`：为 DID 任务增加辅助 CTC 损失。
  - `ASR-DID-ROB`：基线方法（[11] 的方案），使用方言 ID 前置的 ASR 抽取多模态特征进行 DID。
  - `ASR-ROB`：仅使用 RoBERTa 编码器（从 CTC 嵌入）的联合模型。
  - `ASR-BN`：仅使用瓶颈编码器（从语音）的联合模型。
  - `ASR-BN-ROB`（提出的完整模型）：融合瓶颈编码器、RoBERTa 编码器与注意力精炼。
- **评价指标**：DID 准确率、字符错误率（CER，不含空格）、词错误率（WER）。

### 4. 资源与算力
- 论文中**未明确说明 GPU 型号、数量或训练时长**等具体算力配置。仅提到在 ESPnet 框架下实现，批大小动态调整至 6M batch bins，使用 Adam 优化器，学习率 0.002，warmup 15k 步，early stopping patience 为 5，上游 IndicWav2Vec 模型参数冻结。

### 5. 实验数量与充分性
- **实验组数**：共报告了 8 种系统在所有 8 种语言上的 DID 准确率、CER 和 WER，以及正确/错误 DID 预测下的 ASR 表现。此外，通过比较 ASR-ROB、ASR-BN 和 ASR-BN-ROB 可视为消融实验，验证不同模态融合的作用。
- **充分性与公平性**：
  - 对比方法覆盖了主流的多任务 ASR-DID 策略，并基于统一的数据划分和训练超参，比较公平。
  - 提供了统计显著性检验（配对 T 检验），证实 DID、CER、WER 上的提升具有统计学意义。
  - 不仅给出平均指标，还展示各语言的详细结果和混淆矩阵，并分析了错误 DID 预测对 ASR 的特殊影响，实验分析较为深入。
  - 实验仍局限于单一数据集（RESPIN 子集）和朗读语音，缺乏对自发语音、跨领域数据的考察，但仍能有效支撑核心论点。

### 6. 主要结论与发现
- 提出的 ASR-BN-ROB 模型在 8 种语言上平均 DID 准确率达到 81.63%，明显优于传统的 ASR-DID-ROB 基线（80.74%），并且在大多数语言上取得最佳或次佳结果。
- ASR 性能同样提升：平均 CER 从基线的 4.76% 降至 4.65%，平均 WER 从 18.16% 降至 17.73%，实现了 ASR 与 DID 的同步增益，打破了以往方法中 DID 提升伴随 ASR 下降的困境。
- 错误方言预测对 ASR 的负面影响得到缓解：在基线模型中，错误 DID 的 CER/WER 分别高达 6.01%/21.85%，而所提模型将该组错误下降至 5.68%/20.72%，相对降幅约 5.5%。
- 多模态特征融合和梯度隔离策略是成功的关键，特别是瓶颈编码器与 RoBERTa 编码器的自适应门控结合，改善了方言嵌入的鲁棒性。

### 7. 优点
- **多模态融合与梯度隔离**：巧妙结合语音和 CTC 文本两种模态，并通过 detach 操作避免梯度冲突，实现 ASR 与 DID 的双赢。
- **避免方言 ID 前置**：不必在文本中插入方言标签，消除了错误标签带来的错误上下文，使模型在方言预测错误时仍保持较好的 ASR 性能。
- **模块化设计**：瓶颈编码器保留时序信息，RoBERTa 引入上下文语言线索，注意力编码器进一步精炼，各组件功能清晰，可独立消融。
- **实验充分**：多语言、多方言对比，消融分析，统计检验和错误案例分析，论证完整。
- **公开代码和模型**：提供了可复现的链接，增加了可信度。

### 8. 不足与局限
- **数据集局限**：仅在 RESPIN 朗读语音数据集上测试，未在其他语种、方言或自发语音场景下验证泛化能力。
- **算力报告缺失**：未说明 GPU 资源和训练时间，难以评估实际部署的计算开销。
- **模型参数量**：所提模型参数量（31.37M）较基线（约 25–29M）有所增加，虽然增幅不大，但未分析推理延迟或轻量化潜力。
- **应用范围**：主要面向方言识别与 ASR 的联合优化，对于动态方言切换、代码混合等更复杂场景尚未涉及。
- **方言类别有限**：每种语言的方言数有限（33 种方言分布 8 种语言），对语言内方言更细粒度的区分能力未进一步探讨。

（完）
