---
title: Video = World + Event Stream
title_zh: 视频 = 世界 + 事件流
authors: "Lianghua Huang, Zhi-Fan Wu, Yupeng Shi, Wei Wang, Mengyang Feng, Cheng Yu, Chen Liang, Junjie He, Chen-Wei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Yuxiang Bao, Yuzheng Wang, Zhiwei Lin, Zoubin Bi"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15038v1"
tags: ["query:speech-model"]
score: 8.0
evidence: 融合语音和声音的视频+音频多模态预训练模型
tldr: 实时视频流交互中，稳定世界场景与动态事件流常紧密耦合，构成统一建模瓶颈。Wan-Streamer v0.3将其重构为“世界+事件流”框架，明确解耦持久语境与时序变化。通过海量真实视频上的预训练，模型学习预测世界如何实时响应，该能力可泛化至全双工音视频对话等实时下游任务。实例中，模型以视觉-语言-动作范式将多模态输入转化为语音和动作，实现640x368分辨率下25fps视频流、约200ms模型延迟和约550ms端到端交互延迟。这一世界-事件解耦范式为实时交互AI提供了高效通用的预训练方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15038v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1721, \"height\": 673, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15038v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1720, \"height\": 776, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15038v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1709, \"height\": 686, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15038v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1721, \"height\": 665, \"label\": \"Table\"}]"
motivation: 实时视频流交互需同时建模持久世界与动态事件，但传统方法缺乏统一视角。
method: 提出“视频=世界+事件流”框架，通过大规模视频预训练预测世界响应，并实例化为实时全双工模型。
result: "在实时全双工音视频交互中实现640x368@25fps、约200ms模型延迟、约550ms总交互延迟的高效性能。"
conclusion: 世界-事件解耦框架为实时交互式AI提供了高效、通用的预训练范式，具有广泛适用潜力。
---

## 摘要
我们提出了 Wan-Streamer v0.3，它在单一组织视角下重新构建了我们的原生流式交互模型：视频是一个世界加上一个事件流。世界是视频展开的持久上下文，包括环境、场景、主体、环境声学条件、声音特征以及其他相对稳定的条件。事件流是世界内随时间变化的一切，包括场景或环境变化、主体行为、语音和其他声音。这产生了一个基于大量真实视频的通用预训练任务：给定一个世界和传入的输入，实时预测世界如何移动、变化和响应。由此产生的能力可以被专门化到广泛的实时下游任务中。我们在实时全双工视听交互上实例化它，其中事件流是智能体的语音以及自由形式的行为。从功能上讲，该模型的多模态理解过程类似于视觉-语言-行动：它将多模态用户输入映射到语言形式的语音和行为动作。Wan-Streamer v0.3 保持了 v0.2 的运行参数：640x368 视频，25 帧每秒，160 毫秒的流式单元，约 200 毫秒的模型侧响应延迟，以及在 350 毫秒双向网络预算下约 550 毫秒的总交互延迟。

## Abstract
We present Wan-Streamer v0.3, which reframes our native-streaming interaction model under a single organizing view: a video is a world plus an event stream. The world is the persistent context in which a video unfolds, including the environment, scene, subjects, ambient acoustic conditions, voice characteristics, and other relatively stable conditions. The event stream is everything that changes over time within that world, including scene or environmental changes, subject behavior, speech, and other sounds. This yields a general-purpose pretraining task over large amounts of real video: given a world and incoming input, predict how the world moves, changes, and responds in real time. The resulting competence can be specialized to a broad family of real-time downstream tasks. We instantiate it on real-time full-duplex audio-visual interaction, where the event stream is the agent's speech together with free-form behavior. Functionally, the model's multimodal understanding process is vision-language-action-like: it maps multimodal user input to language-form speech and behavior actions. Wan-Streamer v0.3 preserves the v0.2 operating point: 640x368 video at 25 FPS, a 160 ms streaming unit, approximately 200 ms model-side response latency, and approximately 550 ms total interaction latency under a 350 ms bidirectional network budget.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：先前的 Wan-Streamer v0.1/v0.2 将实时全双工视听交互作为一个端到端的流式目标进行训练，模型直接从交互数据学习。v0.3 试图回退一步，探索底层的通用能力究竟是什么，并提出一种更通用的预训练范式。
- **核心观点**：任何视频都可以被解读为“一个世界 + 一个事件流”。世界是持续稳定的上下文（场景、环境、人物、声音特征等），事件流是世界内随时间变化的一切（行为、语音、声音等）。
- **整体含义**：通过将视频分解为世界和事件流，可以定义一个通用的预训练任务：给定一个世界和输入，实时预测世界如何演变、响应。该能力可广泛迁移至多种实时下游任务，而不仅仅是交互对话。

## 2. 论文提出的方法论

- **世界‑事件分解**
    - 世界 $W$ 表示成一种灵活的、与片段或任务相关的结构化记录（公式1），包含视觉场景、声学环境、角色身份等持久上下文。
    - 事件 $e_k$ 是一个时间局部化记录，包括活跃区间、自由形式的描述以及关联的角色（公式2）。
- **流式世界‑事件因子分解**
    - 将世界显式地作为一级条件对象，事件流按因果顺序预测：$p_\theta(e_{1:K} | W, x_{1:K}) = \prod_{k=1}^K p_\theta(e_k | W, x_{\le k}, e_{<k})$（公式3）。其中 $x_{\le k}$ 是驱动流的输入（预训练时为观测到的过去，交互时为用户流式输入）。
    - 离散部分（语言、行为指令）用下一个标记预测优化，连续音视频潜变量用条件流匹配生成，两者共用同一上下文。
- **预训练与下游迁移**
    - 在大规模真实视频上预训练，学习“世界如何随事件流变化”这一通用动态。此能力可被特化到漫游、视听交互、具身操控等多种实时任务，只需改变世界配置和输入驱动方式。本工作仅实例化到全双工视听交互。
- **自由形式行为角色扮演**
    - 智能体的输出事件流在语言标记流中交织口语和行为指令，行为以自然语言形式写在括号内（如 `(frowning slightly) What did you say?`）。
    - 这种开放词汇的行为描述取代了固定动作集，并与语音共享同一标记流，时序和交织由模型联合学习，无需外部调度器。
- **视觉‑语言‑动作式理解**
    - 功能上模型类似 VLA 流程：以世界为条件，持续将用户的文本、音频、视频映射为语言形式的语音和行为动作，这些动作同步驱动联合音视频潜变量生成。
- **架构与拓扑复用**
    - 保留了 v0.2 的 Ulysses 上下文并行 performer 服务拓扑，没有引入新的关键延迟路径。

## 3. 实验设计

- **实验场景**：仅聚焦于实时全双工视听交互下游任务，未对其他可能的下游任务（如漫游、具身操控）进行实验。
- **基准与对比方法**：
    - 主要与自身前序版本 v0.1、v0.2 做定性对比，在表1中列出分辨率、帧率、延迟、行为格式等维度的变化。
    - 未使用公开的交互质量基准（如对话自然度、动作合理性等量化指标），没有与其他独立模型进行系统对比。
- **评测内容**：
    - **延迟**：沿用与 v0.1/v0.2 相同的边界，报告模型侧信号‑信号延迟约 200 ms，总交互延迟约 550 ms（含 350 ms 双向网络预算）。
    - **定性观察**：在生成的 640×368 对话中，观察到智能体能在说话的同时执行开放词汇行为（面部表情、姿势变化、对声音的反应等），行为与语音同步、与场景和角色一致，空闲时保持稳定聆听状态。

## 4. 资源与算力

- **论文中未明确说明**：未提供任何 GPU 型号、数量、训练时长、批大小、数据集规模等信息。仅提及推理服务拓扑沿用了 v0.2 的 Ulysses 上下文并行 performer，但未指出训练和推理所需的具体算力资源配置。

## 5. 实验数量与充分性

- **实验数量极少**：
    - 仅包含一个版本对比表格（v0.1/v0.2/v0.3）和一段定性行为观察描述。
    - 没有消融实验（如是否有世界‑事件分解的影响、行为指令格式的影响等），也没有对预训练数据规模、预训练效果、迁移学习能力的量化分析。
- **充分性与客观性不足**：
    - 缺乏定量评价指标（例如动作‑语音同步率、行为合理性评分、用户研究等），仅凭定性观察难以客观度量模型提升。
    - 未与领域内其他实时交互模型（如 Doubao、GPT-4o 等）进行公平对比，版本比较仅在自身体系内。
    - 对核心贡献“通用预训练 + 下游特化”的验证仅限于一个下游任务，未展示其他任务的适应性，实验覆盖面非常有限。

## 6. 论文的主要结论与发现

- Wan-Streamer v0.3 将视频重新定义为世界 + 事件流，以此定义了一种通用预训练任务，并在实时全双工视听交互中成功实例化。
- 通过角色扮演格式，智能体可以在不增加关键延迟的情况下，用自然语言产生开放词汇的自由形式行为，动作与语音同步、与场景和角色身份一致。
- 模型保持了与 v0.2 完全相同的低延迟运行参数（640×368@25 FPS，~200 ms 模型侧延迟，~550 ms 总交互延迟）。

## 7. 优点

- **概念简洁且通用**：世界‑事件分解为视频理解与生成提供了一个统一的视角，理论上有望支持多种实时交互任务。
- **行为表达力大幅增强**：将固定动作集替换为自然语言行为指令，使智能体能够执行更丰富、更符合情境的动作。
- **工程成本低**：行为指令以短文本标记形式插入同一语言流，无需额外延迟路径，直接继承 v0.2 的服务拓扑和延迟预算。
- **端到端同步**：语音、行为、视频潜变量在同一流匹配框架下联合生成，天然保持同步。

## 8. 不足与局限

- **实验验证严重不足**：仅在一个下游任务上做了定性演示，缺少定量评估、消融实验和与其他方法的系统对比，难以评判方法的实际优劣。
- **算力与训练细节缺失**：未披露训练资源、数据规模、训练策略等重要信息，可复现性低。
- **下游迁移未实证**：声称支持漫游、具身操控等多种任务，但并未提供任何实验证据，适用范围仅停留在设想层面。
- **行为生成可靠性未检验**：开放词汇行为指令可能产生不符合物理规律或与场景冲突的动作，但论文未讨论安全过滤或可控性研究。
- **评估维度单一**：仅关注延迟，忽略了交互质量、语音‑行为一致性、长期稳定性等更核心的用户体验指标。

（完）
